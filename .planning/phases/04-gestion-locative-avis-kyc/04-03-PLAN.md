---
phase: 04-gestion-locative-avis-kyc
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/web/components/avis/AvisCard.tsx
  - apps/web/components/avis/AvisForm.tsx
  - apps/web/components/avis/StarRating.tsx
  - apps/web/app/api/avis/route.ts
  - apps/web/app/api/avis/[id]/reponse/route.ts
  - apps/web/app/(client)/avis/page.tsx
  - apps/web/app/(pro)/avis/page.tsx
autonomous: true
requirements: [AVIS-01, AVIS-02, AVIS-03]

must_haves:
  truths:
    - "Un locataire peut laisser un avis (note 1-5 + commentaire) pour un propriétaire après une réservation"
    - "Un propriétaire peut laisser un avis pour un locataire après une réservation"
    - "Un propriétaire peut répondre à un avis reçu (reponse_cible)"
    - "Les avis sont visibles publiquement sur les profils (note moyenne calculée)"
    - "La contrainte unique(auteur_id, cible_id, reservation_id) empêche les doublons"
    - "Le locataire voit ses avis reçus et les réservations en attente d'avis"
    - "Le propriétaire voit ses avis reçus avec bouton de réponse"
  artifacts:
    - path: "apps/web/components/avis/StarRating.tsx"
      provides: "Composant étoiles interactif (input) ou readonly (affichage)"
      exports: [StarRating]
    - path: "apps/web/components/avis/AvisCard.tsx"
      provides: "Affiche un avis avec note, commentaire, réponse propriétaire"
      exports: [AvisCard]
    - path: "apps/web/components/avis/AvisForm.tsx"
      provides: "Formulaire de soumission d'avis (note + commentaire)"
      exports: [AvisForm]
    - path: "apps/web/app/api/avis/route.ts"
      provides: "POST — crée un avis, vérifie réservation terminée"
      exports: [POST]
    - path: "apps/web/app/api/avis/[id]/reponse/route.ts"
      provides: "PATCH — ajoute reponse_cible, vérifie que l'utilisateur est la cible"
      exports: [PATCH]
    - path: "apps/web/app/(client)/avis/page.tsx"
      provides: "Page locataire — avis reçus + réservations à noter"
    - path: "apps/web/app/(pro)/avis/page.tsx"
      provides: "Page propriétaire — avis reçus + formulaire de réponse inline"
  key_links:
    - from: "apps/web/components/avis/AvisForm.tsx"
      to: "apps/web/app/api/avis/route.ts"
      via: "fetch POST /api/avis"
    - from: "apps/web/app/(pro)/avis/page.tsx"
      to: "apps/web/app/api/avis/[id]/reponse/route.ts"
      via: "fetch PATCH /api/avis/{id}/reponse"
    - from: "apps/web/app/api/avis/route.ts"
      to: "supabase avis table"
      via: "supabase.from('avis').insert() with unique constraint check"
---

<objective>
Ce plan crée le système d'avis bidirectionnel post-séjour. Après qu'une réservation passe au statut 'terminee', locataire ET propriétaire peuvent se noter mutuellement (1-5 étoiles + commentaire). Le propriétaire peut répondre aux avis reçus. Les avis sont publics et la note moyenne est calculée côté serveur. Ce système construit la confiance sur la plateforme Immo CI — un propriétaire avec 4.8/5 attire plus de locataires.

Purpose: Générer la confiance et la réputation sur la plateforme — pilier essentiel d'une marketplace immobilière.
Output: 3 composants avis, 2 routes API, 2 pages (client + pro).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Schema avis (migration 007) — champs EXACTS -->
<!--
  id uuid PK default gen_random_uuid()
  auteur_id uuid NOT NULL FK profiles
  cible_id uuid NOT NULL FK profiles
  reservation_id uuid FK reservations ON DELETE SET NULL (nullable!)
  note integer NOT NULL CHECK (note BETWEEN 1 AND 5)
  commentaire text nullable
  reponse_cible text nullable  -- réponse du propriétaire
  created_at timestamptz
  updated_at timestamptz
  UNIQUE(auteur_id, cible_id, reservation_id)  -- empêche les doublons
-->

<!-- Schema reservations — statut pour trigger avis -->
<!--
  id uuid PK
  locataire_id uuid
  proprietaire_id uuid
  bien_id uuid
  statut text  -- CHECK: en_attente|confirmee|annulee|terminee
  -- NOTE: 'terminee' est le bon statut (pas 'expiree') — cf STATE.md
-->

<!-- RLS avis — important pour les routes API -->
<!--
  "Avis visibles par tous" → FOR SELECT USING (true) — pas besoin de service role pour lire
  "Auteur crée un avis"    → FOR INSERT WITH CHECK (auteur_id = auth.uid())
  "Cible répond à un avis" → FOR UPDATE USING (cible_id = auth.uid() OR auteur_id = auth.uid())
  
  IMPLICATION: POST /api/avis peut utiliser la clé ANON avec cookies d'auth
  IMPLICATION: PATCH /api/avis/[id]/reponse peut utiliser la clé ANON avec cookies d'auth
  → Utiliser createClient() (anon key) et vérifier user.id === cible_id
-->

<!-- Pattern createClient avec cookies (routes authentifiées) -->
<!--
  import { createClient } from '@/lib/supabase/server'
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })
-->

<!-- Pattern pour noter une réservation — vérifier que statut='terminee' avant insert -->
<!--
  const { data: resa } = await supabase.from('reservations')
    .select('id, locataire_id, proprietaire_id, statut')
    .eq('id', reservationId)
    .single()
  if (resa.statut !== 'terminee') return 403
  // Vérifier que l'auteur est l'une des deux parties
  if (resa.locataire_id !== user.id && resa.proprietaire_id !== user.id) return 403
-->

<!-- Note moyenne — calcul côté serveur -->
<!--
  const { data } = await supabase.from('avis')
    .select('note')
    .eq('cible_id', profileId)
  const noteMoyenne = data?.length
    ? (data.reduce((s, a) => s + a.note, 0) / data.length).toFixed(1)
    : null
-->

<!-- StarRating: utiliser des SVG étoiles simples, pas de lib externe -->
<!-- AvisCard: réutiliser Card + Badge de @/components/ui/ -->
<!-- AvisForm: utiliser 'use client', useState pour note + commentaire -->
<!-- cn() from @/lib/utils pour les classNames -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composants avis + routes API</name>
  <files>
    apps/web/components/avis/StarRating.tsx,
    apps/web/components/avis/AvisCard.tsx,
    apps/web/components/avis/AvisForm.tsx,
    apps/web/app/api/avis/route.ts,
    apps/web/app/api/avis/[id]/reponse/route.ts
  </files>
  <read_first>
    apps/web/components/ui/Card.tsx,
    apps/web/components/ui/Badge.tsx,
    apps/web/components/ui/Button.tsx,
    apps/web/lib/supabase/server.ts
  </read_first>
  <action>
**1. Créer `apps/web/components/avis/StarRating.tsx`**

Composant étoiles: mode interactif (sélection note) et mode readonly (affichage). Pas de lib externe — SVG pur. Utilise 'use client' car interactive.

```typescript
// apps/web/components/avis/StarRating.tsx
'use client'
import { useState } from 'react'
import { cn } from '@/lib/utils'

interface StarRatingProps {
  value:     number        // 0-5 (0 = non noté)
  onChange?: (note: number) => void  // défini = mode interactif
  size?:     'sm' | 'md' | 'lg'
  className?: string
}

const SIZE_MAP = { sm: 16, md: 20, lg: 28 }

export function StarRating({ value, onChange, size = 'md', className }: StarRatingProps) {
  const [hovered, setHovered] = useState<number>(0)
  const readonly = !onChange
  const starSize = SIZE_MAP[size]
  const display  = hovered > 0 ? hovered : value

  return (
    <div
      className={cn('flex items-center gap-0.5', className)}
      role={readonly ? 'img' : 'radiogroup'}
      aria-label={`Note: ${value} sur 5`}
    >
      {[1, 2, 3, 4, 5].map(star => (
        <button
          key={star}
          type="button"
          disabled={readonly}
          onClick={() => onChange?.(star)}
          onMouseEnter={() => !readonly && setHovered(star)}
          onMouseLeave={() => !readonly && setHovered(0)}
          className={cn(
            'transition-transform',
            !readonly && 'hover:scale-110 cursor-pointer',
            readonly && 'cursor-default'
          )}
          aria-label={`${star} étoile${star > 1 ? 's' : ''}`}
        >
          <svg
            width={starSize}
            height={starSize}
            viewBox="0 0 24 24"
            fill={star <= display ? '#E67E22' : 'none'}
            stroke={star <= display ? '#E67E22' : '#D1D5DB'}
            strokeWidth={1.5}
          >
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
          </svg>
        </button>
      ))}
      {!readonly && value > 0 && (
        <span className="ml-2 text-sm font-medium text-gray-700">{value}/5</span>
      )}
    </div>
  )
}
```

**2. Créer `apps/web/components/avis/AvisCard.tsx`**

Affichage d'un avis avec auteur, note, commentaire, et réponse propriétaire si elle existe. Mode readonly uniquement — la réponse est gérée dans les pages.

```typescript
// apps/web/components/avis/AvisCard.tsx
import { StarRating } from './StarRating'
import { Card }       from '@/components/ui/Card'

interface AvisCardProps {
  auteurNom:    string
  note:         number
  commentaire?: string
  reponseCible?: string
  dateCreation: string
  showReponse?: boolean  // afficher la section réponse
}

export function AvisCard({
  auteurNom, note, commentaire, reponseCible, dateCreation, showReponse = true,
}: AvisCardProps) {
  const dateLabel = new Date(dateCreation).toLocaleDateString('fr-FR', {
    day: 'numeric', month: 'long', year: 'numeric',
  })

  return (
    <Card className="p-4">
      {/* En-tête */}
      <div className="flex items-start justify-between mb-3">
        <div>
          <p className="font-semibold text-gray-900">{auteurNom}</p>
          <p className="text-xs text-gray-400">{dateLabel}</p>
        </div>
        <StarRating value={note} size="sm" />
      </div>

      {/* Commentaire */}
      {commentaire && (
        <p className="text-sm text-gray-700 mb-3 leading-relaxed">{commentaire}</p>
      )}

      {/* Réponse propriétaire */}
      {showReponse && reponseCible && (
        <div className="mt-3 pl-4 border-l-2 border-primary/30 bg-blue-50 rounded-r-md p-3">
          <p className="text-xs font-semibold text-primary mb-1">Réponse du propriétaire :</p>
          <p className="text-sm text-gray-700">{reponseCible}</p>
        </div>
      )}
    </Card>
  )
}
```

**3. Créer `apps/web/components/avis/AvisForm.tsx`**

Formulaire de soumission d'avis. Client Component avec state local. Appelle `POST /api/avis` et refresh la page après succès.

```typescript
// apps/web/components/avis/AvisForm.tsx
'use client'
import { useState }   from 'react'
import { useRouter }  from 'next/navigation'
import { StarRating } from './StarRating'
import { Button }     from '@/components/ui/Button'

interface AvisFormProps {
  reservationId: string
  cibleId:       string
  cibleNom:      string
  onSuccess?:    () => void
}

export function AvisForm({ reservationId, cibleId, cibleNom, onSuccess }: AvisFormProps) {
  const router = useRouter()
  const [note,        setNote]        = useState(0)
  const [commentaire, setCommentaire] = useState('')
  const [loading,     setLoading]     = useState(false)
  const [error,       setError]       = useState<string | null>(null)
  const [submitted,   setSubmitted]   = useState(false)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (note === 0) { setError('Veuillez sélectionner une note'); return }
    setLoading(true)
    setError(null)

    const res = await fetch('/api/avis', {
      method:  'POST',
      headers: { 'Content-Type': 'application/json' },
      body:    JSON.stringify({ reservationId, cibleId, note, commentaire }),
    })

    const data = await res.json()
    setLoading(false)

    if (!res.ok) {
      setError(data.error ?? 'Erreur lors de la soumission')
      return
    }

    setSubmitted(true)
    onSuccess?.()
    router.refresh()
  }

  if (submitted) {
    return (
      <div className="p-4 bg-green-50 rounded-lg text-green-700 text-sm font-medium">
        Votre avis a été envoyé. Merci !
      </div>
    )
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4 p-4 bg-gray-50 rounded-lg">
      <h3 className="font-semibold text-gray-900">Évaluer {cibleNom}</h3>

      <div>
        <label className="block text-sm font-medium text-gray-700 mb-2">Note *</label>
        <StarRating value={note} onChange={setNote} size="lg" />
      </div>

      <div>
        <label htmlFor="commentaire" className="block text-sm font-medium text-gray-700 mb-1">
          Commentaire (optionnel)
        </label>
        <textarea
          id="commentaire"
          value={commentaire}
          onChange={e => setCommentaire(e.target.value)}
          rows={3}
          maxLength={500}
          placeholder="Décrivez votre expérience..."
          className="w-full border border-gray-200 rounded-md px-3 py-2 text-sm resize-none focus:outline-none focus:ring-2 focus:ring-primary/30"
        />
        <p className="text-xs text-gray-400 text-right">{commentaire.length}/500</p>
      </div>

      {error && <p className="text-sm text-red-600">{error}</p>}

      <Button type="submit" disabled={loading || note === 0} className="w-full">
        {loading ? 'Envoi...' : 'Publier mon avis'}
      </Button>
    </form>
  )
}
```

**4. Créer `apps/web/app/api/avis/route.ts`**

```typescript
// apps/web/app/api/avis/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function POST(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })

  const body = await req.json() as {
    reservationId: string
    cibleId:       string
    note:          number
    commentaire?:  string
  }

  const { reservationId, cibleId, note, commentaire } = body

  if (!reservationId || !cibleId || !note) {
    return NextResponse.json({ error: 'reservationId, cibleId et note requis' }, { status: 400 })
  }
  if (note < 1 || note > 5) {
    return NextResponse.json({ error: 'note doit être entre 1 et 5' }, { status: 400 })
  }

  // Vérifier que la réservation est terminée et que l'auteur est une des parties
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: resa } = await (supabase.from('reservations') as any)
    .select('id, locataire_id, proprietaire_id, statut')
    .eq('id', reservationId)
    .single()

  if (!resa) {
    return NextResponse.json({ error: 'Réservation introuvable' }, { status: 404 })
  }
  // NOTE: statut 'terminee' (pas 'expiree') — cf STATE.md
  if (resa.statut !== 'terminee') {
    return NextResponse.json({ error: 'La réservation doit être terminée pour laisser un avis' }, { status: 403 })
  }
  if (resa.locataire_id !== user.id && resa.proprietaire_id !== user.id) {
    return NextResponse.json({ error: 'Vous n\'êtes pas partie de cette réservation' }, { status: 403 })
  }
  // L'auteur doit noter la partie adverse (pas soi-même)
  if (cibleId === user.id) {
    return NextResponse.json({ error: 'Vous ne pouvez pas vous noter vous-même' }, { status: 400 })
  }

  // Insérer l'avis (RLS: auteur_id = auth.uid() vérifié par Supabase)
  // UNIQUE(auteur_id, cible_id, reservation_id) — retourne erreur si doublon
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: avis, error: insertError } = await (supabase.from('avis') as any)
    .insert({
      auteur_id:     user.id,
      cible_id:      cibleId,
      reservation_id: reservationId,
      note:          note,
      commentaire:   commentaire ?? null,
    })
    .select('id')
    .single()

  if (insertError) {
    // Code 23505 = violation unique constraint
    if (insertError.code === '23505') {
      return NextResponse.json({ error: 'Vous avez déjà évalué cette personne pour cette réservation' }, { status: 409 })
    }
    return NextResponse.json({ error: insertError.message }, { status: 500 })
  }

  // Notification pour la cible: avis_recu
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  await (supabase.from('notifications') as any).insert({
    user_id:   cibleId,
    type:      'avis_recu',
    titre:     'Vous avez reçu un nouvel avis',
    contenu:   `Un utilisateur vous a attribué ${note}/5 étoiles.${commentaire ? ` "${commentaire.slice(0, 80)}..."` : ''}`,
    lien_type: 'reservation',
    lien_id:   reservationId,
  })

  return NextResponse.json({ avisId: (avis as { id: string }).id }, { status: 201 })
}
```

**5. Créer `apps/web/app/api/avis/[id]/reponse/route.ts`**

```typescript
// apps/web/app/api/avis/[id]/reponse/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function PATCH(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })

  const { id: avisId } = await params
  const { reponse } = await req.json() as { reponse: string }

  if (!reponse?.trim()) {
    return NextResponse.json({ error: 'La réponse ne peut pas être vide' }, { status: 400 })
  }

  // Vérifier que l'utilisateur est bien la cible de cet avis
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: avis } = await (supabase.from('avis') as any)
    .select('id, cible_id, reponse_cible')
    .eq('id', avisId)
    .single()

  if (!avis) return NextResponse.json({ error: 'Avis introuvable' }, { status: 404 })
  if (avis.cible_id !== user.id) {
    return NextResponse.json({ error: 'Seule la cible peut répondre à un avis' }, { status: 403 })
  }
  if (avis.reponse_cible) {
    return NextResponse.json({ error: 'Une réponse a déjà été publiée pour cet avis' }, { status: 409 })
  }

  // Mettre à jour reponse_cible (RLS: cible_id = auth.uid())
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error: updateError } = await (supabase.from('avis') as any)
    .update({ reponse_cible: reponse.trim() })
    .eq('id', avisId)

  if (updateError) {
    return NextResponse.json({ error: updateError.message }, { status: 500 })
  }

  return NextResponse.json({ success: true })
}
```
  </action>
  <verify>
    <automated>test -f "apps/web/components/avis/StarRating.tsx" && test -f "apps/web/components/avis/AvisCard.tsx" && test -f "apps/web/components/avis/AvisForm.tsx" && test -f "apps/web/app/api/avis/route.ts" && test -f "apps/web/app/api/avis/[id]/reponse/route.ts" && grep -q "terminee" "apps/web/app/api/avis/route.ts" && grep -q "23505" "apps/web/app/api/avis/route.ts" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- StarRating: mode interactif si `onChange` défini, mode readonly sinon
- StarRating: étoile orange (#E67E22) si sélectionnée, grise sinon
- AvisCard: affiche auteurNom, StarRating readonly, commentaire, et réponse si définie
- AvisForm: bouton "Publier" disabled si note=0
- `POST /api/avis` retourne 403 si réservation.statut !== 'terminee'
- `POST /api/avis` retourne 409 si avis déjà existant (code Postgres 23505)
- `POST /api/avis` insère une notification `avis_recu` pour la cible
- `PATCH /api/avis/[id]/reponse` retourne 403 si user !== cible_id
- `PATCH /api/avis/[id]/reponse` retourne 409 si reponse_cible existe déjà
  </acceptance_criteria>
  <done>Composants avis et routes API créés — soumission et réponse aux avis fonctionnelles.</done>
</task>

<task type="auto">
  <name>Task 2: Pages avis client + pro</name>
  <files>
    apps/web/app/(client)/avis/page.tsx,
    apps/web/app/(pro)/avis/page.tsx
  </files>
  <read_first>
    apps/web/app/(pro)/dashboard/page.tsx,
    apps/web/components/avis/AvisCard.tsx,
    apps/web/components/avis/AvisForm.tsx,
    apps/web/app/(pro)/layout.tsx
  </read_first>
  <action>
**1. Créer `apps/web/app/(client)/avis/page.tsx`**

Page locataire — deux sections: (1) réservations terminées sans avis locataire → affiche AvisForm, (2) avis reçus sur le profil du locataire. Server Component avec fetch parallèle.

```typescript
// apps/web/app/(client)/avis/page.tsx
import { createClient }  from '@/lib/supabase/server'
import { redirect }      from 'next/navigation'
import { AvisCard }      from '@/components/avis/AvisCard'
import { AvisForm }      from '@/components/avis/AvisForm'
import { Card }          from '@/components/ui/Card'

export const metadata = { title: 'Mes Avis — Immo CI' }

export default async function ClientAvisPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  // Fetch parallèle: réservations terminées + avis reçus
  const [reservationsResult, avisRecusResult] = await Promise.all([
    // Réservations terminées où le locataire n'a pas encore noté le propriétaire
    (supabase.from('reservations') as any)
      .select(`
        id, date_fin, statut,
        biens ( titre, commune ),
        profiles!proprietaire_id ( id, nom_complet )
      `)
      .eq('locataire_id', user.id)
      .eq('statut', 'terminee')
      .not('id', 'in', `(
        SELECT reservation_id FROM avis 
        WHERE auteur_id = '${user.id}' 
        AND reservation_id IS NOT NULL
      )`)
      .order('date_fin', { ascending: false })
      .limit(10),

    // Avis reçus par le locataire
    (supabase.from('avis') as any)
      .select(`
        id, note, commentaire, reponse_cible, created_at,
        profiles!auteur_id ( nom_complet )
      `)
      .eq('cible_id', user.id)
      .order('created_at', { ascending: false })
      .limit(20),
  ])

  const reservationsANoter = reservationsResult.data ?? []
  const avisRecus           = avisRecusResult.data ?? []

  // Calculer note moyenne
  const noteMoyenne = avisRecus.length
    ? (avisRecus.reduce((s: number, a: any) => s + a.note, 0) / avisRecus.length).toFixed(1)
    : null

  return (
    <div className="max-w-3xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-gray-900 mb-6">Mes Avis</h1>

      {/* Réservations à noter */}
      {reservationsANoter.length > 0 && (
        <section className="mb-8">
          <h2 className="text-lg font-semibold text-gray-800 mb-3">
            À évaluer ({reservationsANoter.length})
          </h2>
          <div className="space-y-4">
            {reservationsANoter.map((resa: any) => {
              const bien = resa.biens as { titre: string; commune: string } | null
              const proprio = resa['profiles!proprietaire_id'] as { id: string; nom_complet: string } | null
              return (
                <Card key={resa.id} className="p-4">
                  <p className="font-medium text-gray-900 mb-1">
                    {bien?.titre ?? 'Bien'} — {bien?.commune}
                  </p>
                  <p className="text-sm text-gray-500 mb-3">
                    Séjour terminé le {new Date(resa.date_fin).toLocaleDateString('fr-FR')}
                  </p>
                  <AvisForm
                    reservationId={resa.id}
                    cibleId={proprio?.id ?? ''}
                    cibleNom={proprio?.nom_complet ?? 'le propriétaire'}
                  />
                </Card>
              )
            })}
          </div>
        </section>
      )}

      {/* Avis reçus */}
      <section>
        <div className="flex items-center justify-between mb-3">
          <h2 className="text-lg font-semibold text-gray-800">
            Avis reçus ({avisRecus.length})
          </h2>
          {noteMoyenne && (
            <span className="text-sm font-medium text-orange-600 bg-orange-50 px-3 py-1 rounded-full">
              Note moyenne: {noteMoyenne}/5
            </span>
          )}
        </div>

        {avisRecus.length === 0 ? (
          <Card className="p-8 text-center text-gray-500">
            <p>Aucun avis reçu pour le moment.</p>
          </Card>
        ) : (
          <div className="space-y-3">
            {avisRecus.map((avis: any) => {
              const auteur = avis['profiles!auteur_id'] as { nom_complet: string } | null
              return (
                <AvisCard
                  key={avis.id}
                  auteurNom={auteur?.nom_complet ?? 'Anonyme'}
                  note={avis.note}
                  commentaire={avis.commentaire}
                  reponseCible={avis.reponse_cible}
                  dateCreation={avis.created_at}
                />
              )
            })}
          </div>
        )}
      </section>
    </div>
  )
}
```

**IMPORTANT pour la query NOT IN**: La sous-requête SQL inline dans `.not('id', 'in', ...)` peut ne pas fonctionner via le client Supabase JS (il ne supporte pas les sous-requêtes SQL inline dans les filtres). Utiliser l'approche en deux temps à la place:

```typescript
// Approche robuste pour filtrer les réservations déjà notées
// Étape 1: récupérer les IDs de réservations déjà notées par cet utilisateur
const { data: dejaNotes } = await (supabase.from('avis') as any)
  .select('reservation_id')
  .eq('auteur_id', user.id)
  .not('reservation_id', 'is', null)

const dejaNoteIds = (dejaNotes ?? []).map((a: any) => a.reservation_id as string)

// Étape 2: query des réservations terminées sans avis
let resaQuery = (supabase.from('reservations') as any)
  .select(`
    id, date_fin, statut,
    biens ( titre, commune ),
    profiles!proprietaire_id ( id, nom_complet )
  `)
  .eq('locataire_id', user.id)
  .eq('statut', 'terminee')
  .order('date_fin', { ascending: false })
  .limit(10)

if (dejaNoteIds.length > 0) {
  resaQuery = resaQuery.not('id', 'in', `(${dejaNoteIds.map((id: string) => `'${id}'`).join(',')})`)
}
// bienIds.length guard pattern (cf STATE.md) — .in() sur tableau vide retourne erreur
```

Remplacer la version avec sous-requête inline par cette approche deux étapes dans la page.

**2. Créer `apps/web/app/(pro)/avis/page.tsx`**

Page propriétaire — avis reçus avec réponse inline + réservations terminées sans note du propriétaire. Pattern Server Component + Client composant pour le formulaire de réponse.

```typescript
// apps/web/app/(pro)/avis/page.tsx
import { createClient }  from '@/lib/supabase/server'
import { redirect }      from 'next/navigation'
import { AvisCard }      from '@/components/avis/AvisCard'
import { AvisForm }      from '@/components/avis/AvisForm'
import { Card }          from '@/components/ui/Card'
import { ReponseForm }   from '@/components/avis/ReponseForm'

export const metadata = { title: 'Avis Reçus — Immo CI Pro' }

export default async function ProAvisPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  // Étape 1: réservations déjà notées par le propriétaire
  const { data: dejaNotesPro } = await (supabase.from('avis') as any)
    .select('reservation_id')
    .eq('auteur_id', user.id)
    .not('reservation_id', 'is', null)

  const dejaNoteProIds = (dejaNotesPro ?? []).map((a: any) => a.reservation_id as string)

  // Fetch parallèle: avis reçus + réservations terminées à noter
  const [avisRecusResult, reservationsResult] = await Promise.all([
    // Avis reçus par le propriétaire (avec info auteur)
    (supabase.from('avis') as any)
      .select(`
        id, note, commentaire, reponse_cible, created_at,
        reservation_id,
        profiles!auteur_id ( nom_complet )
      `)
      .eq('cible_id', user.id)
      .order('created_at', { ascending: false })
      .limit(20),

    // Réservations terminées où le proprio n'a pas encore noté le locataire
    (async () => {
      let q = (supabase.from('reservations') as any)
        .select(`
          id, date_fin,
          biens ( titre, commune ),
          profiles!locataire_id ( id, nom_complet )
        `)
        .eq('proprietaire_id', user.id)
        .eq('statut', 'terminee')
        .order('date_fin', { ascending: false })
        .limit(10)

      if (dejaNoteProIds.length > 0) {
        q = q.not('id', 'in', `(${dejaNoteProIds.map((id: string) => `'${id}'`).join(',')})`)
      }
      return q
    })(),
  ])

  const avisRecus        = avisRecusResult.data ?? []
  const reservationsANoter = reservationsResult.data ?? []

  const noteMoyenne = avisRecus.length
    ? (avisRecus.reduce((s: number, a: any) => s + a.note, 0) / avisRecus.length).toFixed(1)
    : null

  return (
    <div className="max-w-3xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-gray-900 mb-6">Avis Reçus</h1>

      {/* À évaluer (propriétaire note ses locataires) */}
      {reservationsANoter.length > 0 && (
        <section className="mb-8">
          <h2 className="text-lg font-semibold text-gray-800 mb-3">
            Locataires à évaluer ({reservationsANoter.length})
          </h2>
          <div className="space-y-4">
            {reservationsANoter.map((resa: any) => {
              const bien     = resa.biens as { titre: string; commune: string } | null
              const locataire = resa['profiles!locataire_id'] as { id: string; nom_complet: string } | null
              return (
                <Card key={resa.id} className="p-4">
                  <p className="font-medium text-gray-900 mb-1">
                    {locataire?.nom_complet ?? 'Locataire'} · {bien?.titre}, {bien?.commune}
                  </p>
                  <p className="text-sm text-gray-500 mb-3">
                    Séjour terminé le {new Date(resa.date_fin).toLocaleDateString('fr-FR')}
                  </p>
                  <AvisForm
                    reservationId={resa.id}
                    cibleId={locataire?.id ?? ''}
                    cibleNom={locataire?.nom_complet ?? 'le locataire'}
                  />
                </Card>
              )
            })}
          </div>
        </section>
      )}

      {/* Avis reçus */}
      <section>
        <div className="flex items-center justify-between mb-3">
          <h2 className="text-lg font-semibold text-gray-800">
            Avis de mes locataires ({avisRecus.length})
          </h2>
          {noteMoyenne && (
            <span className="text-sm font-medium text-orange-600 bg-orange-50 px-3 py-1 rounded-full">
              Note moyenne: {noteMoyenne}/5
            </span>
          )}
        </div>

        {avisRecus.length === 0 ? (
          <Card className="p-8 text-center text-gray-500">
            <p>Aucun avis reçu pour le moment.</p>
            <p className="text-sm mt-2">Les avis apparaissent après la fin d&apos;une réservation.</p>
          </Card>
        ) : (
          <div className="space-y-4">
            {avisRecus.map((avis: any) => {
              const auteur = avis['profiles!auteur_id'] as { nom_complet: string } | null
              return (
                <div key={avis.id} className="space-y-2">
                  <AvisCard
                    auteurNom={auteur?.nom_complet ?? 'Anonyme'}
                    note={avis.note}
                    commentaire={avis.commentaire}
                    reponseCible={avis.reponse_cible}
                    dateCreation={avis.created_at}
                    showReponse={true}
                  />
                  {/* Formulaire de réponse si pas encore répondu */}
                  {!avis.reponse_cible && (
                    <ReponseForm avisId={avis.id} />
                  )}
                </div>
              )
            })}
          </div>
        )}
      </section>
    </div>
  )
}
```

**3. Créer `apps/web/components/avis/ReponseForm.tsx`** (Client Component nécessaire pour la page pro)

```typescript
// apps/web/components/avis/ReponseForm.tsx
'use client'
import { useState }  from 'react'
import { useRouter } from 'next/navigation'
import { Button }    from '@/components/ui/Button'

interface ReponseFormProps {
  avisId: string
}

export function ReponseForm({ avisId }: ReponseFormProps) {
  const router  = useRouter()
  const [open,    setOpen]    = useState(false)
  const [reponse, setReponse] = useState('')
  const [loading, setLoading] = useState(false)
  const [error,   setError]   = useState<string | null>(null)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!reponse.trim()) return
    setLoading(true)
    setError(null)

    const res = await fetch(`/api/avis/${avisId}/reponse`, {
      method:  'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body:    JSON.stringify({ reponse }),
    })

    setLoading(false)
    if (!res.ok) {
      const data = await res.json()
      setError(data.error ?? 'Erreur')
      return
    }
    setOpen(false)
    router.refresh()
  }

  if (!open) {
    return (
      <button
        onClick={() => setOpen(true)}
        className="text-sm text-primary hover:underline pl-2"
      >
        + Répondre à cet avis
      </button>
    )
  }

  return (
    <form onSubmit={handleSubmit} className="pl-4 border-l-2 border-primary/30 space-y-2">
      <textarea
        value={reponse}
        onChange={e => setReponse(e.target.value)}
        rows={2}
        maxLength={500}
        placeholder="Votre réponse..."
        className="w-full border border-gray-200 rounded-md px-3 py-2 text-sm resize-none focus:outline-none focus:ring-2 focus:ring-primary/30"
        autoFocus
      />
      {error && <p className="text-xs text-red-600">{error}</p>}
      <div className="flex gap-2">
        <Button type="submit" disabled={loading || !reponse.trim()} className="text-sm py-1 px-3">
          {loading ? 'Envoi...' : 'Publier'}
        </Button>
        <button
          type="button"
          onClick={() => setOpen(false)}
          className="text-sm text-gray-500 hover:text-gray-700"
        >
          Annuler
        </button>
      </div>
    </form>
  )
}
```

Ajouter `ReponseForm` à la liste des files_modified (c'est un 8ème fichier créé dans ce plan).
  </action>
  <verify>
    <automated>test -f "apps/web/app/(client)/avis/page.tsx" && test -f "apps/web/app/(pro)/avis/page.tsx" && grep -q "terminee" "apps/web/app/(client)/avis/page.tsx" && grep -q "noteMoyenne" "apps/web/app/(pro)/avis/page.tsx" && test -f "apps/web/components/avis/ReponseForm.tsx" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `apps/web/app/(client)/avis/page.tsx` est un Server Component (pas de 'use client')
- La page client affiche les réservations terminées sans avis avec AvisForm
- La page client affiche les avis reçus avec note moyenne
- L'approche deux-étapes est utilisée (pas de sous-requête SQL inline)
- `apps/web/app/(pro)/avis/page.tsx` est un Server Component (pas de 'use client')
- La page pro affiche les avis reçus avec ReponseForm si pas encore répondu
- La page pro affiche les locataires à noter (réservations terminées sans avis)
- ReponseForm est un Client Component avec formulaire inline
- router.refresh() est appelé après soumission réussie pour recharger les Server Components
  </acceptance_criteria>
  <done>Pages avis client et pro créées — système d'avis bidirectionnel complet avec réponse propriétaire.</done>
</task>

</tasks>

<verification>
```bash
# Tous les fichiers créés
for f in \
  "apps/web/components/avis/StarRating.tsx" \
  "apps/web/components/avis/AvisCard.tsx" \
  "apps/web/components/avis/AvisForm.tsx" \
  "apps/web/components/avis/ReponseForm.tsx" \
  "apps/web/app/api/avis/route.ts" \
  "apps/web/app/api/avis/[id]/reponse/route.ts" \
  "apps/web/app/(client)/avis/page.tsx" \
  "apps/web/app/(pro)/avis/page.tsx"; do
  test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
done

# Patterns critiques
grep -q "terminee" "apps/web/app/api/avis/route.ts" && echo "statut terminee OK"
grep -q "23505" "apps/web/app/api/avis/route.ts" && echo "unique constraint 23505 OK"
grep -q "avis_recu" "apps/web/app/api/avis/route.ts" && echo "notification avis_recu OK"
grep -q "cible_id !== user.id" "apps/web/app/api/avis/[id]/reponse/route.ts" && echo "auth check OK"

# TypeScript
cd apps/web && npx tsc --noEmit 2>&1 | grep -E "avis|error" | head -20
```
</verification>

<success_criteria>
1. StarRating en mode interactif: clic sur étoile 3 → value=3, étoiles 1-3 orange
2. StarRating en mode readonly: aucun clic possible, étoiles affichées selon value
3. `POST /api/avis` avec réservation non terminée retourne HTTP 403
4. `POST /api/avis` avec doublon retourne HTTP 409 (code 23505)
5. `POST /api/avis` insère une notification `avis_recu` pour la cible
6. `PATCH /api/avis/[id]/reponse` avec un user qui n'est pas la cible retourne HTTP 403
7. Page `/client/avis` affiche les réservations terminées sans avis en section "À évaluer"
8. Page `/pro/avis` affiche le bouton "+ Répondre" pour chaque avis sans réponse
9. La note moyenne est calculée et affichée sur les deux pages
10. router.refresh() recharge les données Server Component après soumission réussie
</success_criteria>

<output>
After completion, create `.planning/phases/04-gestion-locative-avis-kyc/04-03-SUMMARY.md`
</output>
