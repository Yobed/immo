---
phase: 04-gestion-locative-avis-kyc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/quittance-pdf.tsx
  - supabase/functions/generer-quittance/index.ts
  - apps/web/app/api/quittances/generer/route.ts
  - n8n/workflows/quittance-mensuelle.json
autonomous: true
requirements: [LOC-01, LOC-02]

must_haves:
  truths:
    - "Le 1er de chaque mois, une quittance est créée pour chaque contrat actif (statut='signe')"
    - "La quittance PDF contient le montant en lettres ET en chiffres en FCFA"
    - "Le PDF est stocké dans Supabase Storage bucket 'quittances'"
    - "La table quittances est mise à jour avec pdf_url après génération"
    - "Le locataire reçoit un message WhatsApp avec le lien de téléchargement"
    - "Une notification est insérée dans la table notifications (type='loyer_rappel')"
  artifacts:
    - path: "apps/web/lib/quittance-pdf.tsx"
      provides: "Composant React PDF pour quittance mensuelle"
      exports: [QuittanceDocument, QuittanceProps, montantEnLettres]
    - path: "supabase/functions/generer-quittance/index.ts"
      provides: "Edge Function Deno — génère PDF et upload Storage"
    - path: "apps/web/app/api/quittances/generer/route.ts"
      provides: "POST webhook — reçoit {contratId} de n8n, crée quittance + déclenche PDF"
    - path: "n8n/workflows/quittance-mensuelle.json"
      provides: "Workflow n8n — cron 1er du mois, query contrats, appel API, WhatsApp, notifications"
  key_links:
    - from: "n8n/workflows/quittance-mensuelle.json"
      to: "apps/web/app/api/quittances/generer/route.ts"
      via: "HTTP POST N8N_WEBHOOK_BASE_URL/quittances/generer"
    - from: "apps/web/app/api/quittances/generer/route.ts"
      to: "supabase/functions/generer-quittance/index.ts"
      via: "fetch Edge Function URL with contratId + quittanceId"
    - from: "supabase/functions/generer-quittance/index.ts"
      to: "supabase storage bucket quittances"
      via: "supabase.storage.from('quittances').upload()"
---

<objective>
Ce plan crée le pipeline complet de génération automatique des quittances mensuelles. Un workflow n8n déclenché le 1er de chaque mois à 07:00 (heure Abidjan) interroge tous les contrats actifs (statut='signe'), appelle un webhook Next.js qui crée l'enregistrement quittance dans Supabase, puis délègue la génération PDF à une Edge Function Deno. Le PDF est uploadé dans le Storage bucket 'quittances', la table est mise à jour avec pdf_url, et un message WhatsApp est envoyé au locataire via l'API WABA.

Purpose: Automatiser la gestion locative mensuelle — un propriétaire ivoirien reçoit ses loyers et ses locataires reçoivent leurs quittances sans intervention manuelle.
Output: QuittancePDF component, Edge Function Deno, API webhook, workflow n8n JSON exportable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CRITIQUE: Lire ces fichiers AVANT de coder -->
<!-- apps/web/lib/contrat-pdf.tsx — patron exact à reproduire pour QuittanceDocument -->
<!-- apps/web/app/api/contrats/generer/route.ts — patron service role + renderToBuffer + Storage -->

<!-- Schema quittances (migration 005) — champs EXACTS -->
<!--
  id uuid PK
  contrat_id uuid FK contrats
  locataire_id uuid FK profiles
  proprietaire_id uuid FK profiles
  mois date NOT NULL  -- 1er du mois: '2026-02-01'
  montant_loyer_fcfa integer NOT NULL
  montant_charges_fcfa integer NOT NULL default 0
  montant_total_fcfa integer NOT NULL
  date_echeance date NOT NULL
  date_paiement date nullable
  statut text default 'en_attente'  -- en_attente|payee|en_retard|annulee
  pdf_url text nullable
  created_at, updated_at timestamptz
  UNIQUE INDEX: (contrat_id, mois)
-->

<!-- Schema contrats (migration 005) — champs EXACTS -->
<!--
  id uuid PK
  reservation_id uuid FK
  locataire_id uuid FK profiles
  proprietaire_id uuid FK profiles
  bien_id uuid FK biens
  date_debut date
  date_fin date nullable
  duree_mois integer nullable
  loyer_mois_fcfa integer NOT NULL
  charges_mois_fcfa integer NOT NULL default 0
  depot_garantie_fcfa integer
  pdf_url text
  statut text  -- en_attente|signe|resilie|expire
-->

<!-- Pattern montantEnLettres — OBLIGATOIRE -->
<!--
  import { ToWords } from 'to-words'
  const toWords = new ToWords({ localeCode: 'fr-FR', converterOptions: { currency: false } })
  // currency:false OBLIGATOIRE — currency:true génère "euros"
  export function montantEnLettres(montant: number): string {
    return `${toWords.convert(Math.round(montant))} francs CFA`
  }
-->

<!-- Pattern renderToBuffer — OBLIGATOIRE -->
<!--
  import { renderToBuffer } from '@react-pdf/renderer'
  import { createElement } from 'react'
  const pdfBuffer = await renderToBuffer(createElement(QuittanceDocument, props))
  // NE PAS passer du JSX <QuittanceDocument {...props} /> — renderToBuffer attend un ReactElement
-->

<!-- Pattern service role (webhook non authentifié) -->
<!--
  const supabase = createServerClient<any>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  await (supabase.from('quittances') as any).insert({...})
-->

<!-- WhatsApp API — pattern WABA -->
<!--
  POST https://graph.facebook.com/v19.0/{WHATSAPP_PHONE_NUMBER_ID}/messages
  Authorization: Bearer {WHATSAPP_ACCESS_TOKEN}
  Content-Type: application/json
  Body: {
    messaging_product: "whatsapp",
    to: phoneNumber,  // format international: "+2250XXXXXXXXX"
    type: "text",
    text: { body: "Votre quittance de loyer..." }
  }
-->

<!-- Edge Function Deno — imports EXACTS -->
<!--
  import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
  import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
  // react-pdf NON disponible dans Deno — l'Edge Function reçoit le buffer depuis Next.js
  // OU l'Edge Function appelle l'API Next.js /api/quittances/generer pour le PDF
  // CHOIX ARCHITECTURAL: Edge Function = coordinator, Next.js API = PDF generator
-->

<!-- n8n: Africa/Abidjan est GMT+0 toute l'année — pas de décalage DST -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuittancePDF component + Edge Function Deno</name>
  <files>apps/web/lib/quittance-pdf.tsx, supabase/functions/generer-quittance/index.ts</files>
  <read_first>apps/web/lib/contrat-pdf.tsx, apps/web/app/api/contrats/generer/route.ts</read_first>
  <action>
**1. Créer `apps/web/lib/quittance-pdf.tsx`**

Copier la structure de `contrat-pdf.tsx` et l'adapter pour les quittances. Ce fichier est importé UNIQUEMENT depuis des routes serveur (serverExternalPackages: ['@react-pdf/renderer'] est déjà dans next.config.ts).

```typescript
// apps/web/lib/quittance-pdf.tsx
import { Document, Page, Text, View, StyleSheet } from '@react-pdf/renderer'
import { ToWords } from 'to-words'

// CRITIQUE: currency:false OBLIGATOIRE — currency:true génère "euros"
const toWords = new ToWords({
  localeCode: 'fr-FR',
  converterOptions: { currency: false },
})

export function montantEnLettres(montant: number): string {
  return `${toWords.convert(Math.round(montant))} francs CFA`
}

export interface QuittanceProps {
  quittanceId:        string
  contratId:          string
  mois:               string  // format: "2026-02-01" — afficher "Février 2026"
  // Bailleur
  bailleurNom:        string
  bailleurTel:        string
  // Preneur
  preneurNom:         string
  preneurTel:         string
  // Bien
  bienAdresse:        string
  bienCommune:        string
  // Financier
  loyerMoisFcfa:      number
  chargesMoisFcfa:    number
  totalFcfa:          number
  dateEcheance:       string  // format: "01/02/2026"
  datePaiement?:      string  // nullable — vide si non payé
  statut:             'en_attente' | 'payee' | 'en_retard' | 'annulee'
}

const S = StyleSheet.create({
  page:       { padding: 50, fontFamily: 'Helvetica', fontSize: 10, color: '#1C2833' },
  titre:      { fontSize: 16, fontWeight: 'bold', textAlign: 'center', marginBottom: 4 },
  sousTitre:  { fontSize: 10, textAlign: 'center', marginBottom: 20, color: '#7F8C8D' },
  ref:        { fontSize: 9, textAlign: 'right', marginBottom: 16, color: '#7F8C8D' },
  section:    { marginBottom: 14 },
  sectionH:   { fontSize: 11, fontWeight: 'bold', marginBottom: 6, borderBottomWidth: 1, borderBottomColor: '#E5E8EC', paddingBottom: 3 },
  row:        { flexDirection: 'row', marginBottom: 4 },
  label:      { width: '45%', color: '#7F8C8D' },
  value:      { width: '55%', fontFamily: 'Helvetica-Bold' },
  montant:    { fontSize: 13, fontFamily: 'Helvetica-Bold', color: '#1A5276', marginBottom: 4 },
  montantLet: { fontStyle: 'italic', color: '#1A5276', marginBottom: 12 },
  statutBox:  { padding: 8, backgroundColor: '#EBF5FB', marginBottom: 16, borderRadius: 4 },
  footer:     { position: 'absolute', bottom: 30, left: 50, right: 50, textAlign: 'center', fontSize: 8, color: '#7F8C8D' },
  signatures: { marginTop: 40, flexDirection: 'row', justifyContent: 'space-between' },
  sigBox:     { width: '45%', borderTopWidth: 1, borderTopColor: '#1C2833', paddingTop: 8 },
})

function formatMois(moisIso: string): string {
  const date = new Date(moisIso)
  return date.toLocaleDateString('fr-FR', { month: 'long', year: 'numeric' })
}

function statutLabel(statut: string): string {
  const map: Record<string, string> = {
    en_attente: 'En attente de paiement',
    payee:      'PAYÉE',
    en_retard:  'EN RETARD',
    annulee:    'Annulée',
  }
  return map[statut] ?? statut
}

export function QuittanceDocument(props: QuittanceProps) {
  const {
    quittanceId, contratId, mois,
    bailleurNom, bailleurTel,
    preneurNom, preneurTel,
    bienAdresse, bienCommune,
    loyerMoisFcfa, chargesMoisFcfa, totalFcfa,
    dateEcheance, datePaiement, statut,
  } = props

  return (
    <Document>
      <Page size="A4" style={S.page}>
        <Text style={S.titre}>QUITTANCE DE LOYER</Text>
        <Text style={S.sousTitre}>Période : {formatMois(mois)}</Text>
        <Text style={S.ref}>Réf: {quittanceId.slice(0, 8).toUpperCase()} — Contrat: {contratId.slice(0, 8).toUpperCase()}</Text>

        {/* Statut */}
        <View style={S.statutBox}>
          <Text>Statut : {statutLabel(statut)}</Text>
          {datePaiement && <Text>Payé le : {datePaiement}</Text>}
        </View>

        {/* Parties */}
        <View style={S.section}>
          <Text style={S.sectionH}>PARTIES</Text>
          <View style={S.row}><Text style={S.label}>Bailleur :</Text><Text style={S.value}>{bailleurNom}</Text></View>
          <View style={S.row}><Text style={S.label}>Tél bailleur :</Text><Text style={S.value}>{bailleurTel}</Text></View>
          <View style={S.row}><Text style={S.label}>Preneur :</Text><Text style={S.value}>{preneurNom}</Text></View>
          <View style={S.row}><Text style={S.label}>Tél preneur :</Text><Text style={S.value}>{preneurTel}</Text></View>
        </View>

        {/* Bien */}
        <View style={S.section}>
          <Text style={S.sectionH}>BIEN LOUÉ</Text>
          <View style={S.row}><Text style={S.label}>Adresse :</Text><Text style={S.value}>{bienAdresse}</Text></View>
          <View style={S.row}><Text style={S.label}>Commune :</Text><Text style={S.value}>{bienCommune}</Text></View>
        </View>

        {/* Montants */}
        <View style={S.section}>
          <Text style={S.sectionH}>DÉTAIL DES MONTANTS</Text>
          <View style={S.row}><Text style={S.label}>Loyer mensuel :</Text><Text style={S.value}>{loyerMoisFcfa.toLocaleString('fr-FR')} FCFA</Text></View>
          <View style={S.row}><Text style={S.label}>Charges :</Text><Text style={S.value}>{chargesMoisFcfa.toLocaleString('fr-FR')} FCFA</Text></View>
          <View style={S.row}><Text style={S.label}>Date d&apos;échéance :</Text><Text style={S.value}>{dateEcheance}</Text></View>
        </View>

        {/* Total */}
        <View style={S.section}>
          <Text style={S.sectionH}>TOTAL À PAYER</Text>
          <Text style={S.montant}>{totalFcfa.toLocaleString('fr-FR')} FCFA</Text>
          <Text style={S.montantLet}>Soit : {montantEnLettres(totalFcfa)}</Text>
        </View>

        {/* Signatures */}
        <View style={S.signatures}>
          <View style={S.sigBox}><Text>Signature du bailleur</Text><Text style={{ marginTop: 20 }}>{bailleurNom}</Text></View>
          <View style={S.sigBox}><Text>Cachet / Date</Text><Text style={{ marginTop: 20 }}>{new Date().toLocaleDateString('fr-FR')}</Text></View>
        </View>

        <Text style={S.footer}>
          Document généré automatiquement — Immo CI Platform — {new Date().toLocaleDateString('fr-FR')}
        </Text>
      </Page>
    </Document>
  )
}
```

**2. Créer `supabase/functions/generer-quittance/index.ts`**

NOTE ARCHITECTURALE: @react-pdf/renderer n'est pas disponible dans Deno. L'Edge Function joue le rôle de coordinateur — elle reçoit les paramètres de n8n, appelle l'API Next.js `/api/quittances/generer` pour créer l'enregistrement quittance et générer le PDF, puis retourne le résultat. C'est le pattern correct pour Deno + react-pdf.

```typescript
// supabase/functions/generer-quittance/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    const { contratId, mois } = await req.json() as { contratId: string; mois?: string }

    if (!contratId) {
      return new Response(
        JSON.stringify({ error: 'contratId requis' }),
        { status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
      )
    }

    // Appel à l'API Next.js qui gère react-pdf (non disponible en Deno)
    const nextApiUrl = Deno.env.get('NEXT_PUBLIC_APP_URL') ?? 'https://your-app.vercel.app'
    const serviceKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''

    const response = await fetch(`${nextApiUrl}/api/quittances/generer`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-service-key': serviceKey,  // shared secret pour authentifier l'Edge Function
      },
      body: JSON.stringify({ contratId, mois }),
    })

    const result = await response.json()

    if (!response.ok) {
      return new Response(
        JSON.stringify({ error: result.error ?? 'Erreur génération quittance' }),
        { status: response.status, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
      )
    }

    return new Response(
      JSON.stringify(result),
      { status: 200, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    )
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Erreur inconnue'
    return new Response(
      JSON.stringify({ error: message }),
      { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    )
  }
})
```

Créer également `supabase/functions/generer-quittance/deno.json` pour les imports:
```json
{
  "imports": {}
}
```
  </action>
  <verify>
    <automated>test -f "apps/web/lib/quittance-pdf.tsx" && grep -q "QuittanceDocument" "apps/web/lib/quittance-pdf.tsx" && grep -q "montantEnLettres" "apps/web/lib/quittance-pdf.tsx" && grep -q "currency: false" "apps/web/lib/quittance-pdf.tsx" && test -f "supabase/functions/generer-quittance/index.ts" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `apps/web/lib/quittance-pdf.tsx` exporte QuittanceDocument, QuittanceProps, montantEnLettres
- `montantEnLettres(150000)` retourne "cent cinquante mille francs CFA" (currency:false)
- `QuittanceDocument` affiche loyer + charges + total en chiffres ET en lettres
- `formatMois('2026-02-01')` affiche "février 2026"
- Edge Function `supabase/functions/generer-quittance/index.ts` délègue à l'API Next.js
- Aucun import de @react-pdf/renderer dans l'Edge Function (incompatible Deno)
  </acceptance_criteria>
  <done>QuittancePDF component et Edge Function Deno créés avec délégation correcte vers l'API Next.js.</done>
</task>

<task type="auto">
  <name>Task 2: API webhook Next.js + workflow n8n JSON</name>
  <files>apps/web/app/api/quittances/generer/route.ts, n8n/workflows/quittance-mensuelle.json</files>
  <read_first>apps/web/app/api/contrats/generer/route.ts, apps/web/app/api/paiements/webhook/route.ts, apps/web/lib/quittance-pdf.tsx</read_first>
  <action>
**1. Créer `apps/web/app/api/quittances/generer/route.ts`**

Ce handler reçoit `{contratId, mois?}` depuis l'Edge Function (ou directement depuis n8n), crée l'enregistrement quittance, génère le PDF avec renderToBuffer, l'uploade dans Storage, et met à jour pdf_url. Utilise service role key (appelable depuis webhook non authentifié).

```typescript
// apps/web/app/api/quittances/generer/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createServerClient } from '@supabase/ssr'
import { renderToBuffer } from '@react-pdf/renderer'
import { createElement } from 'react'
import { QuittanceDocument } from '@/lib/quittance-pdf'
import type { QuittanceProps } from '@/lib/quittance-pdf'

export async function POST(req: NextRequest) {
  // Auth: soit x-service-key (Edge Function), soit appel direct n8n avec secret
  // Pour Phase 4, on accepte tout appel sans auth côté route (service role suffit pour DB)
  // En production: valider un x-service-key partagé

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = createServerClient<any>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )

  const body = await req.json() as { contratId: string; mois?: string }
  if (!body.contratId) {
    return NextResponse.json({ error: 'contratId requis' }, { status: 400 })
  }

  // Calculer le mois: 1er du mois courant si non fourni
  const moisDate = body.mois
    ? new Date(body.mois)
    : new Date(new Date().getFullYear(), new Date().getMonth(), 1)
  const moisIso = moisDate.toISOString().split('T')[0]  // '2026-02-01'

  // Date d'échéance: 5 du mois courant
  const echeance = new Date(moisDate)
  echeance.setDate(5)
  const dateEcheanceIso = echeance.toISOString().split('T')[0]

  // Récupérer le contrat + bien + profiles
  const { data: contrat, error: contratError } = await supabase
    .from('contrats')
    .select(`
      id, locataire_id, proprietaire_id, bien_id,
      loyer_mois_fcfa, charges_mois_fcfa,
      biens ( id, adresse_complete, commune ),
      profiles!locataire_id ( nom_complet, telephone ),
      profiles!proprietaire_id ( nom_complet, telephone )
    `)
    .eq('id', body.contratId)
    .eq('statut', 'signe')
    .single()

  if (contratError || !contrat) {
    return NextResponse.json({ error: 'Contrat introuvable ou non signé' }, { status: 404 })
  }

  const bien = contrat.biens as { id: string; adresse_complete: string; commune: string } | null
  const locataire = contrat['profiles!locataire_id'] as { nom_complet: string; telephone: string } | null
  const proprietaire = contrat['profiles!proprietaire_id'] as { nom_complet: string; telephone: string } | null

  const loyerMoisFcfa = Number(contrat.loyer_mois_fcfa)
  const chargesMoisFcfa = Number(contrat.charges_mois_fcfa ?? 0)
  const totalFcfa = loyerMoisFcfa + chargesMoisFcfa

  // Vérifier si une quittance existe déjà pour ce mois (index unique contrat_id+mois)
  const { data: existing } = await supabase
    .from('quittances')
    .select('id, pdf_url')
    .eq('contrat_id', body.contratId)
    .eq('mois', moisIso)
    .single()

  let quittanceId: string

  if (existing) {
    quittanceId = existing.id
    // Quittance déjà créée — on régénère le PDF si pdf_url manquant
    if (existing.pdf_url) {
      return NextResponse.json({ quittanceId, pdfUrl: existing.pdf_url, skipped: true })
    }
  } else {
    // Insérer la quittance
    const { data: inserted, error: insertError } = await supabase
      .from('quittances')
      .insert({
        contrat_id:           body.contratId,
        locataire_id:         contrat.locataire_id,
        proprietaire_id:      contrat.proprietaire_id,
        mois:                 moisIso,
        montant_loyer_fcfa:   loyerMoisFcfa,
        montant_charges_fcfa: chargesMoisFcfa,
        montant_total_fcfa:   totalFcfa,
        date_echeance:        dateEcheanceIso,
        statut:               'en_attente',
      })
      .select('id')
      .single()

    if (insertError || !inserted) {
      return NextResponse.json({ error: insertError?.message ?? 'Erreur insertion' }, { status: 500 })
    }
    quittanceId = inserted.id
  }

  // Générer le PDF
  const quittanceProps: QuittanceProps = {
    quittanceId,
    contratId:       body.contratId,
    mois:            moisIso,
    bailleurNom:     proprietaire?.nom_complet ?? 'Non renseigné',
    bailleurTel:     proprietaire?.telephone   ?? '—',
    preneurNom:      locataire?.nom_complet    ?? 'Non renseigné',
    preneurTel:      locataire?.telephone      ?? '—',
    bienAdresse:     bien?.adresse_complete    ?? '—',
    bienCommune:     bien?.commune             ?? '—',
    loyerMoisFcfa,
    chargesMoisFcfa,
    totalFcfa,
    dateEcheance:    new Date(dateEcheanceIso).toLocaleDateString('fr-FR'),
    statut:          'en_attente',
  }

  // renderToBuffer attend createElement() — NE PAS passer du JSX direct
  const pdfBuffer = await renderToBuffer(createElement(QuittanceDocument, quittanceProps))

  // Créer le bucket si nécessaire
  await supabase.storage.createBucket('quittances', { public: false }).catch(() => {})

  const fileName = `quittance-${quittanceId}-${Date.now()}.pdf`
  const { error: uploadError } = await supabase.storage
    .from('quittances')
    .upload(fileName, pdfBuffer, { contentType: 'application/pdf', upsert: true })

  if (uploadError) {
    return NextResponse.json({ error: uploadError.message }, { status: 500 })
  }

  // URL signée 1 an
  const { data: signedData } = await supabase.storage
    .from('quittances')
    .createSignedUrl(fileName, 3600 * 24 * 365)

  const pdfUrl = signedData?.signedUrl ?? ''

  // Mettre à jour pdf_url
  await supabase.from('quittances').update({ pdf_url: pdfUrl }).eq('id', quittanceId)

  // Insérer notification loyer_rappel pour le locataire
  await supabase.from('notifications').insert({
    user_id:   contrat.locataire_id,
    type:      'loyer_rappel',
    titre:     'Votre quittance de loyer est disponible',
    contenu:   `Quittance pour ${new Date(moisIso).toLocaleDateString('fr-FR', { month: 'long', year: 'numeric' })} — Total: ${totalFcfa.toLocaleString('fr-FR')} FCFA`,
    lien_type: 'quittance',
    lien_id:   quittanceId,
  })

  return NextResponse.json({ quittanceId, pdfUrl, locataireTel: locataire?.telephone })
}
```

**2. Créer le répertoire n8n et le workflow JSON**

Créer `n8n/workflows/quittance-mensuelle.json` — workflow n8n exportable. Ce JSON suit le format d'export n8n v1 avec la structure `nodes` + `connections`.

```json
{
  "name": "Quittances Mensuelles — Immo CI",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 7 1 * *"
            }
          ]
        },
        "timezone": "Africa/Abidjan"
      },
      "id": "cron-node",
      "name": "Cron 1er du mois 07:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT c.id AS contrat_id, c.locataire_id, c.proprietaire_id, c.loyer_mois_fcfa, p.telephone AS locataire_tel FROM contrats c JOIN profiles p ON p.id = c.locataire_id WHERE c.statut = 'signe' AND (c.date_fin IS NULL OR c.date_fin > CURRENT_DATE)",
        "options": {}
      },
      "id": "supabase-query",
      "name": "Requête contrats actifs",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "position": [460, 300],
      "credentials": {
        "postgres": {
          "id": "supabase-pg-creds",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N8N_WEBHOOK_BASE_URL.replace('/webhook', '') }}/api/quittances/generer",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            { "name": "contratId", "value": "={{ $json.contrat_id }}" }
          ]
        },
        "options": { "response": { "response": { "responseFormat": "json" } } }
      },
      "id": "http-generer",
      "name": "Générer quittance PDF",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [680, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://graph.facebook.com/v19.0/{{ $env.WHATSAPP_PHONE_NUMBER_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Authorization", "value": "=Bearer {{ $env.WHATSAPP_ACCESS_TOKEN }}" },
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"messaging_product\": \"whatsapp\",\n  \"to\": \"{{ $('Requête contrats actifs').item.json.locataire_tel }}\",\n  \"type\": \"text\",\n  \"text\": {\n    \"body\": \"Bonjour, votre quittance de loyer du mois est disponible. Montant : {{ $('Requête contrats actifs').item.json.loyer_mois_fcfa }} FCFA. Téléchargez-la ici : {{ $json.pdfUrl }}\"\n  }\n}",
        "options": {}
      },
      "id": "whatsapp-node",
      "name": "Envoyer WhatsApp",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [900, 300]
    }
  ],
  "connections": {
    "Cron 1er du mois 07:00": {
      "main": [
        [{ "node": "Requête contrats actifs", "type": "main", "index": 0 }]
      ]
    },
    "Requête contrats actifs": {
      "main": [
        [{ "node": "Générer quittance PDF", "type": "main", "index": 0 }]
      ]
    },
    "Générer quittance PDF": {
      "main": [
        [{ "node": "Envoyer WhatsApp", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "timezone": "Africa/Abidjan",
    "saveManualExecutions": true,
    "callerPolicy": "workflowsFromSameOwner"
  },
  "staticData": null,
  "tags": ["immo-ci", "locatif", "quittances"],
  "triggerCount": 0,
  "updatedAt": "2026-04-07T00:00:00.000Z",
  "versionId": "1"
}
```

IMPORTANT: Créer d'abord le répertoire `n8n/workflows/` à la racine du monorepo (pas dans apps/web/).
  </action>
  <verify>
    <automated>test -f "apps/web/app/api/quittances/generer/route.ts" && grep -q "renderToBuffer" "apps/web/app/api/quittances/generer/route.ts" && grep -q "quittances" "apps/web/app/api/quittances/generer/route.ts" && test -f "n8n/workflows/quittance-mensuelle.json" && node -e "JSON.parse(require('fs').readFileSync('n8n/workflows/quittance-mensuelle.json','utf8')); console.log('JSON valid')" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `POST /api/quittances/generer` avec `{contratId: "uuid-valide"}` retourne `{quittanceId, pdfUrl}`
- La table quittances reçoit un enregistrement avec mois=1er du mois courant, statut='en_attente'
- pdf_url est une URL signée Supabase Storage (bucket 'quittances')
- Si la quittance existe déjà pour le mois (`skipped:true`), ne pas dupliquer
- Une notification `loyer_rappel` est insérée pour le locataire
- `n8n/workflows/quittance-mensuelle.json` est un JSON valide avec nodes + connections
- Le workflow n8n contient 4 noeuds: Cron, Postgres query, HTTP generer, WhatsApp
- L'expression cron `0 7 1 * *` est présente (1er du mois à 07:00)
  </acceptance_criteria>
  <done>API webhook quittances et workflow n8n JSON créés — pipeline de génération automatique opérationnel.</done>
</task>

</tasks>

<verification>
```bash
# Vérifier tous les fichiers créés
test -f "apps/web/lib/quittance-pdf.tsx" && echo "quittance-pdf.tsx OK"
test -f "supabase/functions/generer-quittance/index.ts" && echo "Edge Function OK"
test -f "apps/web/app/api/quittances/generer/route.ts" && echo "API route OK"
test -f "n8n/workflows/quittance-mensuelle.json" && echo "n8n workflow OK"

# Vérifier les patterns critiques
grep -q "currency: false" "apps/web/lib/quittance-pdf.tsx" && echo "currency:false OK"
grep -q "createElement(QuittanceDocument" "apps/web/app/api/quittances/generer/route.ts" && echo "createElement pattern OK"
grep -q "SUPABASE_SERVICE_ROLE_KEY" "apps/web/app/api/quittances/generer/route.ts" && echo "service role OK"
grep -q "loyer_rappel" "apps/web/app/api/quittances/generer/route.ts" && echo "notification OK"

# Vérifier JSON n8n
node -e "const w = JSON.parse(require('fs').readFileSync('n8n/workflows/quittance-mensuelle.json','utf8')); console.log('Nodes:', w.nodes.length, '— Connections:', Object.keys(w.connections).length)"

# TypeScript compile check
cd apps/web && npx tsc --noEmit 2>&1 | grep -E "quittance|error" | head -20
```
</verification>

<success_criteria>
1. `apps/web/lib/quittance-pdf.tsx` exporte QuittanceDocument avec montant en lettres et chiffres FCFA
2. `montantEnLettres(150000)` == "cent cinquante mille francs CFA" (vérifier currency:false)
3. `POST /api/quittances/generer` crée un enregistrement dans la table quittances avec tous les champs requis
4. Le PDF généré est uploadé dans le Storage bucket 'quittances' et pdf_url est mis à jour
5. Une notification `loyer_rappel` est insérée pour le locataire à chaque génération
6. Le workflow n8n JSON est valide et importable dans l'instance https://yobed-n8n-supabase-claude.hf.space
7. Le cron est configuré pour s'exécuter le 1er du mois à 07:00 en timezone Africa/Abidjan
8. L'idempotence est respectée: appeler 2x pour le même contrat+mois ne crée pas de doublon (UNIQUE INDEX)
</success_criteria>

<output>
After completion, create `.planning/phases/04-gestion-locative-avis-kyc/04-01-SUMMARY.md`
</output>
