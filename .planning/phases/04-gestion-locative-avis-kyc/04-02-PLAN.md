---
phase: 04-gestion-locative-avis-kyc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/whatsapp.ts
  - apps/web/app/api/quittances/webhook/route.ts
  - n8n/workflows/relance-loyer.json
  - apps/web/app/(pro)/quittances/page.tsx
autonomous: true
requirements: [LOC-03, LOC-04, LOC-05]

must_haves:
  truths:
    - "Le workflow n8n s'exécute chaque jour à 08:00 heure Abidjan"
    - "J-3 et J-1 avant échéance: message WhatsApp de rappel envoyé au locataire"
    - "J+1 après échéance: statut quittance passe à 'en_retard', WhatsApp d'alerte au locataire"
    - "J+7 après échéance: WhatsApp de relance finale + notification au propriétaire"
    - "Le propriétaire peut consulter toutes ses quittances avec filtres statut"
    - "La page /pro/quittances affiche statut, date d'échéance, et badge coloré par statut"
  artifacts:
    - path: "apps/web/lib/whatsapp.ts"
      provides: "Client WhatsApp Business API — sendWhatsApp(to, body)"
      exports: [sendWhatsApp, WhatsAppError]
    - path: "apps/web/app/api/quittances/webhook/route.ts"
      provides: "POST webhook — reçoit événements n8n (update statut, insert notification)"
    - path: "n8n/workflows/relance-loyer.json"
      provides: "Workflow n8n — cron quotidien 08:00, routing J-3/J-1/J+1/J+7, WhatsApp"
    - path: "apps/web/app/(pro)/quittances/page.tsx"
      provides: "Page Server Component — liste quittances propriétaire avec filtres"
  key_links:
    - from: "n8n/workflows/relance-loyer.json"
      to: "apps/web/app/api/quittances/webhook/route.ts"
      via: "HTTP POST N8N_WEBHOOK_BASE_URL/quittances/webhook"
    - from: "apps/web/app/api/quittances/webhook/route.ts"
      to: "supabase quittances table"
      via: "supabase.from('quittances').update({statut:'en_retard'})"
    - from: "n8n/workflows/relance-loyer.json"
      to: "WhatsApp Business API"
      via: "graph.facebook.com/v19.0/{PHONE_NUMBER_ID}/messages"
---

<objective>
Ce plan crée le système de relances automatiques pour les loyers impayés. Un workflow n8n quotidien (08:00 Abidjan) interroge les quittances en attente ou en retard, calcule les jalons J-3/J-1/J+1/J+7 par rapport à la date d'échéance, envoie des messages WhatsApp personnalisés via l'API WABA, et appelle un webhook Next.js pour mettre à jour les statuts et insérer des notifications. La page /pro/quittances offre au propriétaire une vue complète filtrée par statut.

Purpose: Réduire les impayés grâce aux relances automatiques — le propriétaire ivoirien n'a pas à relancer manuellement ses locataires.
Output: lib/whatsapp.ts client, API webhook relances, workflow n8n JSON, page quittances propriétaire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Schema quittances — champs utilisés dans ce plan -->
<!--
  id uuid PK
  contrat_id uuid FK contrats
  locataire_id uuid FK profiles
  proprietaire_id uuid FK profiles
  mois date  -- '2026-02-01'
  montant_total_fcfa integer
  date_echeance date  -- généralement le 5 du mois
  date_paiement date nullable
  statut text  -- en_attente|payee|en_retard|annulee
  pdf_url text nullable
-->

<!-- Schema notifications — pour insertion via webhook -->
<!--
  id uuid PK
  user_id uuid FK profiles
  type text  -- 'loyer_rappel', 'loyer_retard'
  titre text
  contenu text
  lu boolean default false
  lien_type text  -- 'quittance'
  lien_id uuid
-->

<!-- WhatsApp API — pattern EXACT -->
<!--
  POST https://graph.facebook.com/v19.0/{WHATSAPP_PHONE_NUMBER_ID}/messages
  Headers:
    Authorization: Bearer {WHATSAPP_ACCESS_TOKEN}
    Content-Type: application/json
  Body:
  {
    "messaging_product": "whatsapp",
    "to": "+2250102030405",       // format E.164 international
    "type": "text",
    "text": { "body": "Message ici" }
  }
  Réponse succès: { messages: [{ id: "wamid.xxx" }] }
  Réponse erreur: { error: { message: "...", code: 100 } }
-->

<!-- Calcul des jalons n8n — logique SQL ou JavaScript -->
<!--
  J-3: date_echeance - INTERVAL '3 days' = CURRENT_DATE  →  rappel 3 jours avant
  J-1: date_echeance - INTERVAL '1 day'  = CURRENT_DATE  →  rappel 1 jour avant
  J+1: date_echeance + INTERVAL '1 day'  = CURRENT_DATE  →  premier retard
  J+7: date_echeance + INTERVAL '7 days' = CURRENT_DATE  →  relance finale

  Query SQL dans n8n:
  SELECT q.id, q.contrat_id, q.locataire_id, q.proprietaire_id,
         q.montant_total_fcfa, q.date_echeance, q.statut,
         p.telephone AS locataire_tel,
         CASE
           WHEN q.date_echeance - CURRENT_DATE = 3 THEN 'J-3'
           WHEN q.date_echeance - CURRENT_DATE = 1 THEN 'J-1'
           WHEN CURRENT_DATE - q.date_echeance = 1 THEN 'J+1'
           WHEN CURRENT_DATE - q.date_echeance = 7 THEN 'J+7'
           ELSE NULL
         END AS jalon
  FROM quittances q
  JOIN profiles p ON p.id = q.locataire_id
  WHERE q.statut IN ('en_attente', 'en_retard')
    AND (
      q.date_echeance - CURRENT_DATE IN (3, 1)
      OR CURRENT_DATE - q.date_echeance IN (1, 7)
    )
-->

<!-- Africa/Abidjan = GMT+0 toute l'année — aucun changement d'heure DST -->
<!-- n8n cron expression: "0 8 * * *" = tous les jours à 08h00 -->

<!-- Pattern service role webhook (non authentifié) -->
<!--
  const supabase = createServerClient<any>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )
  // DOIT retourner HTTP 200 — sinon n8n retente indéfiniment
  return NextResponse.json({ status: 'ok' })
-->

<!-- Page quittances: Server Component pattern (identique à dashboard) -->
<!--
  import { createClient } from '@/lib/supabase/server'
  import { redirect } from 'next/navigation'
  // Fetch avec filtre statut depuis searchParams
  const { searchParams } = props
  const statut = searchParams?.statut ?? 'tous'
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/whatsapp.ts + API webhook relances</name>
  <files>apps/web/lib/whatsapp.ts, apps/web/app/api/quittances/webhook/route.ts</files>
  <read_first>apps/web/app/api/paiements/webhook/route.ts</read_first>
  <action>
**1. Créer `apps/web/lib/whatsapp.ts`**

Client WhatsApp Business API réutilisable dans toute l'application. Ne pas utiliser de SDK externe — appel fetch direct vers graph.facebook.com.

```typescript
// apps/web/lib/whatsapp.ts
// Client WhatsApp Business API (WABA) — Immo CI Platform
// Utilise l'API Graph Meta v19.0

export class WhatsAppError extends Error {
  constructor(
    message: string,
    public readonly code?: number,
    public readonly type?: string
  ) {
    super(message)
    this.name = 'WhatsAppError'
  }
}

export interface WhatsAppResult {
  messageId: string
  to: string
}

/**
 * Envoie un message texte WhatsApp via l'API WABA.
 * @param to Numéro E.164 international ex: "+2250102030405"
 * @param body Texte du message (max 4096 chars)
 * @throws WhatsAppError si l'envoi échoue
 */
export async function sendWhatsApp(to: string, body: string): Promise<WhatsAppResult> {
  const phoneNumberId = process.env.WHATSAPP_PHONE_NUMBER_ID
  const accessToken   = process.env.WHATSAPP_ACCESS_TOKEN

  if (!phoneNumberId || !accessToken) {
    throw new WhatsAppError('WHATSAPP_PHONE_NUMBER_ID et WHATSAPP_ACCESS_TOKEN requis')
  }

  // Normaliser le numéro: supprimer espaces et s'assurer du format E.164
  const normalizedTo = to.replace(/\s+/g, '').startsWith('+') ? to.replace(/\s+/g, '') : `+${to.replace(/\s+/g, '')}`

  const url = `https://graph.facebook.com/v19.0/${phoneNumberId}/messages`

  const response = await fetch(url, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${accessToken}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      messaging_product: 'whatsapp',
      to: normalizedTo,
      type: 'text',
      text: { body: body.slice(0, 4096) },  // truncate to WhatsApp limit
    }),
  })

  const data = await response.json() as {
    messages?: Array<{ id: string }>
    error?: { message: string; code: number; type: string }
  }

  if (!response.ok || data.error) {
    throw new WhatsAppError(
      data.error?.message ?? `WhatsApp API error ${response.status}`,
      data.error?.code,
      data.error?.type
    )
  }

  return {
    messageId: data.messages?.[0]?.id ?? '',
    to: normalizedTo,
  }
}

/** Templates de messages pour les jalons de relance loyer */
export const RELANCE_MESSAGES = {
  'J-3': (montant: number, dateEcheance: string) =>
    `Bonjour, rappel : votre loyer de ${montant.toLocaleString('fr-FR')} FCFA est dû le ${dateEcheance}. Merci de procéder au règlement dans les délais. — Immo CI`,

  'J-1': (montant: number, dateEcheance: string) =>
    `RAPPEL URGENT : votre loyer de ${montant.toLocaleString('fr-FR')} FCFA est dû DEMAIN (${dateEcheance}). Veuillez régulariser votre situation. — Immo CI`,

  'J+1': (montant: number, dateEcheance: string) =>
    `⚠️ Votre loyer de ${montant.toLocaleString('fr-FR')} FCFA était dû le ${dateEcheance}. Votre compte est maintenant en retard. Contactez votre propriétaire pour régulariser. — Immo CI`,

  'J+7': (montant: number, _dateEcheance: string) =>
    `DERNIER RAPPEL : votre loyer de ${montant.toLocaleString('fr-FR')} FCFA est en retard depuis 7 jours. Sans règlement, des pénalités contractuelles s'appliquent. Contactez votre propriétaire immédiatement. — Immo CI`,
} as const

export type RelanceJalon = keyof typeof RELANCE_MESSAGES
```

**2. Créer `apps/web/app/api/quittances/webhook/route.ts`**

Ce webhook est appelé par n8n après avoir envoyé le WhatsApp. Il met à jour le statut de la quittance (en_retard à J+1) et insère une notification pour le propriétaire (J+7). DOIT retourner HTTP 200 — n8n retente indéfiniment en cas d'erreur.

```typescript
// apps/web/app/api/quittances/webhook/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createServerClient } from '@supabase/ssr'

// Payload envoyé par n8n pour chaque quittance
interface RelancePayload {
  quittanceId:    string
  locataireId:    string
  proprietaireId: string
  jalon:          'J-3' | 'J-1' | 'J+1' | 'J+7'
  montantFcfa:    number
  dateEcheance:   string
}

export async function POST(req: NextRequest) {
  // Service role: webhook non authentifié depuis n8n
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = createServerClient<any>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )

  let payload: RelancePayload
  try {
    payload = await req.json() as RelancePayload
  } catch {
    // Body invalide — retourner 200 pour éviter les retries n8n
    return NextResponse.json({ status: 'ok', warning: 'body invalide' })
  }

  const { quittanceId, locataireId, proprietaireId, jalon, montantFcfa, dateEcheance } = payload

  if (!quittanceId || !jalon) {
    return NextResponse.json({ status: 'ok', warning: 'quittanceId ou jalon manquant' })
  }

  // J+1: passer le statut à 'en_retard'
  if (jalon === 'J+1') {
    await supabase
      .from('quittances')
      .update({ statut: 'en_retard' })
      .eq('id', quittanceId)
      .eq('statut', 'en_attente')  // ne pas écraser si déjà en_retard

    // Notification locataire: loyer_retard
    await supabase.from('notifications').insert({
      user_id:   locataireId,
      type:      'loyer_retard',
      titre:     'Loyer en retard',
      contenu:   `Votre loyer de ${montantFcfa.toLocaleString('fr-FR')} FCFA était dû le ${dateEcheance} et n'a pas été reçu.`,
      lien_type: 'quittance',
      lien_id:   quittanceId,
    })
  }

  // J+7: notification propriétaire (loyer_retard)
  if (jalon === 'J+7') {
    await supabase.from('notifications').insert({
      user_id:   proprietaireId,
      type:      'loyer_retard',
      titre:     'Loyer impayé — 7 jours de retard',
      contenu:   `Un locataire a ${montantFcfa.toLocaleString('fr-FR')} FCFA en retard depuis 7 jours (échéance: ${dateEcheance}).`,
      lien_type: 'quittance',
      lien_id:   quittanceId,
    })
  }

  // J-3 et J-1: notification locataire loyer_rappel
  if (jalon === 'J-3' || jalon === 'J-1') {
    await supabase.from('notifications').insert({
      user_id:   locataireId,
      type:      'loyer_rappel',
      titre:     `Rappel loyer — ${jalon}`,
      contenu:   `Votre loyer de ${montantFcfa.toLocaleString('fr-FR')} FCFA est dû le ${dateEcheance}.`,
      lien_type: 'quittance',
      lien_id:   quittanceId,
    })
  }

  // CRITIQUE: toujours retourner 200 — sinon n8n retente indéfiniment
  return NextResponse.json({ status: 'ok', processed: quittanceId, jalon })
}
```
  </action>
  <verify>
    <automated>test -f "apps/web/lib/whatsapp.ts" && grep -q "sendWhatsApp" "apps/web/lib/whatsapp.ts" && grep -q "graph.facebook.com/v19.0" "apps/web/lib/whatsapp.ts" && test -f "apps/web/app/api/quittances/webhook/route.ts" && grep -q "en_retard" "apps/web/app/api/quittances/webhook/route.ts" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `apps/web/lib/whatsapp.ts` exporte sendWhatsApp(to, body) et RELANCE_MESSAGES
- sendWhatsApp normalise le numéro au format E.164 (+225...)
- RELANCE_MESSAGES contient les 4 jalons: J-3, J-1, J+1, J+7
- `POST /api/quittances/webhook` avec `{jalon:'J+1'}` met à jour statut='en_retard'
- `POST /api/quittances/webhook` insère une notification pour le locataire (J-3, J-1, J+1)
- `POST /api/quittances/webhook` insère une notification pour le propriétaire (J+7)
- Le webhook retourne toujours HTTP 200 même en cas de payload invalide
  </acceptance_criteria>
  <done>Client WhatsApp et webhook relances créés — les jalons J-3/J-1/J+1/J+7 sont gérés correctement.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow n8n relances JSON + page /pro/quittances</name>
  <files>n8n/workflows/relance-loyer.json, apps/web/app/(pro)/quittances/page.tsx</files>
  <read_first>apps/web/app/(pro)/dashboard/page.tsx, apps/web/lib/whatsapp.ts</read_first>
  <action>
**1. Créer `n8n/workflows/relance-loyer.json`**

Workflow n8n exportable. Exécution quotidienne à 08:00 heure Abidjan. La requête SQL calcule directement les jalons. Pour chaque quittance, n8n envoie le WhatsApp puis appelle le webhook pour mettre à jour le statut/notifications.

```json
{
  "name": "Relances Loyer — Immo CI",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 8 * * *"
            }
          ]
        },
        "timezone": "Africa/Abidjan"
      },
      "id": "cron-daily",
      "name": "Cron quotidien 08:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT q.id AS quittance_id, q.contrat_id, q.locataire_id, q.proprietaire_id, q.montant_total_fcfa, q.date_echeance, q.statut, p.telephone AS locataire_tel, pp.telephone AS proprietaire_tel, CASE WHEN (q.date_echeance::date - CURRENT_DATE) = 3 THEN 'J-3' WHEN (q.date_echeance::date - CURRENT_DATE) = 1 THEN 'J-1' WHEN (CURRENT_DATE - q.date_echeance::date) = 1 THEN 'J+1' WHEN (CURRENT_DATE - q.date_echeance::date) = 7 THEN 'J+7' ELSE NULL END AS jalon FROM quittances q JOIN profiles p ON p.id = q.locataire_id JOIN profiles pp ON pp.id = q.proprietaire_id WHERE q.statut IN ('en_attente', 'en_retard') AND ((q.date_echeance::date - CURRENT_DATE) IN (3, 1) OR (CURRENT_DATE - q.date_echeance::date) IN (1, 7))",
        "options": {}
      },
      "id": "postgres-query",
      "name": "Quittances à relancer",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2,
      "position": [460, 300],
      "credentials": {
        "postgres": {
          "id": "supabase-pg-creds",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": { "caseSensitive": true, "leftValue": "", "typeValidation": "strict" },
          "conditions": [
            {
              "leftValue": "={{ $json.jalon }}",
              "rightValue": "",
              "operator": { "type": "string", "operation": "notEmpty" }
            }
          ],
          "combinator": "and"
        }
      },
      "id": "filter-jalon",
      "name": "Filtrer jalon non null",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 2,
      "position": [680, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://graph.facebook.com/v19.0/{{ $env.WHATSAPP_PHONE_NUMBER_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Authorization", "value": "=Bearer {{ $env.WHATSAPP_ACCESS_TOKEN }}" },
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ messaging_product: 'whatsapp', to: $json.locataire_tel, type: 'text', text: { body: $json.jalon === 'J-3' ? 'Rappel : votre loyer de ' + $json.montant_total_fcfa.toLocaleString('fr-FR') + ' FCFA est dû le ' + $json.date_echeance + '. Merci de procéder au règlement. — Immo CI' : $json.jalon === 'J-1' ? 'RAPPEL URGENT : votre loyer de ' + $json.montant_total_fcfa.toLocaleString('fr-FR') + ' FCFA est dû DEMAIN (' + $json.date_echeance + '). — Immo CI' : $json.jalon === 'J+1' ? '⚠️ Votre loyer de ' + $json.montant_total_fcfa.toLocaleString('fr-FR') + ' FCFA était dû le ' + $json.date_echeance + '. Votre compte est en retard. — Immo CI' : 'DERNIER RAPPEL : votre loyer de ' + $json.montant_total_fcfa.toLocaleString('fr-FR') + ' FCFA est en retard depuis 7 jours. Contactez votre propriétaire. — Immo CI' } }) }}",
        "options": { "response": { "response": { "responseFormat": "json" } } }
      },
      "id": "whatsapp-send",
      "name": "Envoyer WhatsApp relance",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [900, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N8N_WEBHOOK_BASE_URL.replace('/webhook', '') }}/api/quittances/webhook",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ quittanceId: $('Quittances à relancer').item.json.quittance_id, locataireId: $('Quittances à relancer').item.json.locataire_id, proprietaireId: $('Quittances à relancer').item.json.proprietaire_id, jalon: $('Quittances à relancer').item.json.jalon, montantFcfa: $('Quittances à relancer').item.json.montant_total_fcfa, dateEcheance: $('Quittances à relancer').item.json.date_echeance }) }}",
        "options": {}
      },
      "id": "webhook-update",
      "name": "Mettre à jour statut & notifications",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1120, 300]
    }
  ],
  "connections": {
    "Cron quotidien 08:00": {
      "main": [
        [{ "node": "Quittances à relancer", "type": "main", "index": 0 }]
      ]
    },
    "Quittances à relancer": {
      "main": [
        [{ "node": "Filtrer jalon non null", "type": "main", "index": 0 }]
      ]
    },
    "Filtrer jalon non null": {
      "main": [
        [{ "node": "Envoyer WhatsApp relance", "type": "main", "index": 0 }]
      ]
    },
    "Envoyer WhatsApp relance": {
      "main": [
        [{ "node": "Mettre à jour statut & notifications", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "timezone": "Africa/Abidjan",
    "saveManualExecutions": true,
    "callerPolicy": "workflowsFromSameOwner",
    "errorWorkflow": ""
  },
  "staticData": null,
  "tags": ["immo-ci", "locatif", "relances"],
  "triggerCount": 0,
  "updatedAt": "2026-04-07T00:00:00.000Z",
  "versionId": "1"
}
```

**2. Créer `apps/web/app/(pro)/quittances/page.tsx`**

Page Server Component — liste toutes les quittances du propriétaire connecté, filtrée par statut via searchParams. Suit le pattern exact du dashboard (Server Component + fetch Supabase parallèle).

```typescript
// apps/web/app/(pro)/quittances/page.tsx
import { createClient }  from '@/lib/supabase/server'
import { redirect }      from 'next/navigation'
import { Badge }         from '@/components/ui/Badge'
import { Card }          from '@/components/ui/Card'
import Link              from 'next/link'

export const metadata = { title: 'Mes Quittances — Immo CI' }

// Couleurs statut quittances
const STATUT_BADGE: Record<string, { variant: 'default' | 'success' | 'danger' | 'warning'; label: string }> = {
  en_attente: { variant: 'warning',  label: 'En attente' },
  payee:      { variant: 'success',  label: 'Payée' },
  en_retard:  { variant: 'danger',   label: 'En retard' },
  annulee:    { variant: 'default',  label: 'Annulée' },
}

const FILTRES = [
  { value: 'tous',       label: 'Toutes' },
  { value: 'en_attente', label: 'En attente' },
  { value: 'en_retard',  label: 'En retard' },
  { value: 'payee',      label: 'Payées' },
]

interface PageProps {
  searchParams: Promise<{ statut?: string }>
}

export default async function QuittancesPage({ searchParams }: PageProps) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  const { statut: filtreStatut } = await searchParams
  const filtreActif = filtreStatut && filtreStatut !== 'tous' ? filtreStatut : null

  // Query quittances du propriétaire — avec join contrat + locataire
  let query = (supabase.from('quittances') as any)
    .select(`
      id, mois, montant_loyer_fcfa, montant_charges_fcfa, montant_total_fcfa,
      date_echeance, date_paiement, statut, pdf_url,
      contrats ( id, bien_id, biens ( titre, commune ) ),
      profiles!locataire_id ( nom_complet, telephone )
    `)
    .eq('proprietaire_id', user.id)
    .order('date_echeance', { ascending: false })
    .limit(50)

  if (filtreActif) {
    query = query.eq('statut', filtreActif)
  }

  const { data: quittances } = await query

  // Stats rapides
  const stats = {
    total:      (quittances ?? []).length,
    en_retard:  (quittances ?? []).filter((q: any) => q.statut === 'en_retard').length,
    en_attente: (quittances ?? []).filter((q: any) => q.statut === 'en_attente').length,
    payee:      (quittances ?? []).filter((q: any) => q.statut === 'payee').length,
  }

  return (
    <div className="max-w-5xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-gray-900 mb-2">Mes Quittances</h1>
      <p className="text-gray-500 mb-6">Suivi des loyers de vos locataires</p>

      {/* Stats rapides */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
        <Card className="p-4 text-center">
          <p className="text-2xl font-bold">{stats.total}</p>
          <p className="text-sm text-gray-500">Total</p>
        </Card>
        <Card className="p-4 text-center border-l-4 border-yellow-400">
          <p className="text-2xl font-bold text-yellow-600">{stats.en_attente}</p>
          <p className="text-sm text-gray-500">En attente</p>
        </Card>
        <Card className="p-4 text-center border-l-4 border-red-500">
          <p className="text-2xl font-bold text-red-600">{stats.en_retard}</p>
          <p className="text-sm text-gray-500">En retard</p>
        </Card>
        <Card className="p-4 text-center border-l-4 border-green-500">
          <p className="text-2xl font-bold text-green-600">{stats.payee}</p>
          <p className="text-sm text-gray-500">Payées</p>
        </Card>
      </div>

      {/* Filtres statut */}
      <div className="flex gap-2 mb-6 flex-wrap">
        {FILTRES.map(f => (
          <Link
            key={f.value}
            href={`/pro/quittances${f.value === 'tous' ? '' : `?statut=${f.value}`}`}
            className={`px-4 py-2 rounded-full text-sm font-medium border transition-colors ${
              (filtreStatut ?? 'tous') === f.value
                ? 'bg-primary text-white border-primary'
                : 'bg-white text-gray-700 border-gray-200 hover:border-primary'
            }`}
          >
            {f.label}
          </Link>
        ))}
      </div>

      {/* Liste quittances */}
      {!quittances || quittances.length === 0 ? (
        <Card className="p-12 text-center text-gray-500">
          <p className="text-lg">Aucune quittance trouvée</p>
          <p className="text-sm mt-2">Les quittances sont générées automatiquement le 1er de chaque mois.</p>
        </Card>
      ) : (
        <div className="space-y-3">
          {quittances.map((q: any) => {
            const bien   = q.contrats?.biens as { titre: string; commune: string } | null
            const locataire = q['profiles!locataire_id'] as { nom_complet: string; telephone: string } | null
            const badge  = STATUT_BADGE[q.statut] ?? { variant: 'default' as const, label: q.statut }
            const moisLabel = new Date(q.mois).toLocaleDateString('fr-FR', { month: 'long', year: 'numeric' })

            return (
              <Card key={q.id} className="p-4 flex items-center justify-between gap-4">
                <div className="flex-1 min-w-0">
                  <div className="flex items-center gap-2 mb-1">
                    <span className="font-semibold text-gray-900 capitalize">{moisLabel}</span>
                    <Badge variant={badge.variant}>{badge.label}</Badge>
                  </div>
                  <p className="text-sm text-gray-600 truncate">
                    {locataire?.nom_complet ?? 'Locataire'} · {bien?.titre ?? 'Bien'}, {bien?.commune}
                  </p>
                  <p className="text-xs text-gray-400 mt-1">
                    Échéance: {new Date(q.date_echeance).toLocaleDateString('fr-FR')}
                    {q.date_paiement && ` · Payé le: ${new Date(q.date_paiement).toLocaleDateString('fr-FR')}`}
                  </p>
                </div>
                <div className="text-right shrink-0">
                  <p className="font-bold text-gray-900">{Number(q.montant_total_fcfa).toLocaleString('fr-FR')} FCFA</p>
                  {q.pdf_url && (
                    <a
                      href={q.pdf_url}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="text-sm text-primary hover:underline"
                    >
                      Télécharger PDF
                    </a>
                  )}
                </div>
              </Card>
            )
          })}
        </div>
      )}
    </div>
  )
}
```

IMPORTANT: Vérifier que `Badge` dans `@/components/ui/Badge` accepte les variants `warning` et `error`. Si le composant Badge existant n'a que `default` et `success`, ajouter ces variants dans le fichier Badge existant. Lire `apps/web/components/ui/Badge.tsx` avant de finir cette tâche.
  </action>
  <verify>
    <automated>test -f "n8n/workflows/relance-loyer.json" && node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflows/relance-loyer.json','utf8')); console.log('Nodes:', w.nodes.length); if(w.nodes.length < 4) process.exit(1)" && test -f "apps/web/app/(pro)/quittances/page.tsx" && grep -q "proprietaire_id" "apps/web/app/(pro)/quittances/page.tsx" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `n8n/workflows/relance-loyer.json` JSON valide avec 5 noeuds: Cron, Postgres, Filter, WhatsApp, Webhook
- L'expression cron `0 8 * * *` est présente (quotidien 08:00)
- La query SQL contient `CASE WHEN` pour calculer les jalons J-3/J-1/J+1/J+7
- Le noeud Filter élimine les lignes avec jalon NULL avant d'envoyer WhatsApp
- `apps/web/app/(pro)/quittances/page.tsx` est un Server Component (pas de 'use client')
- La page affiche les quittances filtrées par `searchParams.statut`
- Les 4 filtres (Toutes, En attente, En retard, Payées) sont des liens Next.js
- Les badges de statut sont colorés: yellow/en_attente, red/en_retard, green/payee
- Le lien "Télécharger PDF" apparaît si pdf_url est non nul
- Les stats rapides (total, en_attente, en_retard, payee) sont affichées en haut
  </acceptance_criteria>
  <done>Workflow n8n relances et page /pro/quittances créés — relances automatiques J-3/J-1/J+1/J+7 opérationnelles.</done>
</task>

</tasks>

<verification>
```bash
# Fichiers créés
test -f "apps/web/lib/whatsapp.ts" && echo "whatsapp.ts OK"
test -f "apps/web/app/api/quittances/webhook/route.ts" && echo "webhook OK"
test -f "n8n/workflows/relance-loyer.json" && echo "relance workflow OK"
test -f "apps/web/app/(pro)/quittances/page.tsx" && echo "quittances page OK"

# Patterns critiques
grep -q "graph.facebook.com/v19.0" "apps/web/lib/whatsapp.ts" && echo "WhatsApp URL OK"
grep -q "RELANCE_MESSAGES" "apps/web/lib/whatsapp.ts" && echo "Messages templates OK"
grep -q "en_retard" "apps/web/app/api/quittances/webhook/route.ts" && echo "Statut update OK"
grep -q "J+7" "apps/web/app/api/quittances/webhook/route.ts" && echo "J+7 proprietaire notif OK"

# n8n JSON valide
node -e "const w=JSON.parse(require('fs').readFileSync('n8n/workflows/relance-loyer.json','utf8')); console.log('Valid JSON, nodes:', w.nodes.length, 'connections:', Object.keys(w.connections).length)"

# TypeScript compile check
cd apps/web && npx tsc --noEmit 2>&1 | grep -E "whatsapp|quittances|error" | head -20
```
</verification>

<success_criteria>
1. `sendWhatsApp("+2250102030405", "test")` appelle `graph.facebook.com/v19.0/{ID}/messages` avec Bearer token
2. `RELANCE_MESSAGES['J+1'](150000, '05/02/2026')` retourne le message de retard correct
3. `POST /api/quittances/webhook` avec `{jalon:'J+1', quittanceId:'xxx'}` met à jour statut='en_retard'
4. `POST /api/quittances/webhook` retourne HTTP 200 même si le payload est invalide
5. Le workflow n8n JSON est importable — 5 noeuds visibles dans l'interface n8n
6. Le cron `0 8 * * *` est bien configuré en timezone Africa/Abidjan
7. La page `/pro/quittances` charge les quittances du propriétaire sans erreur
8. Le filtre `?statut=en_retard` retourne uniquement les quittances en retard
9. Le PDF est téléchargeable via lien direct si pdf_url est présent
</success_criteria>

<output>
After completion, create `.planning/phases/04-gestion-locative-avis-kyc/04-02-SUMMARY.md`
</output>
