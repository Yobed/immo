---
phase: 03-paiements-r-servations-ia-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/lib/cinetpay.ts
  - apps/web/app/api/paiements/initier/route.ts
  - apps/web/app/api/paiements/webhook/route.ts
  - apps/web/app/(client)/paiement/retour/page.tsx
  - apps/web/components/paiements/PaiementButton.tsx
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06]

user_setup:
  - service: cinetpay
    why: "Paiements mobiles Côte d'Ivoire (Wave, Orange Money, MTN, Moov, CB)"
    env_vars:
      - name: CINETPAY_API_KEY
        source: "CinetPay Dashboard -> Mon Compte -> API Keys"
      - name: CINETPAY_SITE_ID
        source: "CinetPay Dashboard -> Mon Compte -> API Keys -> Site ID"
      - name: CINETPAY_SECRET_KEY
        source: "CinetPay Dashboard -> Mon Compte -> API Keys -> Secret Key"
      - name: CINETPAY_BASE_URL
        source: "Valeur fixe : https://api-checkout.cinetpay.com/v2"

must_haves:
  truths:
    - "Un utilisateur peut initier un paiement et est redirigé vers la page CinetPay"
    - "Le webhook CinetPay appelle verifierPaiement() -> /v2/payment/check avant toute mise a jour BDD"
    - "La table paiements est mise a jour avec statut succes ou echec apres webhook"
    - "La commission 10% est calculee et stockee dans commission_fcfa / montant_net_fcfa"
    - "La page de retour affiche un etat clair (succes / echec / annulation)"
  artifacts:
    - path: "apps/web/lib/cinetpay.ts"
      provides: "initierPaiement() + verifierPaiement() + calculerSplit()"
      exports: ["initierPaiement", "verifierPaiement", "calculerSplit", "arrondir5", "canalVersMethode"]
    - path: "apps/web/app/api/paiements/initier/route.ts"
      provides: "POST /api/paiements/initier"
    - path: "apps/web/app/api/paiements/webhook/route.ts"
      provides: "POST /api/paiements/webhook — appel /v2/payment/check obligatoire"
    - path: "apps/web/app/(client)/paiement/retour/page.tsx"
      provides: "Page de retour paiement avec statut depuis searchParams"
    - path: "apps/web/components/paiements/PaiementButton.tsx"
      provides: "Bouton declencheur de paiement reutilisable"
  key_links:
    - from: "PaiementButton.tsx"
      to: "/api/paiements/initier"
      via: "fetch POST"
      pattern: "fetch.*api/paiements/initier"
    - from: "app/api/paiements/webhook/route.ts"
      to: "verifierPaiement()"
      via: "appel /v2/payment/check OBLIGATOIRE — ne jamais lire le statut dans le body"
      pattern: "verifierPaiement"
    - from: "app/api/paiements/webhook/route.ts"
      to: "supabase.from('paiements').update"
      via: "service role key (bypass RLS — webhook est unauthentifie)"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Integration CinetPay complete : initiation de paiement, webhook securise avec verification /v2/payment/check, calcul split commission 10%, page de retour, et composant bouton reutilisable.

Purpose: Permettre aux locataires de payer loyers et reservations via Wave, Orange Money, MTN, Moov Money et carte bancaire — les 5 operateurs CI couverts par CinetPay.
Output: lib/cinetpay.ts + 2 routes API + page retour + PaiementButton composant + toutes deps Phase 3 installees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md

@apps/web/app/api/biens/route.ts
@apps/web/lib/supabase/server.ts
@apps/web/package.json

<interfaces>
<!-- Pattern API route etabli (apps/web/app/api/biens/route.ts) -->
```typescript
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
// createClient() est async — toujours await
// supabase.from('table') as any — database.ts est un placeholder
// Retourner NextResponse.json({ error: ... }, { status: 4xx }) pour les erreurs
```

<!-- Table paiements (migration 008) — colonnes cles -->
```
id uuid, reservation_id uuid, locataire_id uuid,
montant_fcfa numeric, commission_fcfa numeric, montant_net_fcfa numeric,
methode text (wave/orange_money/mtn/moov/carte_bancaire),
statut text (initie/en_cours/succes/echec/annule/rembourse),
cinetpay_transaction_id text UNIQUE, cinetpay_payment_token text,
metadata jsonb, created_at timestamptz
```

<!-- CinetPay API -->
```
POST https://api-checkout.cinetpay.com/v2/payment        -> initier (retourne payment_url)
POST https://api-checkout.cinetpay.com/v2/payment/check  -> verifier statut reel
Webhook body: x-www-form-urlencoded, champ cpm_trans_id
CRITIQUE: Le webhook NE CONTIENT PAS le statut final — toujours appeler /check
Webhook response: DOIT retourner HTTP 200 (sinon CinetPay retente indefiniment)
Montants: multiple de 5 FCFA obligatoire (erreur 703 sinon)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer les dependances Phase 3 et creer lib/cinetpay.ts</name>
  <files>
    apps/web/package.json
    apps/web/lib/cinetpay.ts
  </files>
  <read_first>
    - apps/web/package.json (verifier deps existantes avant install)
    - apps/web/lib/supabase/server.ts (pattern createClient async)
    - .planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md (Standard Stack section)
  </read_first>
  <action>
Etape 1 — Installer TOUTES les dependances Phase 3 en une seule commande depuis la racine du monorepo (--legacy-peer-deps est le standard projet etabli pour pannellum-react) :

```bash
npm install --legacy-peer-deps \
  @anthropic-ai/sdk \
  @react-pdf/renderer \
  recharts \
  @tremor/react \
  to-words \
  @headlessui/react \
  @remixicon/react \
  --workspace=apps/web

npm install --legacy-peer-deps -D @tailwindcss/forms --workspace=apps/web
```

Etape 2 — Creer apps/web/lib/cinetpay.ts avec exactement ce contenu :

```typescript
// lib/cinetpay.ts
// Integration CinetPay v2 — PAY-01 a PAY-06

const CINETPAY_BASE = process.env.CINETPAY_BASE_URL ?? 'https://api-checkout.cinetpay.com/v2'

export const COMMISSION_PCT = 10  // 10% plateforme

/** Calcule le split commission/net. Arrondit au multiple de 5 (regle XOF). */
export function calculerSplit(montantTotal: number): { commission: number; montantNet: number } {
  const commission = Math.round((montantTotal * COMMISSION_PCT / 100) / 5) * 5
  const montantNet = montantTotal - commission
  return { commission, montantNet }
}

/** Arrondit au multiple de 5 le plus proche — CinetPay exige un montant XOF valide */
export function arrondir5(montant: number): number {
  return Math.round(montant / 5) * 5
}

export interface InitierParams {
  transactionId: string   // crypto.randomUUID() — doit etre unique par tentative
  montant: number         // deja arrondi via arrondir5()
  description: string
  notifyUrl: string       // NEXT_PUBLIC_URL + '/api/paiements/webhook'
  returnUrl: string       // NEXT_PUBLIC_URL + '/paiement/retour'
  metadata?: string       // JSON.stringify({ reservationId, ... })
}

export interface CinetPayInitResponse {
  code:    string
  message: string
  data?: {
    payment_token: string
    payment_url:   string
  }
}

export async function initierPaiement(params: InitierParams): Promise<CinetPayInitResponse> {
  const res = await fetch(`${CINETPAY_BASE}/payment`, {
    method:  'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      apikey:         process.env.CINETPAY_API_KEY,
      site_id:        process.env.CINETPAY_SITE_ID,
      transaction_id: params.transactionId,
      amount:         params.montant,
      currency:       'XOF',
      description:    params.description,
      notify_url:     params.notifyUrl,
      return_url:     params.returnUrl,
      channels:       'ALL',   // Wave + Orange Money + MTN + Moov + CB
      lang:           'fr',
      metadata:       params.metadata ?? '',
    }),
  })
  return res.json()
}

export interface VerificationResponse {
  code:    string
  message: string
  data?: {
    status:         string   // 'ACCEPTED' | 'REFUSED' | 'WAITING_FOR_CUSTOMER'
    transaction_id: string
    amount:         number
    currency:       string
    payment_method: string   // 'WAVE-CI' | 'ORANGE-CI' | 'MTN-CI' | 'MOOV-CI' | 'CB'
    payment_date:   string
  }
}

/**
 * CRITIQUE PAY-03 : Appeler apres reception webhook.
 * Ne JAMAIS lire le statut depuis le body du webhook — CinetPay l'omet intentionnellement.
 */
export async function verifierPaiement(transactionId: string): Promise<VerificationResponse> {
  const res = await fetch(`${CINETPAY_BASE}/payment/check`, {
    method:  'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      apikey:         process.env.CINETPAY_API_KEY,
      site_id:        process.env.CINETPAY_SITE_ID,
      transaction_id: transactionId,
    }),
  })
  return res.json()
}

/** Convertit le code canal CinetPay vers la valeur enum de la table paiements */
export function canalVersMethode(canal: string): string {
  const map: Record<string, string> = {
    'WAVE-CI':   'wave',
    'ORANGE-CI': 'orange_money',
    'MTN-CI':    'mtn',
    'MOOV-CI':   'moov',
    'CB':        'carte_bancaire',
  }
  return map[canal] ?? 'autre'
}
```
  </action>
  <verify>
    <automated>ls "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/node_modules/@anthropic-ai/sdk/package.json" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/node_modules/recharts/package.json" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/node_modules/@tremor/react/package.json" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/node_modules/to-words/package.json" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/node_modules/@react-pdf/renderer/package.json" && grep -l "verifierPaiement\|initierPaiement\|calculerSplit" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/lib/cinetpay.ts"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/cinetpay.ts existe et contient les exports: initierPaiement, verifierPaiement, calculerSplit, arrondir5, canalVersMethode
    - apps/web/node_modules/@anthropic-ai/sdk existe
    - apps/web/node_modules/@react-pdf/renderer existe
    - apps/web/node_modules/recharts existe
    - apps/web/node_modules/@tremor/react existe
    - apps/web/node_modules/to-words existe
    - Le commentaire "CRITIQUE PAY-03" est present dans cinetpay.ts
    - arrondir5(150003) retourne 150005 (multiple de 5)
  </acceptance_criteria>
  <done>5 packages installes, lib/cinetpay.ts cree avec initierPaiement + verifierPaiement + calculerSplit + arrondir5 + canalVersMethode</done>
</task>

<task type="auto">
  <name>Task 2: Routes API paiements (initier + webhook) et page de retour</name>
  <files>
    apps/web/app/api/paiements/initier/route.ts
    apps/web/app/api/paiements/webhook/route.ts
    apps/web/app/(client)/paiement/retour/page.tsx
    apps/web/components/paiements/PaiementButton.tsx
  </files>
  <read_first>
    - apps/web/lib/cinetpay.ts (interfaces et fonctions de la Task 1)
    - apps/web/app/api/biens/route.ts (pattern NextResponse + createClient)
    - apps/web/lib/supabase/server.ts (createClient async)
  </read_first>
  <action>
**Creer apps/web/app/api/paiements/initier/route.ts (PAY-02) :**
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { initierPaiement, arrondir5, calculerSplit } from '@/lib/cinetpay'
import crypto from 'crypto'

export async function POST(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifie' }, { status: 401 })

  const body = await req.json() as {
    reservationId: string
    montantFcfa:   number
    description:   string
  }

  const montant      = arrondir5(body.montantFcfa)
  const { commission, montantNet } = calculerSplit(montant)
  const transactionId = crypto.randomUUID()
  const baseUrl       = process.env.NEXT_PUBLIC_URL ?? 'http://localhost:3000'

  // Inserer la ligne paiement avec statut initie
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error: insertError } = await (supabase.from('paiements') as any).insert({
    reservation_id:          body.reservationId,
    locataire_id:            user.id,
    montant_fcfa:            montant,
    commission_fcfa:         commission,
    montant_net_fcfa:        montantNet,
    statut:                  'initie',
    cinetpay_transaction_id: transactionId,
  })
  if (insertError) return NextResponse.json({ error: insertError.message }, { status: 400 })

  const result = await initierPaiement({
    transactionId,
    montant,
    description:  body.description,
    notifyUrl:    `${baseUrl}/api/paiements/webhook`,
    returnUrl:    `${baseUrl}/paiement/retour`,
    metadata:     JSON.stringify({ reservationId: body.reservationId }),
  })

  if (result.code !== '201' || !result.data?.payment_url) {
    return NextResponse.json({ error: result.message }, { status: 502 })
  }

  // Mettre a jour le token CinetPay
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  await (supabase.from('paiements') as any)
    .update({ cinetpay_payment_token: result.data.payment_token, statut: 'en_cours' })
    .eq('cinetpay_transaction_id', transactionId)

  return NextResponse.json({ paymentUrl: result.data.payment_url })
}
```

**Creer apps/web/app/api/paiements/webhook/route.ts (PAY-03, PAY-04, PAY-05) :**
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createServerClient } from '@supabase/ssr'
import { verifierPaiement, canalVersMethode } from '@/lib/cinetpay'

export async function POST(req: NextRequest) {
  // Webhook body = x-www-form-urlencoded
  const formData    = await req.formData()
  const transactionId = formData.get('cpm_trans_id') as string | null

  if (!transactionId) return NextResponse.json({ status: 'ok' })

  // CRITIQUE PAY-03 : Ne jamais lire le statut depuis le body — appeler /v2/payment/check
  const verification = await verifierPaiement(transactionId)
  const verified     = verification.data

  const statutPaiement = verified?.status === 'ACCEPTED' ? 'succes' : 'echec'
  const methode        = canalVersMethode(verified?.payment_method ?? '')

  // Utiliser SUPABASE_SERVICE_ROLE_KEY — webhook est unauthentifie, RLS bloquerait anon
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: paiement } = await (supabase.from('paiements') as any)
    .update({
      statut:   statutPaiement,
      methode:  methode,
      metadata: verification,
    })
    .eq('cinetpay_transaction_id', transactionId)
    .select('reservation_id')
    .single()

  // Si succes : confirmer la reservation liee
  if (statutPaiement === 'succes' && paiement?.reservation_id) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    await (supabase.from('reservations') as any)
      .update({ statut: 'confirmee' })
      .eq('id', paiement.reservation_id)
  }

  // DOIT retourner HTTP 200 — sinon CinetPay retente le webhook indefiniment
  return NextResponse.json({ status: 'ok' })
}
```

**Creer apps/web/app/(client)/paiement/retour/page.tsx (PAY-06) :**
```typescript
import Link from 'next/link'

interface Props {
  searchParams: { cpm_trans_id?: string; cpm_error_message?: string }
}

export default function PaiementRetourPage({ searchParams }: Props) {
  const isSuccess = !searchParams.cpm_error_message ||
                    searchParams.cpm_error_message === ''
  return (
    <div className="min-h-screen bg-surface flex items-center justify-center p-4">
      <div className="bg-surface-card rounded-card p-8 max-w-md w-full text-center shadow-sm">
        {isSuccess ? (
          <>
            <div className="text-5xl mb-4">&#10003;</div>
            <h1 className="font-display text-2xl text-primary mb-2">
              Paiement confirme !
            </h1>
            <p className="text-muted mb-6">
              Votre reservation est confirmee. Un contrat sera genere automatiquement.
            </p>
          </>
        ) : (
          <>
            <div className="text-5xl mb-4">&#10007;</div>
            <h1 className="font-display text-2xl text-danger mb-2">
              Paiement non abouti
            </h1>
            <p className="text-muted mb-2">
              {searchParams.cpm_error_message ?? 'La transaction a ete annulee ou refusee.'}
            </p>
            <p className="text-sm text-muted mb-6">
              Ref. transaction : {searchParams.cpm_trans_id ?? '—'}
            </p>
          </>
        )}
        <Link
          href="/"
          className="inline-block bg-primary text-white px-6 py-2 rounded-btn text-sm font-medium hover:opacity-90"
        >
          Retour a l&apos;accueil
        </Link>
      </div>
    </div>
  )
}
```

**Creer apps/web/components/paiements/PaiementButton.tsx (PAY-01) :**
```typescript
'use client'
import { useState } from 'react'

interface Props {
  reservationId: string
  montantFcfa:   number
  description:   string
  className?:    string
}

export function PaiementButton({ reservationId, montantFcfa, description, className }: Props) {
  const [loading, setLoading] = useState(false)
  const [error,   setError]   = useState<string | null>(null)

  async function handlePay() {
    setLoading(true)
    setError(null)
    try {
      const res = await fetch('/api/paiements/initier', {
        method:  'POST',
        headers: { 'Content-Type': 'application/json' },
        body:    JSON.stringify({ reservationId, montantFcfa, description }),
      })
      const data = await res.json()
      if (!res.ok) {
        setError(data.error ?? 'Erreur lors de l\'initiation du paiement')
        return
      }
      window.location.href = data.paymentUrl
    } catch {
      setError('Impossible de contacter le serveur de paiement')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div>
      <button
        onClick={handlePay}
        disabled={loading}
        className={`bg-secondary text-white px-6 py-3 rounded-btn font-medium hover:opacity-90 disabled:opacity-50 transition-opacity ${className ?? ''}`}
      >
        {loading ? 'Redirection...' : `Payer ${montantFcfa.toLocaleString('fr-FR')} FCFA`}
      </button>
      {error && <p className="text-danger text-sm mt-2">{error}</p>}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -l "verifierPaiement" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/paiements/webhook/route.ts" && grep -l "SUPABASE_SERVICE_ROLE_KEY" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/paiements/webhook/route.ts" && grep -l "paymentUrl" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/paiements/initier/route.ts" && ls "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(client)/paiement/retour/page.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/paiements/PaiementButton.tsx" && echo "ROUTES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/paiements/initier/route.ts contient "arrondir5" et "calculerSplit" et "cinetpay_transaction_id"
    - app/api/paiements/webhook/route.ts contient "verifierPaiement" (appel /v2/payment/check)
    - app/api/paiements/webhook/route.ts contient "SUPABASE_SERVICE_ROLE_KEY" (bypass RLS)
    - app/api/paiements/webhook/route.ts retourne NextResponse.json({ status: 'ok' }) dans tous les cas
    - app/(client)/paiement/retour/page.tsx contient searchParams et lit cpm_error_message
    - components/paiements/PaiementButton.tsx contient "use client" et window.location.href
    - Le flux complet est: POST /api/paiements/initier -> paymentUrl -> webhook -> verifierPaiement -> update paiements
  </acceptance_criteria>
  <done>Routes PAY-02/03/04/05/06 operationnelles. Le webhook appelle /v2/payment/check avant toute mise a jour BDD. La page retour affiche succes/echec selon searchParams CinetPay.</done>
</task>

</tasks>

<verification>
- grep "verifierPaiement" apps/web/app/api/paiements/webhook/route.ts
- grep "SUPABASE_SERVICE_ROLE_KEY" apps/web/app/api/paiements/webhook/route.ts
- grep "arrondir5\|calculerSplit" apps/web/app/api/paiements/initier/route.ts
- ls apps/web/node_modules/{@anthropic-ai/sdk,recharts,@tremor/react,to-words,@react-pdf/renderer}/package.json
- npx tsc --noEmit depuis apps/web (aucune erreur TypeScript)
</verification>

<success_criteria>
1. POST /api/paiements/initier avec { reservationId, montantFcfa, description } retourne { paymentUrl }
2. POST /api/paiements/webhook lit cpm_trans_id, appelle verifierPaiement(), met a jour paiements.statut
3. Paiement ACCEPTED -> paiements.statut = 'succes', reservations.statut = 'confirmee'
4. Paiement REFUSED -> paiements.statut = 'echec'
5. Webhook retourne toujours HTTP 200
6. commission_fcfa + montant_net_fcfa calcules et stockes (split 10%/90%)
7. Page /paiement/retour affiche succes ou echec selon searchParams
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-paiements-r-servations-ia-dashboard/03-01-SUMMARY.md`
</output>
