---
phase: 03-paiements-r-servations-ia-dashboard
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - apps/web/tailwind.config.ts
  - apps/web/components/dashboard/KPICard.tsx
  - apps/web/components/dashboard/RevenueBarChart.tsx
  - apps/web/components/dashboard/OccupancyGauge.tsx
  - apps/web/components/dashboard/PaymentDonut.tsx
  - apps/web/components/dashboard/ConversionFunnel.tsx
  - apps/web/components/dashboard/AlertesSection.tsx
  - apps/web/app/(pro)/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06]

must_haves:
  truths:
    - "Le dashboard charge avec des donnees reelles Supabase en moins de 2 secondes"
    - "4 KPI cards affichent revenus du mois, taux occupation, reservations en attente, messages non lus"
    - "Le bar chart revenus 12 mois affiche les mois precedents et le mois actuel"
    - "La section alertes est triee par priorite : rouge urgent, orange attention, jaune info"
    - "Tous les montants sont en FCFA formates avec separateur milliers"
  artifacts:
    - path: "apps/web/tailwind.config.ts"
      provides: "Path Tremor dans content array — evite le purge CSS Tremor"
      contains: "node_modules/@tremor"
    - path: "apps/web/components/dashboard/KPICard.tsx"
      provides: "Carte KPI avec Tremor Card — use client"
      exports: ["KPICard"]
    - path: "apps/web/components/dashboard/RevenueBarChart.tsx"
      provides: "Bar chart Recharts revenus 12 mois — use client"
      exports: ["RevenueBarChart"]
    - path: "apps/web/components/dashboard/OccupancyGauge.tsx"
      provides: "Gauge taux occupation Tremor ProgressBar — use client"
      exports: ["OccupancyGauge"]
    - path: "apps/web/components/dashboard/PaymentDonut.tsx"
      provides: "Donut repartition paiements Recharts PieChart — use client"
      exports: ["PaymentDonut"]
    - path: "apps/web/components/dashboard/ConversionFunnel.tsx"
      provides: "Funnel conversion Recharts FunnelChart — use client"
      exports: ["ConversionFunnel"]
    - path: "apps/web/components/dashboard/AlertesSection.tsx"
      provides: "Alertes triees par priorite (pure React)"
      exports: ["AlertesSection"]
    - path: "apps/web/app/(pro)/dashboard/page.tsx"
      provides: "Server Component — fetch Supabase + props vers composants client"
  key_links:
    - from: "app/(pro)/dashboard/page.tsx"
      to: "supabase.from('paiements') + reservations + messages"
      via: "Server Component fetch — donnees passees en props"
      pattern: "from.*paiements\|from.*reservations\|from.*messages"
    - from: "tailwind.config.ts"
      to: "node_modules/@tremor"
      via: "content array — requis pour que Tremor soit style en production"
      pattern: "tremor"
    - from: "RevenueBarChart.tsx"
      to: "recharts BarChart"
      via: "use client + ResponsiveContainer"
      pattern: "BarChart\|ResponsiveContainer"
---

<objective>
Dashboard analytics proprietaire complet : KPI cards Tremor (DASH-01), bar chart revenus Recharts (DASH-02), gauge occupation Tremor ProgressBar (DASH-03), donut paiements Recharts (DASH-04), alertes prioritaires (DASH-05), funnel conversion Recharts (DASH-06). Architecture Server Component + Client Components.

Purpose: Donner aux proprietaires une vue 360° de leur activite — revenus, occupation, alertes urgentes — en un seul ecran charge en moins de 2s.
Output: tailwind.config.ts patche + 6 composants dashboard + page Server Component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md

@apps/web/lib/supabase/server.ts
@apps/web/tailwind.config.ts

<interfaces>
<!-- CRITIQUE : Tremor necessite son path dans tailwind.config.ts content array -->
<!-- Pitfall 3 dans RESEARCH.md : sans ca, Tremor est unstyled en prod -->
```typescript
// tailwind.config.ts — AJOUTER dans content:
"./node_modules/@tremor/**/*.{js,ts,jsx,tsx}"
```

<!-- Tremor v3.18.7 : UNIQUEMENT KPI cards (Card, Metric) et ProgressBar -->
<!-- Tremor v3.18.7 N'A PAS de charts (Charts ont ete retires en v3) -->
<!-- Pour les charts : utiliser recharts UNIQUEMENT -->
```typescript
import { Card, Metric, Text, BadgeDelta } from '@tremor/react'  // KPI cards OK
import { ProgressBar }                    from '@tremor/react'  // Gauge OK
// PAS d'import de BarChart, PieChart depuis @tremor/react
```

<!-- Recharts v3.8.1 — tous les charts -->
```typescript
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'
import { PieChart, Pie, Cell, Legend }                                from 'recharts'
import { FunnelChart, Funnel, LabelList, Tooltip as RTooltip }       from 'recharts'
```

<!-- Recharts Pitfall 5 : SSR hydration mismatch -->
<!-- Solution : dynamic import avec ssr:false pour tous les charts recharts -->
```typescript
import dynamic from 'next/dynamic'
const RevenueBarChart = dynamic(() => import('./RevenueBarChart'), { ssr: false })
```

<!-- Table paiements (depuis Plan 03-01) — colonnes pour dashboard -->
```
statut text, montant_net_fcfa numeric, methode text,
created_at timestamptz, proprietaire_id via reservations.biens.proprietaire_id
```

<!-- Table reservations (depuis Plan 03-02) -->
```
statut text (en_attente/confirmee/annulee), created_at timestamptz, bien_id uuid
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patcher tailwind.config.ts et creer les 6 composants dashboard</name>
  <files>
    apps/web/tailwind.config.ts
    apps/web/components/dashboard/KPICard.tsx
    apps/web/components/dashboard/RevenueBarChart.tsx
    apps/web/components/dashboard/OccupancyGauge.tsx
    apps/web/components/dashboard/PaymentDonut.tsx
    apps/web/components/dashboard/ConversionFunnel.tsx
    apps/web/components/dashboard/AlertesSection.tsx
  </files>
  <read_first>
    - apps/web/tailwind.config.ts (LIRE AVANT — ajouter le path Tremor dans content[])
    - .planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md (Pattern 5 + Pitfall 3 + Pitfall 5)
  </read_first>
  <action>
**Etape 1 — Patcher apps/web/tailwind.config.ts (Pitfall 3 Tremor) :**
Lire le fichier existant, puis ajouter dans l'array content[] :
`"./node_modules/@tremor/**/*.{js,ts,jsx,tsx}"`

Le fichier tailwind.config.ts apres patch (content array mis a jour) :
```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './node_modules/@tremor/**/*.{js,ts,jsx,tsx}',  // DASH-01/03 Tremor styles
  ],
  // ... reste du fichier inchange (theme, plugins)
}
export default config
```

**Creer apps/web/components/dashboard/KPICard.tsx (DASH-01) :**
```typescript
'use client'
import { Card, Metric, Text, BadgeDelta } from '@tremor/react'

export interface KPICardProps {
  titre:       string
  valeur:      string      // Pre-formate (ex: "1 250 000 FCFA", "73%", "3")
  sous_titre?: string
  variation?:  number      // % changement vs periode precedente (positif = hausse)
  alerte?:     boolean     // Afficher badge rouge si true
}

export function KPICard({ titre, valeur, sous_titre, variation, alerte }: KPICardProps) {
  const deltaType = variation === undefined ? undefined
    : variation > 0  ? 'increase'
    : variation < 0  ? 'decrease'
    : 'unchanged'

  return (
    <Card className="rounded-card shadow-sm">
      <Text className="text-muted text-sm">{titre}</Text>
      <Metric className={`font-mono mt-1 ${alerte ? 'text-danger' : 'text-primary'}`}>
        {valeur}
        {alerte && <span className="ml-2 text-xs bg-danger text-white px-2 py-0.5 rounded-pill font-sans">!</span>}
      </Metric>
      {(sous_titre || variation !== undefined) && (
        <div className="flex items-center gap-2 mt-2">
          {sous_titre && <Text className="text-muted text-xs">{sous_titre}</Text>}
          {deltaType && variation !== undefined && (
            <BadgeDelta deltaType={deltaType} size="xs">
              {Math.abs(variation).toFixed(1)}%
            </BadgeDelta>
          )}
        </div>
      )}
    </Card>
  )
}
```

**Creer apps/web/components/dashboard/RevenueBarChart.tsx (DASH-02) :**
```typescript
'use client'
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts'

export interface RevenueData {
  mois:  string   // ex: "Jan", "Fev"
  total: number   // FCFA
}

export function RevenueBarChart({ data }: { data: RevenueData[] }) {
  return (
    <div className="bg-surface-card rounded-card p-4 shadow-sm">
      <h3 className="text-sm font-medium text-text mb-4">Revenus des 12 derniers mois</h3>
      <ResponsiveContainer width="100%" height={240}>
        <BarChart data={data} margin={{ top: 4, right: 8, left: 8, bottom: 0 }}>
          <CartesianGrid strokeDasharray="3 3" stroke="var(--border)" />
          <XAxis
            dataKey="mois"
            tick={{ fontSize: 11, fill: 'var(--text-muted)' }}
            axisLine={false}
            tickLine={false}
          />
          <YAxis
            tickFormatter={v => `${(v / 1000).toFixed(0)}k`}
            tick={{ fontSize: 11, fill: 'var(--text-muted)' }}
            axisLine={false}
            tickLine={false}
          />
          <Tooltip
            formatter={(v: number) => [`${v.toLocaleString('fr-FR')} FCFA`, 'Revenus']}
            contentStyle={{ borderRadius: '8px', border: '1px solid var(--border)', fontSize: 12 }}
          />
          <Bar dataKey="total" fill="var(--primary)" radius={[4, 4, 0, 0]} />
        </BarChart>
      </ResponsiveContainer>
    </div>
  )
}
```

**Creer apps/web/components/dashboard/OccupancyGauge.tsx (DASH-03 — Tremor ProgressBar) :**
```typescript
'use client'
import { ProgressBar, Text } from '@tremor/react'

export interface OccupancyData {
  label:       string   // ex: "Villa Cocody"
  taux:        number   // 0-100
  statut:      string   // ex: "Loue" | "Disponible"
}

export function OccupancyGauge({ biens }: { biens: OccupancyData[] }) {
  const tauxGlobal = biens.length > 0
    ? Math.round(biens.filter(b => b.taux === 100).length / biens.length * 100)
    : 0

  return (
    <div className="bg-surface-card rounded-card p-4 shadow-sm">
      <h3 className="text-sm font-medium text-text mb-1">Taux d&apos;occupation</h3>
      <p className="font-mono text-2xl font-bold text-primary mb-3">{tauxGlobal}%</p>
      <div className="space-y-3">
        {biens.map((b, i) => (
          <div key={i}>
            <div className="flex justify-between mb-1">
              <Text className="text-xs text-text truncate max-w-[60%]">{b.label}</Text>
              <Text className="text-xs text-muted">{b.statut}</Text>
            </div>
            <ProgressBar
              value={b.taux}
              color={b.taux === 100 ? 'emerald' : 'blue'}
              className="h-2"
            />
          </div>
        ))}
        {biens.length === 0 && (
          <Text className="text-muted text-sm">Aucun bien actif</Text>
        )}
      </div>
    </div>
  )
}
```

**Creer apps/web/components/dashboard/PaymentDonut.tsx (DASH-04 — Recharts PieChart) :**
```typescript
'use client'
import { PieChart, Pie, Cell, Legend, Tooltip, ResponsiveContainer } from 'recharts'

export interface PaymentMethodData {
  methode: string   // 'wave' | 'orange_money' | 'mtn' | 'moov' | 'carte_bancaire'
  total:   number
  count:   number
}

const METHODE_LABELS: Record<string, string> = {
  wave:           'Wave',
  orange_money:   'Orange Money',
  mtn:            'MTN',
  moov:           'Moov',
  carte_bancaire: 'Carte bancaire',
}

const COULEURS = ['#1A5276', '#E67E22', '#27AE60', '#8E44AD', '#E74C3C']

export function PaymentDonut({ data }: { data: PaymentMethodData[] }) {
  const chartData = data.map(d => ({
    name:  METHODE_LABELS[d.methode] ?? d.methode,
    value: d.total,
  }))

  return (
    <div className="bg-surface-card rounded-card p-4 shadow-sm">
      <h3 className="text-sm font-medium text-text mb-4">Repartition des paiements</h3>
      {chartData.length === 0 ? (
        <p className="text-muted text-sm py-8 text-center">Aucun paiement ce mois</p>
      ) : (
        <ResponsiveContainer width="100%" height={220}>
          <PieChart>
            <Pie
              data={chartData}
              cx="50%"
              cy="50%"
              innerRadius={55}
              outerRadius={80}
              paddingAngle={3}
              dataKey="value"
            >
              {chartData.map((_entry, i) => (
                <Cell key={i} fill={COULEURS[i % COULEURS.length]} />
              ))}
            </Pie>
            <Tooltip
              formatter={(v: number) => `${v.toLocaleString('fr-FR')} FCFA`}
              contentStyle={{ borderRadius: '8px', fontSize: 12 }}
            />
            <Legend
              iconType="circle"
              iconSize={8}
              wrapperStyle={{ fontSize: 11 }}
            />
          </PieChart>
        </ResponsiveContainer>
      )}
    </div>
  )
}
```

**Creer apps/web/components/dashboard/ConversionFunnel.tsx (DASH-06 — Recharts FunnelChart) :**
```typescript
'use client'
import { FunnelChart, Funnel, LabelList, Tooltip, ResponsiveContainer } from 'recharts'

export interface FunnelData {
  vues:         number
  contacts:     number
  visites:      number
  reservations: number
  signatures:   number
}

const FUNNEL_COLORS = ['#1A5276', '#2980B9', '#3498DB', '#E67E22', '#27AE60']

export function ConversionFunnel({ data }: { data: FunnelData }) {
  const chartData = [
    { name: 'Vues',         value: data.vues },
    { name: 'Contacts',     value: data.contacts },
    { name: 'Visites',      value: data.visites },
    { name: 'Reservations', value: data.reservations },
    { name: 'Signatures',   value: data.signatures },
  ]

  return (
    <div className="bg-surface-card rounded-card p-4 shadow-sm">
      <h3 className="text-sm font-medium text-text mb-4">Funnel de conversion</h3>
      <ResponsiveContainer width="100%" height={220}>
        <FunnelChart>
          <Tooltip
            formatter={(v: number) => [v.toLocaleString('fr-FR'), '']}
            contentStyle={{ borderRadius: '8px', fontSize: 12 }}
          />
          <Funnel dataKey="value" data={chartData} isAnimationActive>
            {chartData.map((_entry, i) => (
              <Cell key={i} fill={FUNNEL_COLORS[i % FUNNEL_COLORS.length]} />
            ))}
            <LabelList position="right" fill="#1C2833" stroke="none" dataKey="name" style={{ fontSize: 11 }} />
          </Funnel>
        </FunnelChart>
      </ResponsiveContainer>
    </div>
  )
}
```

**Creer apps/web/components/dashboard/AlertesSection.tsx (DASH-05) :**
```typescript
'use client'

export interface Alerte {
  id:        string
  type:      'urgent' | 'attention' | 'info'
  message:   string
  details?:  string
  lien?:     string
}

const TYPE_STYLES: Record<Alerte['type'], { badge: string; dot: string; label: string }> = {
  urgent:    { badge: 'bg-danger-light text-danger border-danger/20',  dot: 'bg-danger',  label: 'URGENT'    },
  attention: { badge: 'bg-warning/10 text-warning border-warning/20',  dot: 'bg-warning', label: 'ATTENTION' },
  info:      { badge: 'bg-primary-light text-primary border-primary/20', dot: 'bg-primary', label: 'INFO'    },
}

const TYPE_ORDER: Alerte['type'][] = ['urgent', 'attention', 'info']

export function AlertesSection({ alertes }: { alertes: Alerte[] }) {
  const sorted = [...alertes].sort(
    (a, b) => TYPE_ORDER.indexOf(a.type) - TYPE_ORDER.indexOf(b.type)
  )

  return (
    <div className="bg-surface-card rounded-card p-4 shadow-sm">
      <h3 className="text-sm font-medium text-text mb-3">
        Alertes
        {alertes.filter(a => a.type === 'urgent').length > 0 && (
          <span className="ml-2 bg-danger text-white text-xs px-2 py-0.5 rounded-pill">
            {alertes.filter(a => a.type === 'urgent').length}
          </span>
        )}
      </h3>

      {sorted.length === 0 ? (
        <p className="text-muted text-sm py-4 text-center">Aucune alerte</p>
      ) : (
        <ul className="space-y-2">
          {sorted.map(alerte => {
            const s = TYPE_STYLES[alerte.type]
            return (
              <li
                key={alerte.id}
                className={`flex items-start gap-3 p-3 rounded-btn border text-sm ${s.badge}`}
              >
                <span className={`w-2 h-2 rounded-full mt-1.5 flex-shrink-0 ${s.dot}`} />
                <div className="flex-1 min-w-0">
                  <span className="font-semibold text-xs mr-2">{s.label}</span>
                  {alerte.message}
                  {alerte.details && (
                    <p className="text-xs opacity-75 mt-0.5 truncate">{alerte.details}</p>
                  )}
                </div>
                {alerte.lien && (
                  <a href={alerte.lien} className="text-xs underline flex-shrink-0">
                    Voir
                  </a>
                )}
              </li>
            )
          })}
        </ul>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep "tremor" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/tailwind.config.ts" && grep "use client" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/KPICard.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/RevenueBarChart.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/PaymentDonut.tsx" && ls "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/OccupancyGauge.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/ConversionFunnel.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/dashboard/AlertesSection.tsx" && echo "DASHBOARD_COMPONENTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/tailwind.config.ts contient "@tremor" dans le tableau content[]
    - KPICard.tsx importe de "@tremor/react" (Card, Metric) et contient "use client"
    - RevenueBarChart.tsx importe de "recharts" (BarChart, Bar) et contient "use client"
    - OccupancyGauge.tsx importe ProgressBar de "@tremor/react" — PAS de recharts chart
    - PaymentDonut.tsx importe PieChart de "recharts" et contient "use client"
    - ConversionFunnel.tsx importe FunnelChart de "recharts" et contient "use client"
    - AlertesSection.tsx tri par TYPE_ORDER = ['urgent', 'attention', 'info'] (DASH-05)
    - Aucun composant dashboard n'importe de charts depuis "@tremor/react" (Tremor v3 n'a pas de charts)
  </acceptance_criteria>
  <done>tailwind.config.ts patche pour Tremor. 6 composants dashboard crees : KPICard (Tremor), RevenueBarChart (Recharts), OccupancyGauge (Tremor ProgressBar), PaymentDonut (Recharts), ConversionFunnel (Recharts), AlertesSection (React pur).</done>
</task>

<task type="auto">
  <name>Task 2: Page dashboard Server Component avec fetch Supabase et dynamic imports</name>
  <files>
    apps/web/app/(pro)/dashboard/page.tsx
  </files>
  <read_first>
    - apps/web/components/dashboard/KPICard.tsx (interface KPICardProps)
    - apps/web/components/dashboard/RevenueBarChart.tsx (interface RevenueData)
    - apps/web/components/dashboard/OccupancyGauge.tsx (interface OccupancyData)
    - apps/web/components/dashboard/PaymentDonut.tsx (interface PaymentMethodData)
    - apps/web/components/dashboard/ConversionFunnel.tsx (interface FunnelData)
    - apps/web/components/dashboard/AlertesSection.tsx (interface Alerte)
    - apps/web/lib/supabase/server.ts (createClient async)
  </read_first>
  <action>
Creer apps/web/app/(pro)/dashboard/page.tsx :

Architecture: Server Component qui fetch les donnees Supabase et passe en props aux Client Components.
Recharts est importe via dynamic() avec ssr:false pour eviter le Pitfall 5 (hydration mismatch).

```typescript
// app/(pro)/dashboard/page.tsx — Server Component (pas de 'use client')
import { createClient }    from '@/lib/supabase/server'
import { redirect }        from 'next/navigation'
import dynamic             from 'next/dynamic'
import { KPICard }         from '@/components/dashboard/KPICard'
import { OccupancyGauge }  from '@/components/dashboard/OccupancyGauge'
import { AlertesSection }  from '@/components/dashboard/AlertesSection'
import type { Alerte }     from '@/components/dashboard/AlertesSection'
import type { RevenueData }      from '@/components/dashboard/RevenueBarChart'
import type { PaymentMethodData } from '@/components/dashboard/PaymentDonut'
import type { FunnelData }        from '@/components/dashboard/ConversionFunnel'

// Dynamic imports avec ssr:false — evite le Pitfall 5 recharts hydration mismatch
const RevenueBarChart = dynamic(
  () => import('@/components/dashboard/RevenueBarChart').then(m => m.RevenueBarChart),
  { ssr: false }
)
const PaymentDonut = dynamic(
  () => import('@/components/dashboard/PaymentDonut').then(m => m.PaymentDonut),
  { ssr: false }
)
const ConversionFunnel = dynamic(
  () => import('@/components/dashboard/ConversionFunnel').then(m => m.ConversionFunnel),
  { ssr: false }
)

export const metadata = { title: 'Dashboard — Immo CI' }

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  // --- Fetch parallele pour minimiser le temps de chargement ---
  const now         = new Date()
  const debutMois   = new Date(now.getFullYear(), now.getMonth(), 1).toISOString()
  const debutAn12   = new Date(now.getFullYear(), now.getMonth() - 11, 1).toISOString()

  const [
    { data: biens },
    { data: paiementsMois },
    { data: paiements12 },
    { data: reservationsAttente },
    { data: messagesNonLus },
    { data: visitesSansReponse },
    { data: contratsExpirant },
    { data: analyticsVues },
  ] = await Promise.all([
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('biens') as any)
      .select('id, titre, statut')
      .eq('proprietaire_id', user.id)
      .in('statut', ['publie', 'loue']),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('paiements') as any)
      .select('montant_net_fcfa, methode, created_at')
      .eq('statut', 'succes')
      .gte('created_at', debutMois),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('paiements') as any)
      .select('montant_net_fcfa, methode, created_at')
      .eq('statut', 'succes')
      .gte('created_at', debutAn12),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('reservations') as any)
      .select('id')
      .in('bien_id', (biens ?? []).map((b: { id: string }) => b.id))
      .eq('statut', 'en_attente'),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('messages') as any)
      .select('id')
      .neq('expediteur_id', user.id)
      .eq('lu', false),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('visites') as any)
      .select('id, created_at')
      .in('bien_id', (biens ?? []).map((b: { id: string }) => b.id))
      .eq('statut', 'demandee'),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('contrats') as any)
      .select('id, date_fin')
      .eq('bailleur_id', user.id)
      .eq('statut', 'signe')
      .lte('date_fin', new Date(now.getTime() + 30 * 86400000).toISOString().split('T')[0]),

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (supabase.from('analytics_events') as any)
      .select('type, created_at')
      .eq('user_id', user.id)
      .gte('created_at', debutAn12),
  ])

  // --- KPI DASH-01 ---
  const revenusMois  = (paiementsMois ?? []).reduce((s: number, p: { montant_net_fcfa: number }) => s + Number(p.montant_net_fcfa), 0)
  const nbBiensActifs = (biens ?? []).length
  const nbBiensLoues  = (biens ?? []).filter((b: { statut: string }) => b.statut === 'loue').length
  const tauxOccupation = nbBiensActifs > 0 ? Math.round(nbBiensLoues / nbBiensActifs * 100) : 0

  // --- Bar chart revenus 12 mois DASH-02 ---
  const moisFr = ['Jan','Fev','Mar','Avr','Mai','Jun','Jul','Aou','Sep','Oct','Nov','Dec']
  const revenueByMonth: Record<string, number> = {}
  for (let i = 11; i >= 0; i--) {
    const d = new Date(now.getFullYear(), now.getMonth() - i, 1)
    revenueByMonth[`${moisFr[d.getMonth()]} ${d.getFullYear()}`] = 0
  }
  ;(paiements12 ?? []).forEach((p: { montant_net_fcfa: number; created_at: string }) => {
    const d    = new Date(p.created_at)
    const key  = `${moisFr[d.getMonth()]} ${d.getFullYear()}`
    if (key in revenueByMonth) revenueByMonth[key] += Number(p.montant_net_fcfa)
  })
  const revenueData: RevenueData[] = Object.entries(revenueByMonth).map(([mois, total]) => ({
    mois: mois.split(' ')[0],  // Afficher seulement "Jan", "Fev"...
    total,
  }))

  // --- Gauge occupation par bien DASH-03 ---
  const occupancyData = (biens ?? []).map((b: { titre: string; statut: string }) => ({
    label:  b.titre,
    taux:   b.statut === 'loue' ? 100 : 0,
    statut: b.statut === 'loue' ? 'Loue' : 'Disponible',
  }))

  // --- Donut paiements par methode DASH-04 ---
  const methodMap: Record<string, { total: number; count: number }> = {}
  ;(paiements12 ?? []).forEach((p: { methode: string; montant_net_fcfa: number }) => {
    if (!methodMap[p.methode]) methodMap[p.methode] = { total: 0, count: 0 }
    methodMap[p.methode].total += Number(p.montant_net_fcfa)
    methodMap[p.methode].count++
  })
  const paymentData: PaymentMethodData[] = Object.entries(methodMap).map(([methode, d]) => ({
    methode,
    total: d.total,
    count: d.count,
  }))

  // --- Funnel conversion DASH-06 ---
  const vues    = (analyticsVues ?? []).filter((e: { type: string }) => e.type === 'vue_bien').length
  const contacts = (analyticsVues ?? []).filter((e: { type: string }) => e.type === 'contact').length
  const funnelData: FunnelData = {
    vues:         vues    || 0,
    contacts:     contacts || 0,
    visites:      (visitesSansReponse ?? []).length + 0,  // approx
    reservations: (reservationsAttente ?? []).length,
    signatures:   (contratsExpirant ?? []).length,
  }

  // --- Alertes DASH-05 ---
  const alertes: Alerte[] = []

  // Visites sans reponse > 24h
  ;(visitesSansReponse ?? []).forEach((v: { id: string; created_at: string }) => {
    const heures = (Date.now() - new Date(v.created_at).getTime()) / 3600000
    if (heures > 24) {
      alertes.push({
        id:      `visite-${v.id}`,
        type:    'attention',
        message: 'Demande de visite sans reponse depuis plus de 24h',
        lien:    '/pro/visites',
      })
    }
  })

  // Contrats expirant dans 30 jours
  ;(contratsExpirant ?? []).forEach((c: { id: string; date_fin: string }) => {
    alertes.push({
      id:      `contrat-${c.id}`,
      type:    'attention',
      message: `Contrat expirant le ${c.date_fin}`,
      lien:    '/pro/contrats',
    })
  })

  // Reservations en attente
  if ((reservationsAttente ?? []).length > 0) {
    alertes.push({
      id:      'reservations-attente',
      type:    'urgent',
      message: `${(reservationsAttente ?? []).length} reservation(s) en attente de confirmation`,
      lien:    '/pro/reservations',
    })
  }

  return (
    <div className="min-h-screen bg-surface">
      <div className="max-w-7xl mx-auto px-4 py-8">
        <h1 className="font-display text-2xl text-primary mb-6">Dashboard</h1>

        {/* KPI Cards — DASH-01 */}
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
          <KPICard
            titre="Revenus du mois"
            valeur={`${revenusMois.toLocaleString('fr-FR')} FCFA`}
            sous_titre="Net apres commission"
          />
          <KPICard
            titre="Taux d'occupation"
            valeur={`${tauxOccupation}%`}
            sous_titre={`${nbBiensLoues}/${nbBiensActifs} biens loues`}
          />
          <KPICard
            titre="Reservations en attente"
            valeur={String((reservationsAttente ?? []).length)}
            alerte={(reservationsAttente ?? []).length > 0}
          />
          <KPICard
            titre="Messages non lus"
            valeur={String((messagesNonLus ?? []).length)}
            alerte={(messagesNonLus ?? []).length > 0}
          />
        </div>

        {/* Alertes DASH-05 */}
        {alertes.length > 0 && (
          <div className="mb-6">
            <AlertesSection alertes={alertes} />
          </div>
        )}

        {/* Graphiques — 2 colonnes sur desktop */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
          {/* Bar chart revenus DASH-02 */}
          <RevenueBarChart data={revenueData} />

          {/* Gauge occupation DASH-03 */}
          <OccupancyGauge biens={occupancyData} />
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
          {/* Donut paiements DASH-04 */}
          <PaymentDonut data={paymentData} />

          {/* Funnel conversion DASH-06 */}
          <ConversionFunnel data={funnelData} />
        </div>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep "dynamic\|ssr.*false" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(pro)/dashboard/page.tsx" && grep "Promise.all" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(pro)/dashboard/page.tsx" && grep "KPICard\|RevenueBarChart\|OccupancyGauge\|PaymentDonut\|ConversionFunnel\|AlertesSection" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(pro)/dashboard/page.tsx" | wc -l && echo "DASHBOARD_PAGE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/(pro)/dashboard/page.tsx est un Server Component (PAS de "use client")
    - La page importe RevenueBarChart, PaymentDonut, ConversionFunnel via dynamic() avec ssr: false
    - La page utilise Promise.all pour les fetches Supabase en parallele (performance)
    - Les 4 KPICard sont rendus avec : revenus mois, taux occupation, reservations attente, messages non lus
    - OccupancyGauge recoit un tableau de biens avec taux et statut
    - AlertesSection est conditionnelle (affichee seulement si alertes.length > 0)
    - Tous les montants sont formattes avec toLocaleString('fr-FR') pour les separateurs milliers
  </acceptance_criteria>
  <done>Page dashboard Server Component avec fetch Supabase parallele. 6 composants dashboard integres avec dynamic imports ssr:false pour Recharts. KPIs, graphiques et alertes tous presents.</done>
</task>

</tasks>

<verification>
- grep "@tremor" apps/web/tailwind.config.ts
- grep "ssr.*false\|dynamic" apps/web/app/(pro)/dashboard/page.tsx
- grep "Promise.all" apps/web/app/(pro)/dashboard/page.tsx
- grep "use client" apps/web/components/dashboard/KPICard.tsx apps/web/components/dashboard/RevenueBarChart.tsx
- grep "ProgressBar" apps/web/components/dashboard/OccupancyGauge.tsx (Tremor — pas recharts)
- npx tsc --noEmit depuis apps/web (aucune erreur TypeScript)
</verification>

<success_criteria>
1. Page /pro/dashboard se charge avec donnees reelles Supabase (pas de mock)
2. 4 KPI cards affichent des valeurs FCFA formatees avec separateurs
3. Bar chart revenus affiche 12 mois avec aucune hydration error recharts
4. OccupancyGauge affiche ProgressBar Tremor par bien (pas un recharts chart)
5. Donut et Funnel Recharts s'affichent correctement cote client (dynamic import)
6. Alertes triees urgent > attention > info
7. Dashboard charge en moins de 2s (Promise.all fetch parallele)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-paiements-r-servations-ia-dashboard/03-05-SUMMARY.md`
</output>
