---
phase: 03-paiements-r-servations-ia-dashboard
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - apps/web/next.config.ts
  - apps/web/lib/contrat-pdf.tsx
  - apps/web/app/api/contrats/generer/route.ts
  - apps/web/app/api/contrats/[id]/route.ts
autonomous: true
requirements: [RESA-03, RESA-04, RESA-05, RESA-06]

must_haves:
  truths:
    - "POST /api/contrats/generer produit un PDF OHADA valide et le stocke dans Supabase Storage bucket contrats"
    - "Le montant du loyer apparait en chiffres ET en lettres dans le contrat"
    - "Le PDF est accessible via une URL signee stockee dans contrats.pdf_url"
    - "Le contrat contient toutes les clauses obligatoires OHADA (identite parties, bien, loyer, depot garantie, resiliation)"
    - "L'application Next.js demarre sans crash malgre react-pdf (serverExternalPackages corrige)"
  artifacts:
    - path: "apps/web/next.config.ts"
      provides: "serverExternalPackages: ['@react-pdf/renderer'] — evite le crash ba.Component"
      contains: "serverExternalPackages"
    - path: "apps/web/lib/contrat-pdf.tsx"
      provides: "ContratDocument (react-pdf) + montantEnLettres() via to-words fr-FR"
      exports: ["ContratDocument", "montantEnLettres", "ContratProps"]
    - path: "apps/web/app/api/contrats/generer/route.ts"
      provides: "POST /api/contrats/generer — renderToBuffer + Supabase Storage upload"
    - path: "apps/web/app/api/contrats/[id]/route.ts"
      provides: "GET /api/contrats/[id] — URL signee depuis Supabase Storage"
  key_links:
    - from: "app/api/contrats/generer/route.ts"
      to: "supabase.storage.from('contrats').upload()"
      via: "service role key — bucket a creer si inexistant"
      pattern: "storage.from.*contrats"
    - from: "app/api/contrats/generer/route.ts"
      to: "ContratDocument"
      via: "renderToBuffer(<ContratDocument .../>)"
      pattern: "renderToBuffer"
    - from: "lib/contrat-pdf.tsx"
      to: "to-words ToWords fr-FR"
      via: "currency: false + suffix manuel francs CFA"
      pattern: "ToWords.*fr-FR\|currency.*false"
---

<objective>
Generation de contrats PDF OHADA complets : correction next.config.ts (serverExternalPackages), composant react-pdf avec toutes les clauses OHADA, montants FCFA en lettres (to-words fr-FR), upload Supabase Storage bucket contrats, et route GET pour URL signee.

Purpose: Delivrer un contrat de bail conforme au droit ivoirien immediatement apres confirmation de paiement — element differenciateur cle par rapport a Immo+.
Output: next.config.ts patche + lib/contrat-pdf.tsx + 2 routes API contrats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md

@apps/web/next.config.ts
@apps/web/lib/supabase/server.ts

<interfaces>
<!-- CRITIQUE : next.config.ts actuel (DOIT etre patche en Task 1) -->
```typescript
// apps/web/next.config.ts — etat ACTUEL (sans serverExternalPackages)
const nextConfig: NextConfig = {
  transpilePackages: ['@immo-ci/shared'],
  images: { remotePatterns: [...] },
}
// AJOUTER : serverExternalPackages: ['@react-pdf/renderer']
// Sans ca : TypeError: ba.Component is not a constructor au runtime
```

<!-- Table contrats (migration 005) — colonnes cles -->
```
id uuid, reservation_id uuid, bien_id uuid,
bailleur_id uuid, preneur_id uuid,
date_debut date, date_fin date,
loyer_mois_fcfa numeric, charges_mois_fcfa numeric,
depot_garantie_fcfa numeric,
statut text (brouillon/signe/expire/resilie),
pdf_url text,
created_at timestamptz
```

<!-- to-words usage CORRECT (currency:false) vs MAUVAIS (currency:true -> "euros") -->
```typescript
import { ToWords } from 'to-words'
const toWords = new ToWords({ localeCode: 'fr-FR', converterOptions: { currency: false } })
// CORRECT  : `${toWords.convert(500000)} francs CFA` -> "cinq cent mille francs CFA"
// INCORRECT: toWords.convert(500000, { currency: true }) -> "cinq cent mille euros" (FAUX)
```

<!-- Supabase Storage bucket contrats — PAS cree en Phase 1 -->
```typescript
// Le bucket 'contrats' n'existe PAS encore — le creer via Storage API avant upload
const { error: bucketError } = await supabase.storage.createBucket('contrats', {
  public: false,   // Acces controle par URL signee
})
// Ignorer l'erreur si le bucket existe deja (code 'Duplicate')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patcher next.config.ts et creer lib/contrat-pdf.tsx</name>
  <files>
    apps/web/next.config.ts
    apps/web/lib/contrat-pdf.tsx
  </files>
  <read_first>
    - apps/web/next.config.ts (LIRE AVANT de modifier — contenu actuel sans serverExternalPackages)
    - .planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md (Pitfall 2 + Pattern 6)
  </read_first>
  <action>
**Etape 1 — Patcher apps/web/next.config.ts (RESA-03 fix critique) :**

Modifier next.config.ts pour ajouter serverExternalPackages. Le fichier actuel est :
```typescript
import type { NextConfig } from 'next'
const nextConfig: NextConfig = {
  transpilePackages: ['@immo-ci/shared'],
  images: { remotePatterns: [...] },
}
export default nextConfig
```

Remplacer par :
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  transpilePackages: ['@immo-ci/shared'],
  // RESA-03 FIX: Evite le crash "ba.Component is not a constructor" de react-pdf
  // dans les App Router route handlers. Sans ceci, next build reussit mais la route
  // crashe a la premiere requete.
  serverExternalPackages: ['@react-pdf/renderer'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'res.cloudinary.com' },
      { protocol: 'https', hostname: '*.supabase.co' },
    ],
  },
}

export default nextConfig
```

**Etape 2 — Creer apps/web/lib/contrat-pdf.tsx (RESA-04, RESA-05) :**
```tsx
// lib/contrat-pdf.tsx
// Contrat de bail OHADA — @react-pdf/renderer
// IMPORTANT: Ce fichier est importe uniquement depuis des routes serveur
// (serverExternalPackages garantit le bon module resolution)

import { Document, Page, Text, View, StyleSheet } from '@react-pdf/renderer'
import { ToWords } from 'to-words'

// RESA-05 : currency:false obligatoire — currency:true genere "euros" (mauvais)
const toWords = new ToWords({
  localeCode:       'fr-FR',
  converterOptions: { currency: false },
})

/** Convertit un montant entier FCFA en lettres francaises */
export function montantEnLettres(montant: number): string {
  return `${toWords.convert(Math.round(montant))} francs CFA`
  // Ex: 500000 -> "cinq cent mille francs CFA"
}

export interface ContratProps {
  contratId:    string
  reservationId: string
  dateDebut:    string
  dateFin:      string
  // Bailleur
  bailleurNom:  string
  bailleurCni:  string
  bailleurTel:  string
  // Preneur
  preneurNom:   string
  preneurCni:   string
  preneurTel:   string
  // Bien
  bienAdresse:  string
  bienCommune:  string
  surfaceM2:    number
  nbPieces:     number
  // Financier
  loyerMoisFcfa:       number
  chargesMoisFcfa:     number
  depotGarantieFcfa:   number
  // Date de signature
  dateSignature: string
  lieuSignature: string
}

const S = StyleSheet.create({
  page:       { padding: 50, fontFamily: 'Helvetica', fontSize: 10, color: '#1C2833' },
  titre:      { fontSize: 14, fontWeight: 'bold', textAlign: 'center', marginBottom: 6 },
  sousTitre:  { fontSize: 10, textAlign: 'center', marginBottom: 20, color: '#7F8C8D' },
  section:    { marginBottom: 14 },
  sectionH:   { fontSize: 11, fontWeight: 'bold', marginBottom: 6, borderBottomWidth: 1, borderBottomColor: '#E5E8EC', paddingBottom: 3 },
  ligne:      { marginBottom: 4, lineHeight: 1.5 },
  gras:       { fontFamily: 'Helvetica-Bold' },
  montant:    { fontFamily: 'Helvetica-Bold', fontSize: 11 },
  montantLet: { fontStyle: 'italic', color: '#1A5276' },
  footer:     { position: 'absolute', bottom: 30, left: 50, right: 50, textAlign: 'center', fontSize: 8, color: '#7F8C8D' },
  signatures: { marginTop: 30, flexDirection: 'row', justifyContent: 'space-between' },
  sigBox:     { width: '45%', borderTopWidth: 1, borderTopColor: '#1C2833', paddingTop: 8 },
})

export function ContratDocument(props: ContratProps) {
  const {
    bailleurNom, bailleurCni, bailleurTel,
    preneurNom,  preneurCni,  preneurTel,
    bienAdresse, bienCommune, surfaceM2, nbPieces,
    loyerMoisFcfa, chargesMoisFcfa, depotGarantieFcfa,
    dateDebut, dateFin, dateSignature, lieuSignature,
  } = props

  return (
    <Document>
      <Page size="A4" style={S.page}>
        {/* En-tete */}
        <Text style={S.titre}>CONTRAT DE BAIL D&apos;HABITATION</Text>
        <Text style={S.sousTitre}>
          Regi par la loi ivoirienne et les dispositions de l&apos;Acte Uniforme OHADA
        </Text>

        {/* Identite des parties — RESA-04 */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 1 — PARTIES CONTRACTANTES</Text>
          <Text style={S.ligne}>
            <Text style={S.gras}>BAILLEUR : </Text>{bailleurNom} — CNI n° {bailleurCni} — Tel : {bailleurTel}
          </Text>
          <Text style={S.ligne}>
            <Text style={S.gras}>PRENEUR : </Text>{preneurNom} — CNI n° {preneurCni} — Tel : {preneurTel}
          </Text>
        </View>

        {/* Description du bien — RESA-04 */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 2 — DESIGNATION DU BIEN LOUE</Text>
          <Text style={S.ligne}>
            Appartement / Villa situe(e) a : <Text style={S.gras}>{bienAdresse}</Text>
          </Text>
          <Text style={S.ligne}>Commune : {bienCommune}</Text>
          <Text style={S.ligne}>Surface : {surfaceM2} m² — {nbPieces} piece(s)</Text>
        </View>

        {/* Duree du bail — RESA-04 */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 3 — DUREE DU BAIL</Text>
          <Text style={S.ligne}>Le present contrat prend effet le <Text style={S.gras}>{dateDebut}</Text></Text>
          <Text style={S.ligne}>et se termine le <Text style={S.gras}>{dateFin}</Text></Text>
        </View>

        {/* Loyer en chiffres ET en lettres — RESA-05 CRITIQUE */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 4 — LOYER ET CHARGES</Text>
          <Text style={S.montant}>
            Loyer mensuel : {loyerMoisFcfa.toLocaleString('fr-FR')} FCFA
          </Text>
          <Text style={S.montantLet}>
            Soit : {montantEnLettres(loyerMoisFcfa)}
          </Text>
          {chargesMoisFcfa > 0 && (
            <>
              <Text style={[S.ligne, { marginTop: 4 }]}>
                Charges mensuelles : {chargesMoisFcfa.toLocaleString('fr-FR')} FCFA
              </Text>
              <Text style={S.montantLet}>Soit : {montantEnLettres(chargesMoisFcfa)}</Text>
            </>
          )}
        </View>

        {/* Depot de garantie — RESA-04 */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 5 — DEPOT DE GARANTIE</Text>
          <Text style={S.montant}>
            Montant : {depotGarantieFcfa.toLocaleString('fr-FR')} FCFA
          </Text>
          <Text style={S.montantLet}>Soit : {montantEnLettres(depotGarantieFcfa)}</Text>
          <Text style={[S.ligne, { marginTop: 4 }]}>
            Le depot de garantie sera restitue dans un delai de 30 jours apres la remise des cles,
            deduction faite des sommes dues au titre de deteriorations eventuelles.
          </Text>
        </View>

        {/* Clauses resolutoires — RESA-04 */}
        <View style={S.section}>
          <Text style={S.sectionH}>ARTICLE 6 — RESILIATION ET CONGES</Text>
          <Text style={S.ligne}>
            Le present contrat pourra etre resilie par l&apos;une ou l&apos;autre des parties,
            moyennant un preavis d&apos;un (1) mois notifie par ecrit.
            En cas de non-paiement du loyer ou des charges a leur echeance, le present contrat
            sera resilie de plein droit apres mise en demeure restee sans effet pendant quinze (15) jours.
          </Text>
        </View>

        {/* Signatures */}
        <View style={S.signatures}>
          <View style={S.sigBox}>
            <Text style={{ fontSize: 9, marginBottom: 4 }}>LE BAILLEUR</Text>
            <Text style={{ fontSize: 9 }}>{bailleurNom}</Text>
            <Text style={{ fontSize: 8, color: '#7F8C8D', marginTop: 20 }}>Signature :</Text>
          </View>
          <View style={S.sigBox}>
            <Text style={{ fontSize: 9, marginBottom: 4 }}>LE PRENEUR</Text>
            <Text style={{ fontSize: 9 }}>{preneurNom}</Text>
            <Text style={{ fontSize: 8, color: '#7F8C8D', marginTop: 20 }}>Signature :</Text>
          </View>
        </View>

        <Text style={{ marginTop: 16, fontSize: 9, textAlign: 'center', color: '#7F8C8D' }}>
          Fait a {lieuSignature}, le {dateSignature}
        </Text>

        <Text style={S.footer}>
          Immo CI — Plateforme immobiliere Cote d&apos;Ivoire — immo-ci.vercel.app
        </Text>
      </Page>
    </Document>
  )
}
```
  </action>
  <verify>
    <automated>grep "serverExternalPackages" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/next.config.ts" && grep "currency.*false\|montantEnLettres" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/lib/contrat-pdf.tsx" && grep "ToWords" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/lib/contrat-pdf.tsx" && echo "CONFIG_PDF_OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/next.config.ts contient "serverExternalPackages: ['@react-pdf/renderer']"
    - apps/web/lib/contrat-pdf.tsx exporte ContratDocument, montantEnLettres, ContratProps
    - contrat-pdf.tsx utilise currency: false (jamais currency: true)
    - montantEnLettres() ajoute le suffixe "francs CFA" manuellement
    - ContratDocument contient les 6 articles OHADA (parties, bien, duree, loyer, depot, resiliation)
    - Le loyer apparait en chiffres (toLocaleString) ET en lettres (montantEnLettres)
  </acceptance_criteria>
  <done>next.config.ts patche (serverExternalPackages), lib/contrat-pdf.tsx cree avec 6 articles OHADA et montants FCFA en lettres et chiffres.</done>
</task>

<task type="auto">
  <name>Task 2: Route API generer contrat PDF + upload Supabase Storage</name>
  <files>
    apps/web/app/api/contrats/generer/route.ts
    apps/web/app/api/contrats/[id]/route.ts
  </files>
  <read_first>
    - apps/web/lib/contrat-pdf.tsx (ContratProps interface de la Task 1)
    - apps/web/lib/supabase/server.ts (createClient async)
    - apps/web/app/api/biens/route.ts (pattern service role + createClient)
  </read_first>
  <action>
**Creer apps/web/app/api/contrats/generer/route.ts (RESA-03, RESA-06) :**
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createServerClient }        from '@supabase/ssr'
import { renderToBuffer }            from '@react-pdf/renderer'
import { createElement }             from 'react'
import { ContratDocument }           from '@/lib/contrat-pdf'
import type { ContratProps }         from '@/lib/contrat-pdf'

// Ce handler utilise renderToBuffer — necessite serverExternalPackages dans next.config.ts

export async function POST(req: NextRequest) {
  // Utiliser service role key : appelable depuis webhook (unauthentifie) apres paiement confirme
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )

  const body = await req.json() as { reservationId: string }
  if (!body.reservationId) {
    return NextResponse.json({ error: 'reservationId requis' }, { status: 400 })
  }

  // Recuperer la reservation + bien + locataire + proprietaire
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: resa, error: resaError } = await (supabase.from('reservations') as any)
    .select(`
      id, date_debut, date_fin, montant_fcfa,
      biens (
        id, titre, adresse_complete, commune, surface_m2, nb_pieces,
        charges_mois_fcfa, depot_garantie_fcfa,
        proprietaire_id
      ),
      profiles!locataire_id ( nom_complet, telephone )
    `)
    .eq('id', body.reservationId)
    .single()

  if (resaError || !resa) {
    return NextResponse.json({ error: 'Reservation introuvable' }, { status: 404 })
  }

  // Recuperer le profil bailleur
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: bailleur } = await (supabase.from('profiles') as any)
    .select('nom_complet, telephone')
    .eq('id', resa.biens.proprietaire_id)
    .single()

  const bien     = resa.biens
  const locataire = resa.profiles
  const today    = new Date().toLocaleDateString('fr-FR')

  const contratProps: ContratProps = {
    contratId:          resa.id,
    reservationId:      resa.id,
    dateDebut:          resa.date_debut,
    dateFin:            resa.date_fin,
    bailleurNom:        bailleur?.nom_complet  ?? 'Non renseigne',
    bailleurCni:        '—',
    bailleurTel:        bailleur?.telephone    ?? '—',
    preneurNom:         locataire?.nom_complet ?? 'Non renseigne',
    preneurCni:         '—',
    preneurTel:         locataire?.telephone   ?? '—',
    bienAdresse:        bien.adresse_complete  ?? bien.titre,
    bienCommune:        bien.commune,
    surfaceM2:          bien.surface_m2        ?? 0,
    nbPieces:           bien.nb_pieces         ?? 0,
    loyerMoisFcfa:      Number(resa.montant_fcfa),
    chargesMoisFcfa:    Number(bien.charges_mois_fcfa ?? 0),
    depotGarantieFcfa:  Number(bien.depot_garantie_fcfa ?? 0),
    dateSignature:      today,
    lieuSignature:      bien.commune,
  }

  // Generer le PDF buffer — necessite serverExternalPackages: ['@react-pdf/renderer']
  const pdfBuffer = await renderToBuffer(createElement(ContratDocument, contratProps))

  // RESA-06 : Creer le bucket 'contrats' s'il n'existe pas encore
  await supabase.storage.createBucket('contrats', { public: false }).catch(() => {
    // Ignorer si le bucket existe deja (erreur Duplicate)
  })

  const fileName = `contrat-${resa.id}-${Date.now()}.pdf`

  const { error: uploadError } = await supabase.storage
    .from('contrats')
    .upload(fileName, pdfBuffer, { contentType: 'application/pdf', upsert: true })

  if (uploadError) {
    return NextResponse.json({ error: uploadError.message }, { status: 500 })
  }

  // Generer une URL signee valable 1 an (3600 * 24 * 365 secondes)
  const { data: signedData } = await supabase.storage
    .from('contrats')
    .createSignedUrl(fileName, 3600 * 24 * 365)

  const pdfUrl = signedData?.signedUrl ?? ''

  // Inserer dans la table contrats + mettre a jour pdf_url
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: contrat } = await (supabase.from('contrats') as any)
    .insert({
      reservation_id:       resa.id,
      bien_id:              bien.id,
      bailleur_id:          bien.proprietaire_id,
      preneur_id:           resa.locataire_id,
      date_debut:           resa.date_debut,
      date_fin:             resa.date_fin,
      loyer_mois_fcfa:      contratProps.loyerMoisFcfa,
      charges_mois_fcfa:    contratProps.chargesMoisFcfa,
      depot_garantie_fcfa:  contratProps.depotGarantieFcfa,
      statut:               'brouillon',
      pdf_url:              pdfUrl,
    })
    .select('id')
    .single()

  // Lier le contrat a la reservation
  if (contrat?.id) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    await (supabase.from('reservations') as any)
      .update({ contrat_id: contrat.id })
      .eq('id', resa.id)
  }

  return NextResponse.json({ contratId: contrat?.id, pdfUrl })
}
```

**Creer apps/web/app/api/contrats/[id]/route.ts (RESA-06 — acces securise PDF) :**
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient }              from '@/lib/supabase/server'

export async function GET(
  _req: NextRequest,
  { params }: { params: { id: string } }
) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifie' }, { status: 401 })

  // Verifier que l'utilisateur est bailleur ou preneur du contrat
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: contrat } = await (supabase.from('contrats') as any)
    .select('id, pdf_url, bailleur_id, preneur_id')
    .eq('id', params.id)
    .single()

  if (!contrat) return NextResponse.json({ error: 'Contrat introuvable' }, { status: 404 })

  const isParty = contrat.bailleur_id === user.id || contrat.preneur_id === user.id
  if (!isParty) return NextResponse.json({ error: 'Acces non autorise' }, { status: 403 })

  return NextResponse.json({ pdfUrl: contrat.pdf_url })
}
```
  </action>
  <verify>
    <automated>grep "renderToBuffer\|ContratDocument" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/contrats/generer/route.ts" && grep "storage.from.*contrats" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/contrats/generer/route.ts" && grep "createBucket\|contrats" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/contrats/generer/route.ts" && ls "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/contrats/[id]/route.ts" && echo "CONTRATS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/contrats/generer/route.ts contient "renderToBuffer" et "ContratDocument"
    - app/api/contrats/generer/route.ts appelle supabase.storage.createBucket('contrats') avant upload
    - app/api/contrats/generer/route.ts utilise SUPABASE_SERVICE_ROLE_KEY (via createServerClient direct)
    - app/api/contrats/generer/route.ts cree une URL signee (createSignedUrl) et la stocke dans contrats.pdf_url
    - app/api/contrats/[id]/route.ts verifie que user est bailleur_id ou preneur_id avant de retourner pdf_url
    - apps/web/next.config.ts contient "serverExternalPackages: ['@react-pdf/renderer']" (verifie Task 1)
  </acceptance_criteria>
  <done>POST /api/contrats/generer genere le PDF OHADA complet, cree le bucket contrats si necessaire, upload le fichier et stocke l'URL signee. GET /api/contrats/[id] retourne l'URL aux parties autorisees.</done>
</task>

</tasks>

<verification>
- grep "serverExternalPackages" apps/web/next.config.ts
- grep "currency.*false" apps/web/lib/contrat-pdf.tsx
- grep "renderToBuffer" apps/web/app/api/contrats/generer/route.ts
- grep "createBucket\|storage.from" apps/web/app/api/contrats/generer/route.ts
- grep "ARTICLE\|OHADA" apps/web/lib/contrat-pdf.tsx (6 articles OHADA presents)
- npx tsc --noEmit depuis apps/web
</verification>

<success_criteria>
1. next.config.ts contient serverExternalPackages: ['@react-pdf/renderer'] — aucun crash au runtime
2. POST /api/contrats/generer retourne { contratId, pdfUrl } apres upload Supabase Storage
3. Le PDF contient loyer en chiffres ET en lettres (montantEnLettres) — jamais "euros"
4. Le contrat inclut 6 articles OHADA : parties, bien, duree, loyer+charges, depot garantie, resiliation
5. GET /api/contrats/[id] retourne 403 si l'utilisateur n'est ni bailleur ni preneur
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-paiements-r-servations-ia-dashboard/03-03-SUMMARY.md`
</output>
