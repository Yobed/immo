---
phase: 03-paiements-r-servations-ia-dashboard
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/web/app/api/reservations/route.ts
  - apps/web/app/(client)/reservations/nouvelle/page.tsx
  - apps/web/app/(client)/reservations/[id]/page.tsx
  - apps/web/components/reservation/ReservationFlow.tsx
  - apps/web/components/reservation/DatePicker.tsx
autonomous: true
requirements: [RESA-01, RESA-02, RESA-03, RESA-04, RESA-05, RESA-06]

must_haves:
  truths:
    - "Un locataire peut selectionner des dates et initier une reservation"
    - "Le systeme detecte les conflits de dates avant creation (overlap SQL)"
    - "La reservation passe de en_attente a confirmee apres paiement reussi"
    - "Un lien vers le contrat PDF est visible une fois la reservation confirmee"
    - "L'etat de la reservation (en_attente/confirmee/annulee) est affiche clairement"
  artifacts:
    - path: "apps/web/app/api/reservations/route.ts"
      provides: "POST create reservation + GET liste locataire"
      exports: ["POST", "GET"]
    - path: "apps/web/components/reservation/ReservationFlow.tsx"
      provides: "Flow multi-etapes : dates -> recap -> paiement"
    - path: "apps/web/components/reservation/DatePicker.tsx"
      provides: "Selecteur dates avec blocage des dates deja reservees"
    - path: "apps/web/app/(client)/reservations/nouvelle/page.tsx"
      provides: "Page de creation reservation pour un bien"
    - path: "apps/web/app/(client)/reservations/[id]/page.tsx"
      provides: "Page statut reservation avec lien contrat PDF"
  key_links:
    - from: "ReservationFlow.tsx"
      to: "/api/reservations"
      via: "fetch POST"
      pattern: "fetch.*api/reservations"
    - from: "app/api/reservations/route.ts"
      to: "supabase.from('reservations')"
      via: "conflict check date range avant insert"
      pattern: "date_debut.*date_fin\|lte.*gte"
    - from: "app/(client)/reservations/[id]/page.tsx"
      to: "PaiementButton"
      via: "import depuis @/components/paiements/PaiementButton"
      pattern: "PaiementButton"
---

<objective>
Flow reservation complet : selection dates avec detection conflits, creation reservation en_attente, declenchement paiement via PaiementButton (Plan 03-01), et affichage statut post-paiement avec lien contrat.

Purpose: Connecter le browse d'annonces au tunnel de conversion — de la fiche bien jusqu'a la reservation confirmee et payee.
Output: API reservations + ReservationFlow multi-etapes + pages nouvelle et statut.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md

@apps/web/app/api/biens/route.ts
@apps/web/lib/supabase/server.ts
@apps/web/components/paiements/PaiementButton.tsx

<interfaces>
<!-- Depuis Plan 03-01 (PaiementButton) -->
```typescript
// components/paiements/PaiementButton.tsx
interface PaiementButtonProps {
  reservationId: string
  montantFcfa:   number
  description:   string
  className?:    string
}
// Usage: <PaiementButton reservationId={id} montantFcfa={bien.prix_mois_fcfa} description="..." />
// Redirige vers CinetPay puis /paiement/retour
```

<!-- Table reservations (migration 004) — colonnes cles -->
```
id uuid, bien_id uuid, locataire_id uuid,
date_debut date, date_fin date,
montant_fcfa numeric, statut text (en_attente/confirmee/annulee/expiree),
paiement_id uuid, contrat_id uuid,
notes text, created_at timestamptz
```

<!-- Detection conflits dates (pattern etabli dans RESEARCH.md) -->
```sql
SELECT id FROM reservations
WHERE bien_id = $bienId
  AND statut NOT IN ('annulee', 'expiree')
  AND date_debut <= $dateFin
  AND date_fin >= $dateDebut
-- Si result.length > 0 -> conflit -> rejeter avec 409
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route API POST /api/reservations avec detection conflits</name>
  <files>
    apps/web/app/api/reservations/route.ts
  </files>
  <read_first>
    - apps/web/app/api/biens/route.ts (pattern createClient + NextResponse)
    - apps/web/lib/supabase/server.ts (createClient async)
    - .planning/phases/03-paiements-r-servations-ia-dashboard/03-RESEARCH.md (Pattern Reservation Date Conflict Check)
  </read_first>
  <action>
Creer apps/web/app/api/reservations/route.ts :

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function POST(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifie' }, { status: 401 })

  const body = await req.json() as {
    bienId:      string
    dateDebut:   string   // ISO date 'YYYY-MM-DD'
    dateFin:     string
    notes?:      string
  }

  if (!body.bienId || !body.dateDebut || !body.dateFin) {
    return NextResponse.json({ error: 'bienId, dateDebut et dateFin sont requis' }, { status: 400 })
  }
  if (new Date(body.dateFin) <= new Date(body.dateDebut)) {
    return NextResponse.json({ error: 'dateFin doit etre apres dateDebut' }, { status: 400 })
  }

  // RESA-01 : Verifier les conflits de dates avant creation
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: conflicts } = await (supabase.from('reservations') as any)
    .select('id')
    .eq('bien_id', body.bienId)
    .not('statut', 'in', '("annulee","expiree")')
    .lte('date_debut', body.dateFin)
    .gte('date_fin', body.dateDebut)

  if (conflicts && conflicts.length > 0) {
    return NextResponse.json(
      { error: 'Ces dates sont deja reservees pour ce bien', conflictCount: conflicts.length },
      { status: 409 }
    )
  }

  // Recuperer le prix du bien pour stocker montant_fcfa
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: bien } = await (supabase.from('biens') as any)
    .select('prix_mois_fcfa')
    .eq('id', body.bienId)
    .single()

  const montantFcfa = bien?.prix_mois_fcfa ?? 0

  // RESA-02 : Creer la reservation avec statut en_attente
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: reservation, error } = await (supabase.from('reservations') as any)
    .insert({
      bien_id:      body.bienId,
      locataire_id: user.id,
      date_debut:   body.dateDebut,
      date_fin:     body.dateFin,
      montant_fcfa: montantFcfa,
      statut:       'en_attente',
      notes:        body.notes ?? '',
    })
    .select('id, bien_id, date_debut, date_fin, montant_fcfa, statut')
    .single()

  if (error) return NextResponse.json({ error: error.message }, { status: 400 })
  return NextResponse.json(reservation, { status: 201 })
}

export async function GET(req: NextRequest) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Non authentifie' }, { status: 401 })

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase.from('reservations') as any)
    .select(`
      id, date_debut, date_fin, montant_fcfa, statut, created_at,
      biens ( titre, commune, adresse_complete )
    `)
    .eq('locataire_id', user.id)
    .order('created_at', { ascending: false })

  if (error) return NextResponse.json({ error: error.message }, { status: 400 })
  return NextResponse.json(data)
}
```
  </action>
  <verify>
    <automated>grep -l "date_debut\|date_fin\|conflit\|conflicts\|en_attente" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/reservations/route.ts" && grep "409" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/api/reservations/route.ts" && echo "API_RESERVATIONS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/reservations/route.ts contient "lte('date_debut'" et "gte('date_fin'" (detection overlap)
    - app/api/reservations/route.ts retourne status 409 si conflits detectes
    - La reservation inseree a statut = 'en_attente'
    - GET retourne la liste avec biens.titre join
    - POST necessite authentification (401 si pas de user)
  </acceptance_criteria>
  <done>POST /api/reservations cree la reservation en_attente apres verification conflits. GET /api/reservations retourne les reservations du locataire connecte.</done>
</task>

<task type="auto">
  <name>Task 2: ReservationFlow composant + pages nouvelle et statut</name>
  <files>
    apps/web/components/reservation/ReservationFlow.tsx
    apps/web/components/reservation/DatePicker.tsx
    apps/web/app/(client)/reservations/nouvelle/page.tsx
    apps/web/app/(client)/reservations/[id]/page.tsx
  </files>
  <read_first>
    - apps/web/components/paiements/PaiementButton.tsx (interface props pour integration)
    - apps/web/app/api/reservations/route.ts (interface de reponse POST)
    - apps/web/lib/supabase/server.ts (createClient pour Server Components)
  </read_first>
  <action>
**Creer apps/web/components/reservation/DatePicker.tsx :**
```typescript
'use client'
import { useState } from 'react'

interface Props {
  onDatesSelected: (dateDebut: string, dateFin: string) => void
  minDate?: string   // ISO date string
}

export function DatePicker({ onDatesSelected, minDate }: Props) {
  const today   = minDate ?? new Date().toISOString().split('T')[0]
  const [debut, setDebut] = useState('')
  const [fin,   setFin]   = useState('')
  const [error, setError] = useState('')

  function handleConfirm() {
    if (!debut || !fin) { setError('Veuillez selectionner les deux dates'); return }
    if (fin <= debut)   { setError('La date de fin doit etre apres la date de debut'); return }
    setError('')
    onDatesSelected(debut, fin)
  }

  return (
    <div className="space-y-4">
      <div className="grid grid-cols-2 gap-4">
        <div>
          <label className="block text-sm font-medium text-text mb-1">Date d&apos;arrivee</label>
          <input
            type="date"
            min={today}
            value={debut}
            onChange={e => setDebut(e.target.value)}
            className="w-full border border-[var(--border)] rounded-btn px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary"
          />
        </div>
        <div>
          <label className="block text-sm font-medium text-text mb-1">Date de depart</label>
          <input
            type="date"
            min={debut || today}
            value={fin}
            onChange={e => setFin(e.target.value)}
            className="w-full border border-[var(--border)] rounded-btn px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary"
          />
        </div>
      </div>
      {error && <p className="text-danger text-sm">{error}</p>}
      <button
        onClick={handleConfirm}
        className="w-full bg-primary text-white py-2 rounded-btn font-medium hover:opacity-90 transition-opacity"
      >
        Confirmer les dates
      </button>
    </div>
  )
}
```

**Creer apps/web/components/reservation/ReservationFlow.tsx :**
```typescript
'use client'
import { useState } from 'react'
import { DatePicker }    from './DatePicker'
import { PaiementButton } from '@/components/paiements/PaiementButton'

type Step = 'dates' | 'recap' | 'paiement'

interface Props {
  bienId:        string
  bienTitre:     string
  prixMoisFcfa:  number
}

export function ReservationFlow({ bienId, bienTitre, prixMoisFcfa }: Props) {
  const [step,          setStep]          = useState<Step>('dates')
  const [dateDebut,     setDateDebut]     = useState('')
  const [dateFin,       setDateFin]       = useState('')
  const [reservationId, setReservationId] = useState<string | null>(null)
  const [error,         setError]         = useState<string | null>(null)
  const [loading,       setLoading]       = useState(false)

  async function handleDatesSelected(debut: string, fin: string) {
    setDateDebut(debut)
    setDateFin(fin)
    setStep('recap')
  }

  async function handleConfirmerReservation() {
    setLoading(true)
    setError(null)
    try {
      const res = await fetch('/api/reservations', {
        method:  'POST',
        headers: { 'Content-Type': 'application/json' },
        body:    JSON.stringify({ bienId, dateDebut, dateFin }),
      })
      const data = await res.json()
      if (!res.ok) {
        setError(data.error ?? 'Erreur lors de la creation de la reservation')
        return
      }
      setReservationId(data.id)
      setStep('paiement')
    } catch {
      setError('Impossible de contacter le serveur')
    } finally {
      setLoading(false)
    }
  }

  const nbNuits = dateDebut && dateFin
    ? Math.max(0, Math.ceil((new Date(dateFin).getTime() - new Date(dateDebut).getTime()) / 86400000))
    : 0

  return (
    <div className="bg-surface-card rounded-card p-6 shadow-sm">
      <h2 className="font-display text-xl text-primary mb-4">Reserver ce bien</h2>

      {step === 'dates' && (
        <DatePicker onDatesSelected={handleDatesSelected} />
      )}

      {step === 'recap' && (
        <div className="space-y-4">
          <p className="text-sm text-muted">Bien : <span className="text-text font-medium">{bienTitre}</span></p>
          <p className="text-sm text-muted">Arrivee : <span className="text-text font-medium">{dateDebut}</span></p>
          <p className="text-sm text-muted">Depart : <span className="text-text font-medium">{dateFin}</span></p>
          <p className="text-sm text-muted">Duree : <span className="text-text font-medium">{nbNuits} nuit(s)</span></p>
          <p className="text-lg font-semibold font-mono text-primary">
            {prixMoisFcfa.toLocaleString('fr-FR')} FCFA / mois
          </p>
          {error && <p className="text-danger text-sm">{error}</p>}
          <div className="flex gap-3">
            <button
              onClick={() => { setStep('dates'); setError(null) }}
              className="flex-1 border border-[var(--border)] text-text py-2 rounded-btn text-sm hover:bg-surface transition-colors"
            >
              Modifier les dates
            </button>
            <button
              onClick={handleConfirmerReservation}
              disabled={loading}
              className="flex-1 bg-primary text-white py-2 rounded-btn text-sm font-medium hover:opacity-90 disabled:opacity-50"
            >
              {loading ? 'Confirmation...' : 'Confirmer la reservation'}
            </button>
          </div>
        </div>
      )}

      {step === 'paiement' && reservationId && (
        <div className="space-y-4">
          <div className="bg-accent-light rounded-btn p-3 text-accent text-sm">
            Reservation creee ! Finalisez par le paiement.
          </div>
          <PaiementButton
            reservationId={reservationId}
            montantFcfa={prixMoisFcfa}
            description={`Reservation : ${bienTitre}`}
            className="w-full"
          />
        </div>
      )}
    </div>
  )
}
```

**Creer apps/web/app/(client)/reservations/nouvelle/page.tsx :**
```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect }     from 'next/navigation'
import { ReservationFlow } from '@/components/reservation/ReservationFlow'

interface Props {
  searchParams: { bienId?: string }
}

export default async function NouvelleReservationPage({ searchParams }: Props) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  const bienId = searchParams.bienId
  if (!bienId) redirect('/')

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: bien } = await (supabase.from('biens') as any)
    .select('id, titre, commune, prix_mois_fcfa')
    .eq('id', bienId)
    .eq('statut', 'publie')
    .single()

  if (!bien) redirect('/')

  return (
    <div className="min-h-screen bg-surface py-8 px-4">
      <div className="max-w-lg mx-auto">
        <h1 className="font-display text-2xl text-primary mb-2">{bien.titre}</h1>
        <p className="text-muted text-sm mb-6">{bien.commune}</p>
        <ReservationFlow
          bienId={bien.id}
          bienTitre={bien.titre}
          prixMoisFcfa={bien.prix_mois_fcfa}
        />
      </div>
    </div>
  )
}
```

**Creer apps/web/app/(client)/reservations/[id]/page.tsx :**
```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect }     from 'next/navigation'
import Link             from 'next/link'

const STATUT_LABELS: Record<string, { label: string; color: string }> = {
  en_attente: { label: 'En attente de paiement', color: 'text-warning' },
  confirmee:  { label: 'Confirmee',              color: 'text-accent' },
  annulee:    { label: 'Annulee',                color: 'text-danger' },
  expiree:    { label: 'Expiree',                color: 'text-muted' },
}

export default async function ReservationDetailPage({ params }: { params: { id: string } }) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/connexion')

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: reservation } = await (supabase.from('reservations') as any)
    .select(`
      id, date_debut, date_fin, montant_fcfa, statut, created_at,
      biens ( titre, commune, adresse_complete ),
      contrats ( pdf_url )
    `)
    .eq('id', params.id)
    .eq('locataire_id', user.id)
    .single()

  if (!reservation) redirect('/reservations')

  const statut = STATUT_LABELS[reservation.statut] ?? { label: reservation.statut, color: 'text-muted' }

  return (
    <div className="min-h-screen bg-surface py-8 px-4">
      <div className="max-w-lg mx-auto bg-surface-card rounded-card p-6 shadow-sm space-y-4">
        <h1 className="font-display text-xl text-primary">Detail de la reservation</h1>
        <p className="text-sm text-muted">Bien : <span className="text-text font-medium">{reservation.biens?.titre}</span></p>
        <p className="text-sm text-muted">{reservation.biens?.commune} — {reservation.biens?.adresse_complete}</p>
        <p className="text-sm text-muted">Du <strong>{reservation.date_debut}</strong> au <strong>{reservation.date_fin}</strong></p>
        <p className="text-lg font-mono font-semibold text-primary">
          {Number(reservation.montant_fcfa).toLocaleString('fr-FR')} FCFA
        </p>
        <p className={`font-medium ${statut.color}`}>{statut.label}</p>

        {reservation.contrats?.pdf_url && (
          <a
            href={reservation.contrats.pdf_url}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-block bg-primary text-white px-4 py-2 rounded-btn text-sm font-medium hover:opacity-90"
          >
            Telecharger le contrat PDF
          </a>
        )}

        <Link href="/" className="block text-sm text-muted hover:underline">
          Retour a l&apos;accueil
        </Link>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>ls "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/reservation/ReservationFlow.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/reservation/DatePicker.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(client)/reservations/nouvelle/page.tsx" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/app/(client)/reservations/[id]/page.tsx" && grep "PaiementButton" "/c/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo/apps/web/components/reservation/ReservationFlow.tsx" && echo "FLOW_OK"</automated>
  </verify>
  <acceptance_criteria>
    - components/reservation/ReservationFlow.tsx contient "use client" et les 3 etapes (dates/recap/paiement)
    - ReservationFlow importe et utilise PaiementButton
    - components/reservation/DatePicker.tsx contient "use client" et les deux inputs type="date"
    - app/(client)/reservations/nouvelle/page.tsx lit searchParams.bienId et passe props a ReservationFlow
    - app/(client)/reservations/[id]/page.tsx affiche reservation.statut et le lien contrat PDF si present
    - La page statut contient "Telecharger le contrat PDF" conditionnel sur contrats.pdf_url
  </acceptance_criteria>
  <done>Flow reservation complet : DatePicker -> recap -> PaiementButton. Page statut affiche l'etat de la reservation et le lien PDF du contrat une fois genere.</done>
</task>

</tasks>

<verification>
- grep "conflicts\|conflit\|409" apps/web/app/api/reservations/route.ts
- grep "PaiementButton" apps/web/components/reservation/ReservationFlow.tsx
- grep "pdf_url" apps/web/app/(client)/reservations/\[id\]/page.tsx
- npx tsc --noEmit depuis apps/web
</verification>

<success_criteria>
1. POST /api/reservations retourne 409 si dates en conflit avec reservation existante (statut non annulee/expiree)
2. POST /api/reservations retourne 201 avec reservation.id et statut en_attente si pas de conflit
3. ReservationFlow enchai ne : selection dates -> recap -> PaiementButton -> redirect CinetPay
4. Page /reservations/[id] affiche statut colore et lien PDF si contrat.pdf_url present
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-paiements-r-servations-ia-dashboard/03-02-SUMMARY.md`
</output>
