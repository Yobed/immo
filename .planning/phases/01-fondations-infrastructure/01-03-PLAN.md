---
phase: 01-fondations-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/web/lib/supabase/client.ts
  - apps/web/lib/supabase/server.ts
  - apps/web/middleware.ts
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/register/page.tsx
  - apps/web/app/(auth)/callback/route.ts
  - apps/web/app/(auth)/verify-otp/page.tsx
  - supabase/migrations/000_auth_trigger.sql
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "apps/web/middleware.ts contient createServerClient et updateSession"
    - "apps/web/lib/supabase/server.ts utilise getUser() et non getSession()"
    - "supabase/migrations/000_auth_trigger.sql contient handle_new_user"
    - "apps/web/app/(auth)/callback/route.ts contient exchangeCodeForSession"
  actions:
    - "Utilisateur peut s'inscrire avec email + mot de passe"
    - "Utilisateur peut se connecter via Google OAuth"
    - "Utilisateur peut se connecter via OTP SMS"
    - "Session persiste après refresh navigateur"
    - "Routes /pro/* et /client/* redirigent vers /login si non authentifié"

goal: "Authentification Supabase complète (email, Google OAuth, OTP SMS) avec middleware SSR, trigger création profil, et protection des routes par rôle."
---

# Plan 01-03: Auth & Profils

## Contexte

Authentification Supabase SSR pour Next.js 14 App Router. Utiliser `@supabase/ssr` (jamais `@supabase/auth-helpers-nextjs` déprécié). Toujours `getUser()` côté serveur — `getSession()` n'est pas sécurisé car lit depuis le cookie sans valider le JWT.

## Tâches

### Tâche 1 : Clients Supabase SSR

<read_first>
- apps/web/package.json (vérifier @supabase/supabase-js et @supabase/ssr installés)
- .env.local.example (vérifier NEXT_PUBLIC_SUPABASE_URL et NEXT_PUBLIC_SUPABASE_ANON_KEY)
</read_first>

<action>
Créer `apps/web/lib/supabase/client.ts` :
```ts
import { createBrowserClient } from '@supabase/ssr'
import type { Database } from '@packages/shared/types/database'

export function createClient() {
  return createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

Créer `apps/web/lib/supabase/server.ts` :
```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import type { Database } from '@packages/shared/types/database'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {}
        },
      },
    }
  )
}
```
</action>

<acceptance_criteria>
- apps/web/lib/supabase/client.ts contient `createBrowserClient`
- apps/web/lib/supabase/server.ts contient `createServerClient` et `getAll`
- apps/web/lib/supabase/server.ts ne contient PAS `getSession`
</acceptance_criteria>

---

### Tâche 2 : Middleware SSR

<read_first>
- apps/web/lib/supabase/server.ts (pattern createServerClient déjà créé)
</read_first>

<action>
Créer `apps/web/middleware.ts` :
```ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  const protectedRoutes = ['/pro', '/client', '/dashboard']
  const isProtected = protectedRoutes.some(route =>
    request.nextUrl.pathname.startsWith(route)
  )

  if (isProtected && !user) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    url.searchParams.set('redirect', request.nextUrl.pathname)
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```
</action>

<acceptance_criteria>
- apps/web/middleware.ts contient `createServerClient`
- apps/web/middleware.ts contient `supabase.auth.getUser()`
- apps/web/middleware.ts contient `matcher`
- apps/web/middleware.ts ne contient PAS `getSession`
</acceptance_criteria>

---

### Tâche 3 : Trigger PostgreSQL création profil

<read_first>
- supabase/migrations/001_profiles.sql (structure de la table profiles)
</read_first>

<action>
Créer `supabase/migrations/000_auth_trigger.sql` (numéroté 000 pour s'exécuter avant 001) :
```sql
-- Trigger : créer profil automatiquement à l'inscription
create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer set search_path = public
as $$
begin
  insert into public.profiles (id, email, full_name, avatar_url, role)
  values (
    new.id,
    new.email,
    coalesce(new.raw_user_meta_data->>'full_name', split_part(new.email, '@', 1)),
    new.raw_user_meta_data->>'avatar_url',
    'locataire'  -- rôle par défaut
  );
  return new;
end;
$$;

-- Attacher le trigger sur auth.users
drop trigger if exists on_auth_user_created on auth.users;
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure public.handle_new_user();
```
</action>

<acceptance_criteria>
- supabase/migrations/000_auth_trigger.sql contient `handle_new_user`
- supabase/migrations/000_auth_trigger.sql contient `on_auth_user_created`
- supabase/migrations/000_auth_trigger.sql contient `security definer`
- supabase/migrations/000_auth_trigger.sql contient `'locataire'`
</acceptance_criteria>

---

### Tâche 4 : Pages auth (login, register, callback, OTP)

<read_first>
- apps/web/app/layout.tsx (structure layout racine)
- apps/web/lib/supabase/client.ts (client browser)
</read_first>

<action>
Créer `apps/web/app/(auth)/callback/route.ts` (OAuth + email magic link callback) :
```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/'

  if (code) {
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll() { return cookieStore.getAll() },
          setAll(cookiesToSet) {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          },
        },
      }
    )
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) return NextResponse.redirect(`${origin}${next}`)
  }

  return NextResponse.redirect(`${origin}/login?error=auth_callback_error`)
}
```

Créer `apps/web/app/(auth)/login/page.tsx` avec :
- Formulaire email + password (react-hook-form + zod)
- Bouton "Continuer avec Google" → `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${origin}/auth/callback` } })`
- Lien "Se connecter avec mon téléphone" → vers /verify-otp
- Texte et labels en français, prix/codes jamais affichés sur cette page

Créer `apps/web/app/(auth)/register/page.tsx` avec :
- Champs : email, mot de passe, nom complet, rôle (locataire | propriétaire)
- `supabase.auth.signUp({ email, password, options: { data: { full_name, role } } })`
- Message de confirmation email après inscription
- Labels en français

Créer `apps/web/app/(auth)/verify-otp/page.tsx` avec :
- Étape 1 : saisie numéro téléphone (+225...) → `supabase.auth.signInWithOtp({ phone })`
- Étape 2 : saisie code OTP 6 chiffres → `supabase.auth.verifyOtp({ phone, token, type: 'sms' })`
- Messages d'erreur en français
</action>

<acceptance_criteria>
- apps/web/app/(auth)/callback/route.ts contient `exchangeCodeForSession`
- apps/web/app/(auth)/login/page.tsx contient `signInWithOAuth` et `provider: 'google'`
- apps/web/app/(auth)/register/page.tsx contient `signUp`
- apps/web/app/(auth)/verify-otp/page.tsx contient `signInWithOtp` et `verifyOtp`
- Tous les fichiers contiennent du texte en français (ex: "Connexion", "S'inscrire", "Téléphone")
</acceptance_criteria>

---

## Vérification finale

```bash
# Vérifier que middleware utilise getUser (pas getSession)
grep -r "getSession" apps/web/middleware.ts  # doit retourner vide

# Vérifier trigger présent
grep "handle_new_user" supabase/migrations/000_auth_trigger.sql

# Vérifier callback OAuth
grep "exchangeCodeForSession" apps/web/app/(auth)/callback/route.ts

# Vérifier OTP
grep "signInWithOtp" apps/web/app/(auth)/verify-otp/page.tsx
```
