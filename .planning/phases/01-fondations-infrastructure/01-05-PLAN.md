---
phase: 01-fondations-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - apps/web/app/(public)/page.tsx
  - apps/web/app/(public)/layout.tsx
  - apps/web/components/landing/Hero.tsx
  - apps/web/components/landing/HowItWorks.tsx
  - apps/web/components/landing/FeaturedProperties.tsx
  - apps/web/components/landing/Features.tsx
  - apps/web/components/landing/MapZones.tsx
  - apps/web/components/landing/Testimonials.tsx
  - apps/web/components/landing/Stats.tsx
  - apps/web/components/landing/Partners.tsx
  - apps/web/components/landing/CTAFinal.tsx
  - apps/web/components/landing/Footer.tsx
  - apps/web/app/sitemap.ts
  - apps/web/app/robots.ts
  - apps/web/app/layout.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03

must_haves:
  truths:
    - "apps/web/app/(public)/page.tsx importe Hero, HowItWorks, FeaturedProperties"
    - "apps/web/components/landing/Hero.tsx contient App Store et Play Store"
    - "apps/web/app/layout.tsx contient generateMetadata ou metadata"
    - "apps/web/app/sitemap.ts existe"
    - "apps/web/app/robots.ts existe"
  actions:
    - "Landing page 10 sections en français, mobile-first, Playfair Display pour H1/H2"
    - "Hero avec barre de recherche et CTA stores"
    - "SEO : meta tags, Open Graph, sitemap, robots"

goal: "Landing page 10 sections premium en français, mobile-first, avec hero searchbar + CTA App Store/Play Store et SEO complet (meta, OG, sitemap, robots)."
---

# Plan 01-05: Landing Page

## Contexte

Landing page Next.js 14 App Router. 10 sections selon `skills.md` section 13 :
Hero → Comment ça marche → Biens vedette → Fonctionnalités → Carte zones CI → Témoignages → Chiffres clés → Partenaires → CTA final → Footer.

Règles :
- Playfair Display pour H1, H2
- DM Sans pour body
- Mobile-first (sm: → md: → lg:)
- Tout en français
- Prix toujours en FCFA

## Tâches

### Tâche 1 : Layout & Métadonnées globales

<read_first>
- apps/web/app/globals.css (variables CSS disponibles)
- apps/web/tailwind.config.ts (classes disponibles)
</read_first>

<action>
Mettre à jour `apps/web/app/layout.tsx` avec :
```tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: {
    default: 'Immo CI — Immobilier Côte d\'Ivoire',
    template: '%s | Immo CI',
  },
  description: 'Trouvez votre bien immobilier en Côte d\'Ivoire. Location et vente à Abidjan et partout en CI. Paiement Wave, Orange Money, MTN.',
  keywords: ['immobilier', 'Côte d\'Ivoire', 'Abidjan', 'location', 'vente', 'appartement', 'villa'],
  openGraph: {
    type: 'website',
    locale: 'fr_CI',
    url: 'https://immo-ci.vercel.app',
    siteName: 'Immo CI',
    title: 'Immo CI — Immobilier Côte d\'Ivoire',
    description: 'La plateforme immobilière premium de Côte d\'Ivoire',
    images: [{ url: '/og-image.jpg', width: 1200, height: 630, alt: 'Immo CI' }],
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Immo CI — Immobilier Côte d\'Ivoire',
    description: 'La plateforme immobilière premium de Côte d\'Ivoire',
  },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
```

Créer `apps/web/app/sitemap.ts` :
```ts
import { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: 'https://immo-ci.vercel.app', lastModified: new Date(), changeFrequency: 'weekly', priority: 1 },
    { url: 'https://immo-ci.vercel.app/biens', lastModified: new Date(), changeFrequency: 'daily', priority: 0.9 },
    { url: 'https://immo-ci.vercel.app/login', lastModified: new Date(), changeFrequency: 'monthly', priority: 0.5 },
  ]
}
```

Créer `apps/web/app/robots.ts` :
```ts
import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: '*', allow: '/', disallow: ['/pro/', '/client/', '/api/'] },
    sitemap: 'https://immo-ci.vercel.app/sitemap.xml',
  }
}
```
</action>

<acceptance_criteria>
- apps/web/app/layout.tsx contient `metadata` avec `openGraph`
- apps/web/app/layout.tsx contient `lang="fr"`
- apps/web/app/sitemap.ts contient `MetadataRoute.Sitemap`
- apps/web/app/robots.ts contient `MetadataRoute.Robots`
</acceptance_criteria>

---

### Tâche 2 : Section Hero

<read_first>
- apps/web/components/ui/Button.tsx (composant Button disponible)
- apps/web/tailwind.config.ts (classes disponibles)
</read_first>

<action>
Créer `apps/web/components/landing/Hero.tsx` :
```tsx
'use client'
import { useState } from 'react'
import { Button } from '@/components/ui'

export function Hero() {
  const [search, setSearch] = useState('')

  return (
    <section className="relative min-h-[90vh] bg-primary flex items-center overflow-hidden">
      {/* Overlay gradient */}
      <div className="absolute inset-0 bg-gradient-to-br from-primary via-primary/95 to-[#0d3a5c]" />

      <div className="relative z-10 container mx-auto px-4 py-20 text-white">
        <div className="max-w-3xl">
          {/* Badge */}
          <span className="inline-block mb-4 px-3 py-1 rounded-pill bg-secondary/20 text-secondary text-sm font-medium border border-secondary/30">
            🏠 Plateforme N°1 en Côte d'Ivoire
          </span>

          {/* Titre principal */}
          <h1 className="font-display text-4xl md:text-5xl lg:text-6xl font-bold leading-tight mb-6">
            Trouvez votre bien<br />
            <span className="text-secondary">en Côte d'Ivoire</span>
          </h1>

          <p className="font-sans text-lg text-white/80 mb-8 max-w-xl">
            Location et vente à Abidjan et partout en CI. Paiement Wave, Orange Money, MTN. Contrats OHADA en ligne.
          </p>

          {/* Barre de recherche */}
          <div className="flex flex-col sm:flex-row gap-3 mb-8 max-w-2xl">
            <input
              type="text"
              value={search}
              onChange={e => setSearch(e.target.value)}
              placeholder="Rechercher un bien, une commune, un quartier..."
              className="flex-1 rounded-btn px-4 py-3 text-[var(--text)] bg-white border-0 focus:outline-none focus:ring-2 focus:ring-secondary text-sm"
            />
            <Button size="lg" variant="secondary" className="shrink-0">
              Rechercher
            </Button>
          </div>

          {/* CTA App Store / Play Store */}
          <div className="flex flex-wrap gap-3">
            <a
              href="https://apps.apple.com"
              target="_blank"
              rel="noopener noreferrer"
              className="flex items-center gap-2 px-4 py-2 rounded-btn bg-white/10 border border-white/20 hover:bg-white/20 transition-colors"
            >
              <span className="text-xl">🍎</span>
              <div>
                <p className="text-xs text-white/60">Télécharger sur</p>
                <p className="text-sm font-semibold">App Store</p>
              </div>
            </a>
            <a
              href="https://play.google.com"
              target="_blank"
              rel="noopener noreferrer"
              className="flex items-center gap-2 px-4 py-2 rounded-btn bg-white/10 border border-white/20 hover:bg-white/20 transition-colors"
            >
              <span className="text-xl">▶</span>
              <div>
                <p className="text-xs text-white/60">Disponible sur</p>
                <p className="text-sm font-semibold">Google Play</p>
              </div>
            </a>
          </div>
        </div>
      </div>
    </section>
  )
}
```
</action>

<acceptance_criteria>
- apps/web/components/landing/Hero.tsx contient `App Store`
- apps/web/components/landing/Hero.tsx contient `Google Play`
- apps/web/components/landing/Hero.tsx contient `font-display`
- apps/web/components/landing/Hero.tsx contient `text-secondary`
- apps/web/components/landing/Hero.tsx contient `'use client'`
</acceptance_criteria>

---

### Tâche 3 : Sections restantes (8 sections)

<read_first>
- apps/web/components/landing/Hero.tsx (pattern établi)
- apps/web/components/ui/Card.tsx (composant Card disponible)
</read_first>

<action>
Créer les 8 sections restantes dans `apps/web/components/landing/` :

**HowItWorks.tsx** — "Comment ça marche" : 3 étapes (Publiez, Réservez, Payez) avec icônes et descriptions en français. Fond blanc.

**FeaturedProperties.tsx** — "Biens vedette" : grille 3 colonnes de cartes factices (props statiques) avec photo placeholder, titre, prix FCFA, commune, badge type. Titre H2 Playfair. Bouton "Voir tous les biens".

**Features.tsx** — "Fonctionnalités" : liste 6 features différenciatrices (Vue 360°, Paiement Wave/OM, Contrat OHADA, Chatbot IA, Dashboard analytics, Notifications WhatsApp) avec icônes. Fond surface (#F4F6F8).

**MapZones.tsx** — "Zones couvertes" : liste des communes d'Abidjan (Cocody, Plateau, Marcory, Yopougon, Adjamé, Abobo, Koumassi, Port-Bouet, Bingerville, Attécoubé, Treichville, Songon) en grille badges. Fond primaire.

**Testimonials.tsx** — "Témoignages" : 3 témoignages factices (propriétaire, locataire, agence) avec avatar placeholder, nom, note 5 étoiles, texte en français.

**Stats.tsx** — "Chiffres clés" : 4 stats (ex : "2 450 biens", "12 communes", "98% satisfaction", "4.8/5 note moyenne") en grands chiffres JetBrains Mono + labels DM Sans. Fond primaire.

**Partners.tsx** — "Partenaires" : logos texte (Wave, Orange Money, MTN, Moov Money, CinetPay, Cloudinary) en grille. Fond blanc.

**CTAFinal.tsx** — CTA final : titre H2 Playfair "Prêt à trouver votre bien ?", sous-titre, 2 boutons (Chercher un bien → /biens, Publier un bien → /register).

**Footer.tsx** — Footer : logo + tagline, colonnes (À propos, Biens, Compte, Contact), mentions légales, copyright "© 2026 Immo CI. Tous droits réservés."

Chaque composant : `export function [Nom]() { ... }`, mobile-first, texte français.
</action>

<acceptance_criteria>
- apps/web/components/landing/HowItWorks.tsx existe et contient "Comment ça marche"
- apps/web/components/landing/Features.tsx existe et contient "360"
- apps/web/components/landing/Stats.tsx existe et contient "font-mono"
- apps/web/components/landing/Footer.tsx existe et contient "© 2026 Immo CI"
- apps/web/components/landing/CTAFinal.tsx existe et contient "/biens"
</acceptance_criteria>

---

### Tâche 4 : Page principale et assemblage

<read_first>
- apps/web/components/landing/ (tous les composants créés)
</read_first>

<action>
Créer `apps/web/app/(public)/page.tsx` :
```tsx
import { Hero } from '@/components/landing/Hero'
import { HowItWorks } from '@/components/landing/HowItWorks'
import { FeaturedProperties } from '@/components/landing/FeaturedProperties'
import { Features } from '@/components/landing/Features'
import { MapZones } from '@/components/landing/MapZones'
import { Testimonials } from '@/components/landing/Testimonials'
import { Stats } from '@/components/landing/Stats'
import { Partners } from '@/components/landing/Partners'
import { CTAFinal } from '@/components/landing/CTAFinal'
import { Footer } from '@/components/landing/Footer'

export default function HomePage() {
  return (
    <main>
      <Hero />
      <HowItWorks />
      <FeaturedProperties />
      <Features />
      <MapZones />
      <Testimonials />
      <Stats />
      <Partners />
      <CTAFinal />
      <Footer />
    </main>
  )
}
```
</action>

<acceptance_criteria>
- apps/web/app/(public)/page.tsx importe Hero
- apps/web/app/(public)/page.tsx importe Footer
- apps/web/app/(public)/page.tsx contient exactement 10 composants dans le JSX
</acceptance_criteria>

---

## Vérification finale

```bash
# 10 sections présentes
grep -c "import.*landing" apps/web/app/(public)/page.tsx  # doit afficher 10

# Hero contient CTA stores
grep "App Store" apps/web/components/landing/Hero.tsx

# SEO complet
grep "openGraph" apps/web/app/layout.tsx
test -f apps/web/app/sitemap.ts && echo "sitemap OK"
test -f apps/web/app/robots.ts && echo "robots OK"

# Playfair Display utilisé dans hero
grep "font-display" apps/web/components/landing/Hero.tsx

# Texte français
grep "Côte d'Ivoire" apps/web/components/landing/Hero.tsx
```
