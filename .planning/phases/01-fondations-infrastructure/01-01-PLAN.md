---
phase: 01-fondations-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - .gitignore
  - .env.local.example
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.js
  - apps/web/app/layout.tsx
  - apps/web/lib/env.ts
  - apps/mobile/package.json
  - apps/mobile/tsconfig.json
  - apps/mobile/app.json
  - apps/mobile/app/_layout.tsx
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/index.ts
  - packages/shared/types/index.ts
  - packages/shared/constants/communes.ts
  - packages/shared/utils/formatFCFA.ts
  - packages/shared/utils/formatDate.ts
autonomous: true
requirements:
  - FOND-01
  - FOND-02
  - FOND-03
  - FOND-04
  - FOND-06

must_haves:
  truths:
    - "La commande `npm run dev` depuis la racine démarre apps/web (port 3000) et apps/mobile sans erreur"
    - "Les imports depuis `@immo-ci/shared` fonctionnent dans apps/web"
    - "TypeScript strict mode est actif — `npm run type-check` passe sans erreur"
    - "Les variables d'environnement manquantes produisent une erreur explicite au démarrage (pas silencieuse)"
    - "L'app Next.js répond sur http://localhost:3000 avec une page blanche valide (pas de 500)"
  artifacts:
    - path: "package.json"
      provides: "Workspace root avec npm workspaces configuré"
      contains: "workspaces"
    - path: "turbo.json"
      provides: "Pipeline Turborepo (build, dev, lint, type-check)"
      contains: "dependsOn"
    - path: "apps/web/package.json"
      provides: "Dépendances web avec next@14.2.35 verrouillé"
      contains: "14.2.35"
    - path: "apps/web/lib/env.ts"
      provides: "Validation env vars avec zod au démarrage"
      exports: ["env"]
    - path: "packages/shared/index.ts"
      provides: "Point d'entrée du package partagé"
      exports: ["formatFCFA", "formatDate", "COMMUNES_CI"]
    - path: "packages/shared/utils/formatFCFA.ts"
      provides: "Formatteur FCFA (ex: 250 000 FCFA)"
      exports: ["formatFCFA"]
  key_links:
    - from: "apps/web/package.json"
      to: "packages/shared"
      via: "workspace dependency @immo-ci/shared"
      pattern: "@immo-ci/shared"
    - from: "apps/mobile/package.json"
      to: "packages/shared"
      via: "workspace dependency @immo-ci/shared"
      pattern: "@immo-ci/shared"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/lib/env.ts"
      via: "import env au module-level (fail fast)"
      pattern: "from.*lib/env"
---

<objective>
Initialiser le monorepo Turborepo complet avec Next.js 14.2.35 (apps/web), Expo SDK 52 (apps/mobile), et le package partagé packages/shared contenant les types TypeScript, constantes et utilitaires.

Purpose: Toute la plateforme repose sur cette structure. Sans fondations correctes (versions verrouillées, TypeScript strict, validation env, résolution workspace), chaque plan ultérieur est risqué.
Output: Monorepo fonctionnel, `npm run dev` démarre web + mobile, imports @immo-ci/shared résolus, env vars validées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills.md
@.planning/phases/01-fondations-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Initialiser le monorepo Turborepo et apps/web Next.js 14</name>

  <read_first>
    - skills.md section 2 (structure complète du projet)
    - skills.md section 11 (variables d'environnement)
    - .planning/phases/01-fondations-infrastructure/01-RESEARCH.md (Pattern 1, turbo.json minimal, validation env vars)
  </read_first>

  <files>
    package.json, turbo.json, .gitignore, .env.local.example,
    apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts,
    apps/web/postcss.config.js, apps/web/app/layout.tsx, apps/web/lib/env.ts
  </files>

  <action>
Créer le monorepo depuis la racine `C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo` (le projet immo-ci est le dossier courant — ne pas créer un sous-dossier).

**1. package.json racine (workspace root)**
```json
{
  "name": "immo-ci",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "turbo": "2.9.3",
    "typescript": "6.0.2",
    "prettier": "^3.0.0"
  }
}
```

**2. turbo.json**
Utiliser exactement le turbo.json minimal de la RESEARCH.md section "Code Examples" :
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": { "cache": false, "persistent": true },
    "lint": {},
    "type-check": { "dependsOn": ["^build"] }
  }
}
```

**3. .gitignore**
Inclure : `node_modules/`, `.next/`, `.env.local`, `dist/`, `.turbo/`, `*.tsbuildinfo`

**4. .env.local.example**
Copier exactement la liste complète des variables de skills.md section 11, avec des valeurs placeholder `=CHANGE_ME`. Ajouter un commentaire en tête : `# Copier ce fichier en .env.local et renseigner les valeurs réelles`.

**5. apps/web/ — créer le dossier et ses fichiers**

`apps/web/package.json` — CRITIQUE : verrouiller `"next": "14.2.35"` (pas `^14`, pas `latest`).
```json
{
  "name": "@immo-ci/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@supabase/supabase-js": "2.101.1",
    "@supabase/ssr": "0.10.0",
    "zod": "4.3.6",
    "react-hook-form": "7.72.1",
    "date-fns": "4.1.0",
    "@immo-ci/shared": "*"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "tailwindcss": "^3.4.17",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0"
  }
}
```

`apps/web/tsconfig.json` — TypeScript strict obligatoire :
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

`apps/web/next.config.ts` :
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  transpilePackages: ['@immo-ci/shared'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'res.cloudinary.com' },
      { protocol: 'https', hostname: '*.supabase.co' },
    ],
  },
}

export default nextConfig
```

`apps/web/postcss.config.js` — REQUIS pour Tailwind v3 (v4 ne le requiert pas — preuve que v3 est bien installé) :
```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

`apps/web/lib/env.ts` — Validation env vars avec zod (pattern RESEARCH.md) :
```typescript
import { z } from 'zod'

const envSchema = z.object({
  NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
  NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
  SUPABASE_SERVICE_ROLE_KEY: z.string().min(1),
  NEXT_PUBLIC_URL: z.string().url(),
  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),
})

export const env = envSchema.parse(process.env)
```

`apps/web/app/layout.tsx` — Root layout avec next/font/google (jamais @import CSS) :
```typescript
import type { Metadata } from 'next'
import { Playfair_Display, DM_Sans, JetBrains_Mono } from 'next/font/google'
import './globals.css'

const playfair = Playfair_Display({
  subsets: ['latin'],
  weight: ['400', '600', '700'],
  variable: '--font-display',
  display: 'swap',
})
const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500'],
  variable: '--font-sans',
  display: 'swap',
})
const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  weight: ['400', '500'],
  variable: '--font-mono',
  display: 'swap',
})

export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_URL ?? 'https://immo-ci.vercel.app'),
  title: { template: '%s | Immo CI', default: "Immo CI — Immobilier en Côte d'Ivoire" },
  description: "Trouvez votre bien immobilier en Côte d'Ivoire. Location, vente, résidences meublées à Abidjan et partout en CI.",
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr" className={`${playfair.variable} ${dmSans.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans bg-surface text-text antialiased">
        {children}
      </body>
    </html>
  )
}
```

Créer aussi `apps/web/app/globals.css` avec les CSS variables de skills.md section 3 (palette complète) et `apps/web/app/page.tsx` minimaliste (retourne `<main><h1>Immo CI</h1></main>`).

Installer les dépendances depuis la racine : `npm install`
  </action>

  <verify>
    <automated>cd "C:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo" && node -e "const pkg = require('./apps/web/package.json'); if (pkg.dependencies.next !== '14.2.35') throw new Error('next version incorrecte: ' + pkg.dependencies.next); console.log('OK next@14.2.35')"</automated>
  </verify>

  <acceptance_criteria>
    - apps/web/package.json contient `"next": "14.2.35"` (exactement, sans ^ ni ~)
    - apps/web/postcss.config.js existe (preuve Tailwind v3)
    - apps/web/tsconfig.json contient `"strict": true`
    - apps/web/lib/env.ts exporte `env` avec validation zod
    - apps/web/app/layout.tsx importe depuis `next/font/google` (pas @import CSS)
    - turbo.json contient les 4 tâches : build, dev, lint, type-check
    - .env.local.example liste toutes les variables de skills.md section 11
  </acceptance_criteria>

  <done>App web scaffoldée avec versions verrouillées, TypeScript strict, validation env, fonts Google via next/font</done>
</task>

<task type="auto">
  <name>Task 2 : Initialiser apps/mobile (Expo SDK 52) et packages/shared</name>

  <read_first>
    - skills.md section 2 (structure apps/mobile et packages/shared)
    - .planning/phases/01-fondations-infrastructure/01-RESEARCH.md (Pitfall 3 Expo SDK, Pattern turbo)
    - skills.md section 11 (constantes CI : communes, types biens)
  </read_first>

  <files>
    apps/mobile/package.json, apps/mobile/tsconfig.json, apps/mobile/app.json,
    apps/mobile/app/_layout.tsx,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/index.ts,
    packages/shared/types/index.ts, packages/shared/constants/communes.ts,
    packages/shared/utils/formatFCFA.ts, packages/shared/utils/formatDate.ts
  </files>

  <action>
**1. apps/mobile/package.json** — Expo SDK 52 (auto-détection monorepo, élimine config Metro manuelle) :
```json
{
  "name": "@immo-ci/mobile",
  "version": "0.1.0",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "~52.0.0",
    "expo-router": "~4.0.0",
    "expo-status-bar": "~2.0.0",
    "react": "18.3.1",
    "react-native": "0.76.0",
    "@supabase/supabase-js": "2.101.1",
    "@immo-ci/shared": "*"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "typescript": "6.0.2"
  }
}
```

`apps/mobile/app.json` :
```json
{
  "expo": {
    "name": "Immo CI",
    "slug": "immo-ci",
    "version": "1.0.0",
    "scheme": "immo-ci",
    "orientation": "portrait",
    "userInterfaceStyle": "light",
    "ios": { "supportsTablet": true, "bundleIdentifier": "ci.immo.app" },
    "android": { "adaptiveIcon": { "backgroundColor": "#1A5276" }, "package": "ci.immo.app" },
    "plugins": ["expo-router"]
  }
}
```

`apps/mobile/tsconfig.json` :
```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": { "@/*": ["./*"] }
  }
}
```

`apps/mobile/app/_layout.tsx` — Root layout Expo Router minimal :
```tsx
import { Stack } from 'expo-router'

export default function RootLayout() {
  return <Stack screenOptions={{ headerShown: false }} />
}
```

**2. packages/shared/**

`packages/shared/package.json` :
```json
{
  "name": "@immo-ci/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "6.0.2"
  }
}
```

`packages/shared/tsconfig.json` :
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["ES2017"],
    "strict": true,
    "esModuleInterop": true,
    "moduleResolution": "node",
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules"]
}
```

`packages/shared/constants/communes.ts` — Communes et quartiers CI (données de skills.md section 8) :
```typescript
export const COMMUNES_ABIDJAN = [
  'Cocody', 'Plateau', 'Marcory', 'Treichville', 'Adjamé',
  'Yopougon', 'Abobo', 'Koumassi', 'Port-Bouet', 'Bingerville',
  'Attécoubé', 'Songon',
] as const

export type CommuneAbidjan = typeof COMMUNES_ABIDJAN[number]

export const QUARTIERS_PREMIUM = [
  'Riviera Faya', 'Riviera Golf', 'Palmeraie', 'Cocody II Plateaux',
  'Angré', 'Deux Plateaux Vallon', 'Riviera 3', 'Riviera Bonoumin',
] as const

export const TYPES_BIEN = [
  'studio', 'appartement', 'villa', 'maison', 'bureau',
  'commerce', 'terrain', 'residence_meublee',
] as const

export type TypeBien = typeof TYPES_BIEN[number]

export const ROLES_UTILISATEUR = ['locataire', 'proprietaire', 'agence', 'admin'] as const
export type RoleUtilisateur = typeof ROLES_UTILISATEUR[number]
```

`packages/shared/utils/formatFCFA.ts` :
```typescript
/**
 * Formate un montant en FCFA avec séparateur de milliers.
 * Exemple : formatFCFA(250000) → "250 000 FCFA"
 * Exemple : formatFCFA(1500000) → "1 500 000 FCFA"
 */
export function formatFCFA(montant: number): string {
  return new Intl.NumberFormat('fr-CI', {
    style: 'decimal',
    maximumFractionDigits: 0,
  }).format(montant) + ' FCFA'
}

/**
 * Convertit un montant en lettres (simplifié pour usage contrats).
 * Exemple : montantEnLettres(250000) → "deux cent cinquante mille francs CFA"
 * Note : Implémentation simplifiée — couvre jusqu'à 999 999 999 FCFA
 */
export function montantEnLettres(montant: number): string {
  // Implémentation basique — à enrichir si nécessaire
  // Couverture : 0 à 999 999 999
  const unites = ['', 'un', 'deux', 'trois', 'quatre', 'cinq', 'six', 'sept', 'huit', 'neuf',
    'dix', 'onze', 'douze', 'treize', 'quatorze', 'quinze', 'seize', 'dix-sept', 'dix-huit', 'dix-neuf']
  const dizaines = ['', '', 'vingt', 'trente', 'quarante', 'cinquante', 'soixante', 'soixante', 'quatre-vingt', 'quatre-vingt']

  function centToWords(n: number): string {
    if (n === 0) return ''
    if (n < 20) return unites[n]
    const d = Math.floor(n / 10), u = n % 10
    if (d === 7) return `soixante-${unites[10 + u]}`
    if (d === 9) return u === 0 ? 'quatre-vingt' : `quatre-vingt-${unites[u]}`
    return u === 0 ? dizaines[d] : `${dizaines[d]}-${unites[u]}`
  }

  if (montant === 0) return 'zéro franc CFA'
  const milliards = Math.floor(montant / 1_000_000_000)
  const millions = Math.floor((montant % 1_000_000_000) / 1_000_000)
  const milliers = Math.floor((montant % 1_000_000) / 1_000)
  const reste = montant % 1_000
  const parts: string[] = []
  if (milliards > 0) parts.push(`${centToWords(milliards)} milliard${milliards > 1 ? 's' : ''}`)
  if (millions > 0) parts.push(`${centToWords(millions)} million${millions > 1 ? 's' : ''}`)
  if (milliers > 0) parts.push(`${centToWords(milliers)} mille`)
  if (reste > 0) parts.push(centToWords(reste))
  return parts.join(' ') + ' francs CFA'
}
```

`packages/shared/utils/formatDate.ts` :
```typescript
import { format, formatDistanceToNow } from 'date-fns'
import { fr } from 'date-fns/locale'

export function formatDateCI(date: Date | string): string {
  const d = typeof date === 'string' ? new Date(date) : date
  return format(d, 'dd MMMM yyyy', { locale: fr })
}

export function formatDateRelative(date: Date | string): string {
  const d = typeof date === 'string' ? new Date(date) : date
  return formatDistanceToNow(d, { addSuffix: true, locale: fr })
}
```

NOTE : date-fns est une dep de apps/web. Pour packages/shared, utiliser uniquement si date-fns est aussi listée comme dep de shared. Sinon, retirer l'import date-fns de shared et garder uniquement les fonctions format simples avec Intl.DateTimeFormat.

`packages/shared/types/index.ts` — Types de base (les types complets seront générés par Supabase CLI dans 01-02) :
```typescript
export type { CommuneAbidjan, TypeBien, RoleUtilisateur } from '../constants/communes'

// Types de base plateforme (types complets dans database.ts — généré par Supabase CLI en plan 01-02)
export interface PaginatedResponse<T> {
  data: T[]
  total: number
  page: number
  pageSize: number
}

export interface ApiError {
  message: string
  code?: string
}
```

`packages/shared/index.ts` — Point d'entrée unique :
```typescript
export * from './types'
export * from './constants/communes'
export * from './utils/formatFCFA'
export * from './utils/formatDate'
```

Finaliser : depuis la racine, exécuter `npm install` pour résoudre les workspaces.
  </action>

  <verify>
    <automated>cd "C:/Users/WILFRIED/OneDrive - Gravel Ivoire/Bureau/immo" && node -e "const pkg = require('./apps/mobile/package.json'); const expoVer = pkg.dependencies.expo; if (!expoVer.includes('52')) throw new Error('Expo SDK incorrecte: ' + expoVer); console.log('OK Expo SDK 52:', expoVer)" && node -e "require('./packages/shared/index.ts'); console.log('OK shared package resolves')" 2>/dev/null || echo 'Shared (TS check only — pas de ts-node. OK si fichiers existent.'</automated>
  </verify>

  <acceptance_criteria>
    - apps/mobile/package.json contient expo `~52.0.0`
    - packages/shared/index.ts exporte formatFCFA, formatDate, COMMUNES_ABIDJAN, TYPES_BIEN
    - packages/shared/utils/formatFCFA.ts contient la fonction montantEnLettres (requis pour les contrats OHADA)
    - packages/shared/constants/communes.ts liste les 12 communes d'Abidjan
    - packages/shared/package.json utilise `"name": "@immo-ci/shared"`
    - grep -r "@immo-ci/shared" apps/web/package.json retourne une ligne
    - grep -r "@immo-ci/shared" apps/mobile/package.json retourne une ligne
  </acceptance_criteria>

  <done>Expo SDK 52 scaffoldé, packages/shared opérationnel avec formatFCFA, montantEnLettres, communes CI, types de base</done>
</task>

</tasks>

<verification>
Depuis la racine du projet :

```bash
# 1. Vérifier la version Next.js verrouillée
node -e "const p=require('./apps/web/package.json'); console.log('next:', p.dependencies.next)" # doit afficher 14.2.35

# 2. Vérifier Expo SDK 52
node -e "const p=require('./apps/mobile/package.json'); console.log('expo:', p.dependencies.expo)" # doit afficher ~52.0.0

# 3. Vérifier Tailwind v3 (postcss.config.js doit exister)
test -f apps/web/postcss.config.js && echo "postcss.config.js OK" || echo "MISSING"

# 4. Vérifier TypeScript strict
node -e "const t=require('./apps/web/tsconfig.json'); console.log('strict:', t.compilerOptions.strict)" # doit afficher true

# 5. Vérifier que next/font/google est utilisé (pas @import)
grep -r "next/font/google" apps/web/app/layout.tsx && echo "fonts OK"
grep -r "@import.*fonts.googleapis" apps/web/app/layout.tsx && echo "ERREUR: @import détecté" || echo "Pas de @import CSS: OK"

# 6. Installer et vérifier que le workspace résout @immo-ci/shared
npm install && echo "npm install OK"
```
</verification>

<success_criteria>
- `npm run dev` démarre sans erreur (web sur port 3000)
- `next@14.2.35` exactement dans apps/web/package.json
- `expo@~52.0.0` dans apps/mobile/package.json
- `tailwindcss@^3.4.17` dans apps/web/package.json (pas v4)
- `"strict": true` dans apps/web/tsconfig.json et packages/shared/tsconfig.json
- `apps/web/lib/env.ts` exporte `env` validé par zod
- `packages/shared` exporte formatFCFA, montantEnLettres, COMMUNES_ABIDJAN
- `apps/web/app/layout.tsx` utilise `next/font/google` (zéro @import CSS)
- `postcss.config.js` existe dans apps/web (requis par Tailwind v3)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations-infrastructure/01-01-SUMMARY.md` avec :
- Versions exactes installées (next, expo, turbo, tailwindcss, @supabase/ssr)
- Structure des workspaces confirmée
- Exports de packages/shared
- Toute décision ou ajustement fait durant l'exécution
</output>
