---
phase: 05-app-mobile-tests-d-ploiement
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - supabase/functions/send-push/index.ts
  - apps/mobile/hooks/usePushNotifications.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/notifications.tsx
autonomous: true
requirements: [MOB-04]

must_haves:
  truths:
    - "L'app demande la permission de notification au premier lancement sur device physique"
    - "Le token FCM est sauvegardé dans profiles.fcm_token après consentement"
    - "L'Edge Function send-push accepte un user_id et envoie la notification via Expo Push Service (exp.host)"
    - "Une notification reçue affiche titre et body sur l'appareil"
    - "Le deep link depuis la notification navigue vers l'écran approprié (bien/[id] ou reservations)"
  artifacts:
    - path: "supabase/functions/send-push/index.ts"
      provides: "Edge Function Deno envoi push FCM V1"
      exports: ["Deno.serve handler"]
    - path: "apps/mobile/hooks/usePushNotifications.ts"
      provides: "Hook registration FCM token + listener notifications"
      exports: ["usePushNotifications"]
    - path: "apps/mobile/app/notifications.tsx"
      provides: "Écran gestionnaire deep links notifications"
  key_links:
    - from: "apps/mobile/hooks/usePushNotifications.ts"
      to: "supabase (table profiles)"
      via: "supabase.from('profiles').update({ fcm_token })"
      pattern: "update.*fcm_token"
    - from: "supabase/functions/send-push/index.ts"
      to: "Expo Push Service"
      via: "fetch https://exp.host/--/api/v2/push/send"
      pattern: "exp.host.*push"
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/hooks/usePushNotifications.ts"
      via: "usePushNotifications() appelé dans RootLayout après auth"
      pattern: "usePushNotifications"
---

<objective>
Implémente les notifications push Firebase FCM de bout en bout : hook expo-notifications pour enregistrer le token sur device physique, sauvegarde dans `profiles.fcm_token`, et Edge Function Deno qui appelle FCM V1 API avec JWT Google pour envoyer la notification. Les deep links redirigent vers le bon écran.

Purpose: MOB-04 — les utilisateurs reçoivent des alertes push pour nouvelles réservations, paiements confirmés et messages (fonctionnalité différenciante vs concurrents CI).
Output: Edge Function `send-push` déployable, hook `usePushNotifications`, gestion des notifications entrants avec deep links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-app-mobile-tests-d-ploiement/05-RESEARCH.md

<interfaces>
<!-- Pattern Edge Function Deno existant dans le projet -->
<!-- Référence: supabase/functions/generer-quittance/index.ts -->

Pattern Edge Function Deno (même structure que les fonctions existantes):
```typescript
// supabase/functions/send-push/index.ts
import { createClient } from 'npm:@supabase/supabase-js@2'
import { JWT } from 'npm:google-auth-library@9'

// Le service account Firebase est attendu comme secret Supabase:
// supabase secrets set FIREBASE_SERVICE_ACCOUNT='{"type":"service_account",...}'
const serviceAccount = JSON.parse(Deno.env.get('FIREBASE_SERVICE_ACCOUNT')!)

Deno.serve(async (req) => {
  // CORS headers pour appels depuis mobile/web
  if (req.method === 'OPTIONS') {
    return new Response('ok', {
      headers: {
        'Access-Control-Allow-Origin': '*',
        'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
      },
    })
  }
  // ...
})
```

Payload attendu par send-push:
```typescript
interface SendPushPayload {
  user_id: string        // ID Supabase de l'utilisateur cible
  title: string          // Titre de la notification
  body: string           // Corps du message
  data?: {               // Données pour deep link (optionnel)
    lien_type?: 'bien' | 'reservation' | 'message'
    lien_id?: string
  }
}
```

expo-notifications API (SDK 52):
```typescript
import * as Notifications from 'expo-notifications'
import * as Device from 'expo-device'
import Constants from 'expo-constants'

// Configuration du handler AVANT tout autre code notifications
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
  }),
})

// Récupérer le token Expo Push (wraps FCM sur Android, APNs sur iOS)
const tokenData = await Notifications.getExpoPushTokenAsync({
  projectId: Constants.expoConfig?.extra?.eas?.projectId,
})
// tokenData.data = "ExponentPushToken[xxxxxx]" — c'est ce qu'on stocke dans fcm_token

// IMPORTANT: getExpoPushTokenAsync retourne null sur simulateur — toujours tester sur device physique
if (!Device.isDevice) {
  console.warn('Push notifications non supportées sur simulateur')
  return
}
```

Gestion des deep links depuis notification:
```typescript
// Dans usePushNotifications.ts — listener sur tap notification
const subscription = Notifications.addNotificationResponseReceivedListener((response) => {
  const data = response.notification.request.content.data as {
    lien_type?: string
    lien_id?: string
  }
  if (data.lien_type === 'bien' && data.lien_id) {
    router.push(`/bien/${data.lien_id}`)
  } else if (data.lien_type === 'reservation') {
    router.push('/(tabs)/reservations')
  }
})
```

Note sur fcm_token: On stocke le token Expo Push (pas le token FCM brut) dans profiles.fcm_token.
EAS/Expo gère la conversion vers FCM/APNs automatiquement via expo-notifications.
Si l'on veut envoyer directement via FCM V1 API (sans passer par Expo Push Service),
il faut récupérer le token FCM natif via expo-notifications avec getDevicePushTokenAsync().
Pour la v1, utiliser Expo Push Service est plus simple et recommandé.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edge Function send-push + Hook usePushNotifications</name>
  <files>
    supabase/functions/send-push/index.ts,
    apps/mobile/hooks/usePushNotifications.ts
  </files>
  <read_first>
    supabase/functions/generer-quittance/index.ts,
    apps/mobile/lib/supabase.ts,
    apps/mobile/hooks/useAuth.ts
  </read_first>
  <action>
**Étape 1 — Installer les dépendances expo-notifications:**
```bash
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile"
npx expo install expo-notifications expo-device expo-constants
```

**Étape 2 — Edge Function Deno (`supabase/functions/send-push/index.ts`):**

Cette fonction utilise Expo Push Service (plus simple que FCM V1 direct pour v1). Elle lit le `fcm_token` (en réalité Expo Push Token) stocké dans `profiles` et envoie via l'API Expo Push.

```typescript
// supabase/functions/send-push/index.ts
// Pattern: Expo Push Service (wrapper FCM/APNs géré par Expo)
// Alternative FCM V1 directe: nécessite service account Google — configuré en v2
import { createClient } from 'npm:@supabase/supabase-js@2'

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

interface SendPushPayload {
  user_id: string
  title: string
  body: string
  data?: {
    lien_type?: 'bien' | 'reservation' | 'message'
    lien_id?: string
  }
}

Deno.serve(async (req: Request) => {
  // CORS preflight
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    const { user_id, title, body, data }: SendPushPayload = await req.json()

    if (!user_id || !title || !body) {
      return new Response(
        JSON.stringify({ error: 'user_id, title et body sont requis' }),
        { status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
      )
    }

    // Client Supabase avec service role pour lire le fcm_token
    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    )

    // Récupérer le token push depuis profiles
    const { data: profile, error: profileError } = await supabase
      .from('profiles')
      .select('fcm_token')
      .eq('id', user_id)
      .single()

    if (profileError || !profile?.fcm_token) {
      return new Response(
        JSON.stringify({ error: 'Token push introuvable pour cet utilisateur', detail: profileError?.message }),
        { status: 404, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
      )
    }

    // Envoyer via Expo Push Service
    // Le token est au format "ExponentPushToken[xxxxxx]"
    const expoPushPayload = {
      to: profile.fcm_token,
      sound: 'default',
      title,
      body,
      data: data ?? {},
    }

    const expoResponse = await fetch('https://exp.host/--/api/v2/push/send', {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Accept-Encoding': 'gzip, deflate',
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(expoPushPayload),
    })

    const expoResult = await expoResponse.json()

    // Log dans la table notifications si elle existe
    await supabase.from('notifications').insert({
      user_id,
      type: data?.type ?? 'reservation_nouvelle',  // NOT NULL dans migration 008
      titre: title,
      contenu: body,                                // colonne reelle: contenu (pas message)
      lien_type: data?.lien_type ?? null,
      lien_id: data?.lien_id ?? null,               // UUID ou null
      lu: false,
    }).then(() => {}).catch(() => {})
    // Non bloquant — l'absence de type ou un lien_id invalide retournerait une erreur PG

    return new Response(
      JSON.stringify({ success: true, expo_result: expoResult }),
      { status: 200, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    )
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Erreur inconnue'
    return new Response(
      JSON.stringify({ error: message }),
      { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    )
  }
})
```

**Étape 3 — Hook usePushNotifications (`apps/mobile/hooks/usePushNotifications.ts`):**
```typescript
import { useEffect, useRef } from 'react'
import * as Notifications from 'expo-notifications'
import * as Device from 'expo-device'
import Constants from 'expo-constants'
import { useRouter } from 'expo-router'
import { supabase } from '../lib/supabase'
import { useSession } from './useAuth'

// Configurer le handler en dehors du hook pour qu'il soit global
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
  }),
})

async function registerForPushNotificationsAsync(): Promise<string | null> {
  // Les simulateurs ne supportent pas les push notifications
  if (!Device.isDevice) {
    console.warn('[Push] Notifications push non disponibles sur simulateur/émulateur')
    return null
  }

  // Vérifier et demander la permission
  const { status: existingStatus } = await Notifications.getPermissionsAsync()
  let finalStatus = existingStatus

  if (existingStatus !== 'granted') {
    const { status } = await Notifications.requestPermissionsAsync()
    finalStatus = status
  }

  if (finalStatus !== 'granted') {
    console.warn('[Push] Permission refusée par l\'utilisateur')
    return null
  }

  // Récupérer l'Expo Push Token (nécessite EAS projectId dans app.json)
  const projectId = Constants.expoConfig?.extra?.eas?.projectId
  if (!projectId) {
    console.warn('[Push] EAS projectId manquant dans app.json extra.eas.projectId')
    return null
  }

  const tokenData = await Notifications.getExpoPushTokenAsync({ projectId })
  return tokenData.data  // format: "ExponentPushToken[xxxxxx]"
}

export function usePushNotifications() {
  const { session } = useSession()
  const router = useRouter()
  const notificationListener = useRef<Notifications.EventSubscription>()
  const responseListener = useRef<Notifications.EventSubscription>()

  useEffect(() => {
    // Enregistrer seulement si l'utilisateur est connecté
    if (!session?.user.id) return

    // Enregistrer le token et le sauvegarder dans profiles
    registerForPushNotificationsAsync().then(async (token) => {
      if (!token) return

      const { error } = await supabase
        .from('profiles')
        .update({ fcm_token: token })
        .eq('id', session.user.id)

      if (error) {
        console.error('[Push] Erreur sauvegarde token:', error.message)
      } else {
        console.log('[Push] Token enregistré:', token.slice(0, 30) + '...')
      }
    })

    // Listener: notification reçue pendant que l'app est au premier plan
    notificationListener.current = Notifications.addNotificationReceivedListener((notification) => {
      console.log('[Push] Notification reçue:', notification.request.content.title)
    })

    // Listener: utilisateur tape sur une notification (app en arrière-plan ou fermée)
    responseListener.current = Notifications.addNotificationResponseReceivedListener((response) => {
      const data = response.notification.request.content.data as {
        lien_type?: string
        lien_id?: string
      }

      // Deep link selon le type de notification
      if (data?.lien_type === 'bien' && data.lien_id) {
        router.push(`/bien/${data.lien_id}`)
      } else if (data?.lien_type === 'reservation') {
        router.push('/(tabs)/reservations')
      } else if (data?.lien_type === 'message') {
        router.push('/(tabs)/reservations')  // TODO: ouvrir la messagerie en v2
      }
    })

    // Nettoyer les listeners au démontage
    return () => {
      notificationListener.current?.remove()
      responseListener.current?.remove()
    }
  }, [session?.user.id])
}
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\supabase\functions\send-push\index.ts" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\hooks\usePushNotifications.ts" && echo "Fichiers créés" && grep "detectSessionInUrl\|fcm_token\|exp.host" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\supabase\functions\send-push\index.ts" && echo "Edge Function correcte"</automated>
  </verify>
  <done>Edge Function send-push via Expo Push Service, hook usePushNotifications avec registration + listeners deep links.</done>
</task>

<task type="auto">
  <name>Task 2: Intégration usePushNotifications dans _layout.tsx + écran notifications</name>
  <files>
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/notifications.tsx
  </files>
  <read_first>
    apps/mobile/app/_layout.tsx,
    apps/mobile/hooks/usePushNotifications.ts
  </read_first>
  <action>
**IMPORTANT:** Lire `apps/mobile/app/_layout.tsx` tel qu'il existe (créé par plan 05-01 ou minimal existant) et y intégrer le hook usePushNotifications. Ne pas réécrire entièrement le fichier — ajouter l'import et l'appel du hook dans le composant qui a accès à la session.

**Modifications de `apps/mobile/app/_layout.tsx`:**

Le fichier existant doit avoir cette structure (créé par 05-01). Modifier pour ajouter l'import et l'appel du hook dans un composant enfant qui a accès au contexte session:

```typescript
import { Stack } from 'expo-router'
import { StatusBar } from 'expo-status-bar'
import { SessionProvider, AuthGuard } from '../hooks/useAuth'
import { usePushNotifications } from '../hooks/usePushNotifications'  // AJOUTER

// Composant séparé pour utiliser usePushNotifications DANS le SessionProvider
// (le hook utilise useSession qui nécessite SessionProvider dans l'arbre)
function PushNotificationsHandler() {
  usePushNotifications()  // S'enregistre automatiquement quand session active
  return null             // Pas de rendu — pure side effect
}

export default function RootLayout() {
  return (
    <SessionProvider>
      <PushNotificationsHandler />  {/* AJOUTER ici — dans SessionProvider */}
      <AuthGuard>
        <StatusBar style="auto" />
        <Stack screenOptions={{ headerShown: false }}>
          <Stack.Screen name="(tabs)" />
          <Stack.Screen name="(auth)" />
          <Stack.Screen
            name="bien/[id]"
            options={{
              headerShown: true,
              headerTitle: 'Détail du bien',
              headerTintColor: '#1A5276',
            }}
          />
          <Stack.Screen name="notifications" options={{ headerShown: true, headerTitle: 'Notifications' }} />
        </Stack>
      </AuthGuard>
    </SessionProvider>
  )
}
```

Si le fichier `_layout.tsx` existe déjà exactement comme ci-dessus depuis 05-01, uniquement:
1. Ajouter `import { usePushNotifications } from '../hooks/usePushNotifications'`
2. Ajouter le composant `PushNotificationsHandler` avant `RootLayout`
3. Ajouter `<PushNotificationsHandler />` en premier enfant de `<SessionProvider>`
4. Ajouter `<Stack.Screen name="notifications" .../>` dans la Stack

**Fichier 2 — Écran notifications (`apps/mobile/app/notifications.tsx`):**

Cet écran liste les notifications de l'utilisateur depuis la table `notifications` Supabase. Il sert aussi de destination de navigation depuis les deep links.

```typescript
import { useEffect, useState } from 'react'
import { View, Text, FlatList, StyleSheet, TouchableOpacity, ActivityIndicator } from 'react-native'
import { useRouter } from 'expo-router'
import { supabase } from '../lib/supabase'
import { useSession } from '../hooks/useAuth'
import { formatDate } from '@immo-ci/shared'
import { colors, spacing, typography, borderRadius } from '../constants/theme'

interface Notification {
  id: string
  titre: string
  contenu: string
  lien_type: string | null
  lien_id: string | null
  lu: boolean
  created_at: string
}

export default function NotificationsScreen() {
  const { session } = useSession()
  const router = useRouter()
  const [notifications, setNotifications] = useState<Notification[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    if (!session?.user.id) return

    supabase
      .from('notifications')
      .select('*')
      .eq('user_id', session.user.id)
      .order('created_at', { ascending: false })
      .limit(50)
      .then(({ data }) => {
        if (data) setNotifications(data as Notification[])
        setLoading(false)
      })
  }, [session?.user.id])

  function handleTap(notif: Notification) {
    // Marquer comme lu
    supabase
      .from('notifications')
      .update({ lu: true })
      .eq('id', notif.id)
      .then(() => {
        setNotifications((prev) =>
          prev.map((n) => (n.id === notif.id ? { ...n, lu: true } : n))
        )
      })

    // Deep link selon le type
    if (notif.lien_type === 'bien' && notif.lien_id) {
      router.push(`/bien/${notif.lien_id}`)
    } else if (notif.lien_type === 'reservation') {
      router.push('/(tabs)/reservations')
    }
  }

  if (loading) {
    return <View style={styles.center}><ActivityIndicator color={colors.primary} /></View>
  }

  return (
    <FlatList
      data={notifications}
      keyExtractor={(item) => item.id}
      style={styles.container}
      contentContainerStyle={notifications.length === 0 ? styles.emptyContainer : undefined}
      renderItem={({ item }) => (
        <TouchableOpacity
          style={[styles.item, !item.lu && styles.itemUnread]}
          onPress={() => handleTap(item)}
          activeOpacity={0.8}
        >
          {!item.lu && <View style={styles.dot} />}
          <View style={styles.itemContent}>
            <Text style={styles.itemTitle}>{item.titre}</Text>
            <Text style={styles.itemBody} numberOfLines={2}>{item.contenu}</Text>
            <Text style={styles.itemDate}>{formatDate(item.created_at)}</Text>
          </View>
        </TouchableOpacity>
      )}
      ListEmptyComponent={
        <View style={styles.center}>
          <Text style={styles.emptyText}>Aucune notification</Text>
        </View>
      }
      ItemSeparatorComponent={() => <View style={styles.separator} />}
    />
  )
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  center: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  emptyContainer: { flex: 1 },
  item: {
    flexDirection: 'row',
    padding: spacing.md,
    backgroundColor: colors.white,
    alignItems: 'flex-start',
  },
  itemUnread: { backgroundColor: '#EBF5FB' },
  dot: {
    width: 8, height: 8, borderRadius: 4,
    backgroundColor: colors.primary,
    marginTop: 6, marginRight: spacing.sm,
  },
  itemContent: { flex: 1 },
  itemTitle: { ...typography.h3, color: colors.text, marginBottom: spacing.xs },
  itemBody: { ...typography.body, color: colors.textLight, marginBottom: spacing.xs },
  itemDate: { ...typography.caption, color: colors.textLight },
  separator: { height: 1, backgroundColor: colors.border },
  emptyText: { color: colors.textLight, fontSize: 15 },
})
```

**Déploiement de l'Edge Function (commande à exécuter manuellement):**
```bash
# Dans le terminal, depuis la racine du monorepo:
supabase functions deploy send-push --no-verify-jwt
# Note: --no-verify-jwt car la fonction sera appelée depuis d'autres Edge Functions (webhooks n8n)
# Pour appels client authentifiés, retirer ce flag
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\notifications.tsx" && grep "usePushNotifications\|PushNotificationsHandler" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\_layout.tsx" && echo "Hook intégré dans root layout"</automated>
  </verify>
  <done>usePushNotifications intégré dans _layout.tsx via PushNotificationsHandler, écran notifications avec deep links et marquage lu.</done>
</task>

</tasks>

<verification>
```bash
# 1. Edge Function contient l'appel Expo Push Service
grep "exp.host" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\supabase\functions\send-push\index.ts"

# 2. Hook contient le listener deep link
grep "addNotificationResponseReceivedListener" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\hooks\usePushNotifications.ts"

# 3. Hook sauvegarde le token dans profiles
grep "fcm_token" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\hooks\usePushNotifications.ts"

# 4. Root layout intègre PushNotificationsHandler
grep "PushNotificationsHandler" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\_layout.tsx"

# 5. Vérifier Device.isDevice guard dans le hook
grep "Device.isDevice" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\hooks\usePushNotifications.ts"

# 6. TypeScript check
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile" && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. `supabase/functions/send-push/index.ts` lit `profiles.fcm_token` et appelle `https://exp.host/--/api/v2/push/send`
2. `usePushNotifications` vérifie `Device.isDevice` avant de demander la permission
3. Le token Expo Push est sauvegardé dans `profiles.fcm_token` via Supabase UPDATE
4. `addNotificationResponseReceivedListener` gère les deep links `lien_type: 'bien'`, `'reservation'`, `'message'`
5. `PushNotificationsHandler` est un enfant direct de `SessionProvider` dans `_layout.tsx`
6. L'écran `notifications.tsx` affiche la liste depuis la table `notifications` et marque les items comme lus
7. `npx tsc --noEmit` passe sans erreurs sur les fichiers créés

**Note test manuel (device physique requis):**
- Installer l'app sur device via `eas build --profile development`
- Copier le token affiché dans les logs console `[Push] Token enregistré:`
- Tester via Expo Push Tool: https://expo.dev/notifications
</success_criteria>

<output>
After completion, create `.planning/phases/05-app-mobile-tests-d-ploiement/05-02-SUMMARY.md` documenting:
- Edge Function send-push deployed (yes/no) and any errors
- Token format confirmed (ExponentPushToken vs FCM native token)
- Packages installed (expo-notifications, expo-device, expo-constants)
- Deep link navigation tested (which routes work)
- Any deviation from the Expo Push Service approach (e.g., switched to FCM V1 direct)
</output>
