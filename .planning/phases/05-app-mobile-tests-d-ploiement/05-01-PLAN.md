---
phase: 05-app-mobile-tests-d-ploiement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/009_fcm_token.sql
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/favoris.tsx
  - apps/mobile/app/(tabs)/reservations.tsx
  - apps/mobile/app/(tabs)/profil.tsx
  - apps/mobile/app/(auth)/_layout.tsx
  - apps/mobile/app/(auth)/login.tsx
  - apps/mobile/app/(auth)/register.tsx
  - apps/mobile/app/bien/[id].tsx
  - apps/mobile/lib/supabase.ts
  - apps/mobile/hooks/useAuth.ts
  - apps/mobile/components/BienCard.tsx
  - apps/mobile/components/StatutBadge.tsx
  - apps/mobile/constants/theme.ts
  - apps/mobile/.env.example
autonomous: true
requirements: [MOB-01, MOB-02]

must_haves:
  truths:
    - "L'app démarre sans erreur et affiche l'écran login si non authentifié"
    - "Après connexion, les 4 onglets (Accueil, Favoris, Réservations, Profil) sont visibles"
    - "L'onglet Accueil affiche une FlatList de biens avec prix en FCFA"
    - "Taper sur une carte bien ouvre la fiche bien avec ScrollView"
    - "L'onglet Profil affiche le nom, rôle, statut KYC et un bouton Déconnexion"
    - "La déconnexion redirige vers l'écran login"
    - "Toutes les couleurs respectent la palette CI (primary #1A5276, secondary #E67E22)"
  artifacts:
    - path: "supabase/migrations/009_fcm_token.sql"
      provides: "Colonne fcm_token TEXT dans la table profiles"
      contains: "ALTER TABLE profiles ADD COLUMN fcm_token TEXT"
    - path: "apps/mobile/lib/supabase.ts"
      provides: "Client Supabase React Native"
      exports: ["supabase"]
    - path: "apps/mobile/hooks/useAuth.ts"
      provides: "Context auth avec session Supabase"
      exports: ["SessionProvider", "useSession"]
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Root layout avec AuthGuard"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Navigation par onglets"
    - path: "apps/mobile/components/BienCard.tsx"
      provides: "Carte bien React Native"
      exports: ["BienCard"]
    - path: "apps/mobile/components/StatutBadge.tsx"
      provides: "Badge statut coloré"
      exports: ["StatutBadge"]
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/hooks/useAuth.ts"
      via: "SessionProvider wrapping AuthGuard"
      pattern: "SessionProvider"
    - from: "apps/mobile/hooks/useAuth.ts"
      to: "apps/mobile/lib/supabase.ts"
      via: "supabase.auth.onAuthStateChange"
      pattern: "onAuthStateChange"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "supabase (table biens)"
      via: "supabase.from('biens').select()"
      pattern: "supabase.from\\('biens'\\)"
    - from: "apps/mobile/app/(tabs)/profil.tsx"
      to: "supabase.auth.signOut()"
      via: "bouton Déconnexion"
      pattern: "signOut"
---

<objective>
Scaffolde l'app Expo complète : migration BDD pour FCM, client Supabase React Native correctement configuré, hook d'authentification avec garde de navigation, structure de navigation Expo Router (onglets + auth + bien détail), et tous les écrans principaux avec StyleSheet natif CI.

Purpose: C'est la fondation de l'app mobile — sans cette navigation et ces écrans, les plans 05-02, 05-03, 05-04 n'ont rien à tester ni à déployer.
Output: App Expo navigable avec 4 onglets, 2 écrans auth, 1 fiche bien, composants BienCard et StatutBadge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-app-mobile-tests-d-ploiement/05-RESEARCH.md

<interfaces>
<!-- Types disponibles depuis @immo-ci/shared — import direct sans réécriture -->

From packages/shared/index.ts (exports tout):
```typescript
// Types (packages/shared/types/)
export type { Database, Bien, Profile, Reservation, Avis, KycStatut, ReservationStatut }

// Constantes (packages/shared/constants/)
export const COMMUNES_CI: string[]  // ex: ['Cocody', 'Plateau', 'Marcory', ...]
export const TYPES_BIEN: string[]   // ex: ['appartement', 'villa', 'studio', ...]

// Utils (packages/shared/utils/)
export function formatFCFA(amount: number): string  // "1 500 000 FCFA"
export function formatDate(date: string | Date): string  // "07 avr. 2026"
```

Types clés à utiliser dans les écrans:
```typescript
interface Bien {
  id: string
  titre: string
  prix: number
  commune: string
  type_bien: string
  statut: 'publie' | 'brouillon' | 'inactif'
  // surface, nb_pieces, description, etc.
}

interface Profile {
  id: string
  nom: string
  prenom: string
  email: string
  role: 'proprietaire' | 'locataire' | 'agence'
  kyc_statut: 'non_soumis' | 'en_cours' | 'approuve' | 'rejete'
  avatar_url: string | null
  fcm_token: string | null  // ajouté par migration 009
}

type ReservationStatut = 'en_attente' | 'confirmee' | 'annulee' | 'terminee'
```

Pattern Supabase RN correct (OBLIGATOIRE — différent du client web):
```typescript
// lib/supabase.ts — ordre d'import CRITIQUE
import 'react-native-url-polyfill/auto'       // 1er import absolu
import 'expo-sqlite/localStorage/install'      // 2ème — installe localStorage polyfill
import { createClient } from '@supabase/supabase-js'

export const supabase = createClient(
  process.env.EXPO_PUBLIC_SUPABASE_URL!,
  process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!,
  {
    auth: {
      storage: localStorage,
      autoRefreshToken: true,
      persistSession: true,
      detectSessionInUrl: false,  // OBLIGATOIRE — crash sinon en RN
    },
  }
)
```

Pattern auth guard (useSegments — compatible SDK 52, PAS Stack.Protected):
```typescript
// hooks/useAuth.ts
import { createContext, useContext, useEffect, useState } from 'react'
import { useRouter, useSegments } from 'expo-router'
import { Session } from '@supabase/supabase-js'
import { supabase } from '../lib/supabase'

interface SessionContextType {
  session: Session | null
  isLoading: boolean
}
const SessionContext = createContext<SessionContextType>({ session: null, isLoading: true })

export function SessionProvider({ children }: { children: React.ReactNode }) {
  const [session, setSession] = useState<Session | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session)
      setIsLoading(false)
    })
    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session)
    })
    return () => subscription.unsubscribe()
  }, [])

  return <SessionContext.Provider value={{ session, isLoading }}>{children}</SessionContext.Provider>
}

export function useSession() {
  return useContext(SessionContext)
}

// Dans _layout.tsx — AuthGuard component
function AuthGuard({ children }: { children: React.ReactNode }) {
  const { session, isLoading } = useSession()
  const segments = useSegments()
  const router = useRouter()

  useEffect(() => {
    if (isLoading) return
    const inAuthGroup = segments[0] === '(auth)'
    if (!session && !inAuthGroup) router.replace('/(auth)/login')
    else if (session && inAuthGroup) router.replace('/(tabs)')
  }, [session, isLoading, segments])

  if (isLoading) return null
  return <>{children}</>
}
```

Palette couleurs CI (StyleSheet uniquement — PAS NativeWind):
```typescript
// constants/theme.ts
export const colors = {
  primary: '#1A5276',    // bleu CI
  secondary: '#E67E22',  // orange CI
  accent: '#27AE60',     // vert
  danger: '#E74C3C',     // rouge
  text: '#2C3E50',
  textLight: '#8E9EAB',
  background: '#F8F9FA',
  white: '#FFFFFF',
  border: '#D5D8DC',
}
```

Pattern fiche bien (navigation depuis FlatList):
```typescript
// Dans BienCard.tsx
import { useRouter } from 'expo-router'
const router = useRouter()
// onPress: router.push(`/bien/${bien.id}`)

// Dans app/bien/[id].tsx
import { useLocalSearchParams } from 'expo-router'
const { id } = useLocalSearchParams<{ id: string }>()
```

Dépendances à installer AVANT d'écrire le code:
```bash
cd apps/mobile
npx expo install expo-sqlite expo-secure-store expo-image react-native-url-polyfill
npx expo install react-native-safe-area-context react-native-screens
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 009 + Client Supabase RN + Hook useAuth</name>
  <files>
    supabase/migrations/009_fcm_token.sql,
    apps/mobile/lib/supabase.ts,
    apps/mobile/hooks/useAuth.ts,
    apps/mobile/constants/theme.ts,
    apps/mobile/.env.example
  </files>
  <read_first>
    supabase/migrations/008_kyc.sql (ou dernière migration pour voir le pattern),
    apps/mobile/package.json
  </read_first>
  <action>
**Étape 1 — Installer les dépendances manquantes:**
```bash
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile"
npx expo install expo-sqlite expo-secure-store expo-image react-native-url-polyfill
npx expo install react-native-safe-area-context react-native-screens
```

**Étape 2 — Migration SQL 009:**
Créer `supabase/migrations/009_fcm_token.sql` :
```sql
-- Migration 009: Ajout colonne fcm_token pour notifications push Firebase FCM
-- Dépend de: 008 (table profiles existe)

ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS fcm_token TEXT;

-- Permettre à l'utilisateur de mettre à jour son propre fcm_token
-- La policy UPDATE sur profiles existe déjà — elle couvre toutes les colonnes.
-- Vérifier qu'elle inclut fcm_token (pas de restriction de colonnes dans les policies existantes).

COMMENT ON COLUMN profiles.fcm_token IS 'Token Firebase FCM pour notifications push. Enregistré par expo-notifications au démarrage de l''app.';
```

**Étape 3 — Client Supabase RN (`apps/mobile/lib/supabase.ts`):**
CRITIQUE: L'ordre des imports est obligatoire — sinon crash "URL is not defined" au runtime Hermes.
```typescript
import 'react-native-url-polyfill/auto'        // PREMIER import — polyfill URL API pour Hermes
import 'expo-sqlite/localStorage/install'       // DEUXIÈME — installe localStorage comme polyfill AsyncStorage
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@immo-ci/shared'

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!

if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error(
    'Variables EXPO_PUBLIC_SUPABASE_URL et EXPO_PUBLIC_SUPABASE_ANON_KEY manquantes dans apps/mobile/.env'
  )
}

export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: localStorage,       // expo-sqlite/localStorage installé ci-dessus
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,   // OBLIGATOIRE en React Native — pas de browser URL
  },
})
```

**Étape 4 — Hook useAuth (`apps/mobile/hooks/useAuth.ts`):**
```typescript
import { createContext, useContext, useEffect, useState, ReactNode } from 'react'
import { useRouter, useSegments } from 'expo-router'
import { Session } from '@supabase/supabase-js'
import { supabase } from '../lib/supabase'

interface SessionContextValue {
  session: Session | null
  isLoading: boolean
}

const SessionContext = createContext<SessionContextValue>({
  session: null,
  isLoading: true,
})

export function SessionProvider({ children }: { children: ReactNode }) {
  const [session, setSession] = useState<Session | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    // Charger la session existante (depuis expo-sqlite localStorage)
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session)
      setIsLoading(false)
    })

    // Écouter les changements d'état auth (login, logout, refresh token)
    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session)
    })

    return () => subscription.unsubscribe()
  }, [])

  return (
    <SessionContext.Provider value={{ session, isLoading }}>
      {children}
    </SessionContext.Provider>
  )
}

export function useSession() {
  const context = useContext(SessionContext)
  if (!context) {
    throw new Error('useSession doit être utilisé dans un SessionProvider')
  }
  return context
}

// AuthGuard — à utiliser dans app/_layout.tsx
// Redirige vers login si pas de session, vers tabs si déjà connecté
export function AuthGuard({ children }: { children: ReactNode }) {
  const { session, isLoading } = useSession()
  const segments = useSegments()
  const router = useRouter()

  useEffect(() => {
    if (isLoading) return

    const inAuthGroup = segments[0] === '(auth)'

    if (!session && !inAuthGroup) {
      // Pas authentifié et pas sur un écran auth → rediriger vers login
      router.replace('/(auth)/login')
    } else if (session && inAuthGroup) {
      // Authentifié mais sur un écran auth → rediriger vers les onglets
      router.replace('/(tabs)')
    }
  }, [session, isLoading, segments])

  // Pendant le chargement, ne rien afficher (splash screen reste visible)
  if (isLoading) return null

  return <>{children}</>
}
```

**Étape 5 — Constantes thème (`apps/mobile/constants/theme.ts`):**
```typescript
// Palette officielle Immo CI — StyleSheet natif uniquement (pas NativeWind)
export const colors = {
  primary: '#1A5276',    // bleu profond CI
  secondary: '#E67E22',  // orange CI
  accent: '#27AE60',     // vert succès
  danger: '#E74C3C',     // rouge erreur/danger
  text: '#2C3E50',       // texte principal
  textLight: '#8E9EAB',  // texte secondaire
  background: '#F8F9FA', // fond général
  white: '#FFFFFF',
  border: '#D5D8DC',
  card: '#FFFFFF',
}

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
}

export const borderRadius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
}

export const typography = {
  h1: { fontSize: 28, fontWeight: '700' as const },
  h2: { fontSize: 22, fontWeight: '700' as const },
  h3: { fontSize: 18, fontWeight: '600' as const },
  body: { fontSize: 14, fontWeight: '400' as const },
  caption: { fontSize: 12, fontWeight: '400' as const },
  price: { fontSize: 16, fontWeight: '700' as const, fontFamily: 'JetBrains Mono' },
}
```

**Étape 6 — Fichier .env.example (`apps/mobile/.env.example`):**
```
# Copier vers apps/mobile/.env et remplir avec les vraies valeurs
# IMPORTANT: préfixe EXPO_PUBLIC_ obligatoire (différent de NEXT_PUBLIC_ web)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```
  </action>
  <verify>
    <automated>cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo" && node -e "const ts = require('typescript'); console.log('TS OK')" 2>/dev/null || echo "TS check skipped" && ls apps/mobile/lib/supabase.ts apps/mobile/hooks/useAuth.ts apps/mobile/constants/theme.ts supabase/migrations/009_fcm_token.sql && echo "Tous les fichiers créés"</automated>
  </verify>
  <done>Migration 009 SQL créée, client Supabase RN avec detectSessionInUrl:false, hook useAuth avec SessionProvider + AuthGuard, palette CI dans theme.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Root layout + Layouts auth/tabs + Composants BienCard + StatutBadge</name>
  <files>
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/(auth)/_layout.tsx,
    apps/mobile/app/(auth)/login.tsx,
    apps/mobile/app/(auth)/register.tsx,
    apps/mobile/app/(tabs)/_layout.tsx,
    apps/mobile/components/BienCard.tsx,
    apps/mobile/components/StatutBadge.tsx
  </files>
  <read_first>
    apps/mobile/hooks/useAuth.ts,
    apps/mobile/constants/theme.ts
  </read_first>
  <action>
**Fichier 1 — Root layout (`apps/mobile/app/_layout.tsx`):**
```typescript
import { Stack } from 'expo-router'
import { StatusBar } from 'expo-status-bar'
import { SessionProvider, AuthGuard } from '../hooks/useAuth'

export default function RootLayout() {
  return (
    <SessionProvider>
      <AuthGuard>
        <StatusBar style="auto" />
        <Stack screenOptions={{ headerShown: false }}>
          <Stack.Screen name="(tabs)" />
          <Stack.Screen name="(auth)" />
          <Stack.Screen
            name="bien/[id]"
            options={{
              headerShown: true,
              headerTitle: 'Détail du bien',
              headerTintColor: '#1A5276',
            }}
          />
        </Stack>
      </AuthGuard>
    </SessionProvider>
  )
}
```

**Fichier 2 — Auth layout (`apps/mobile/app/(auth)/_layout.tsx`):**
```typescript
import { Stack } from 'expo-router'

export default function AuthLayout() {
  return (
    <Stack screenOptions={{ headerShown: false }}>
      <Stack.Screen name="login" />
      <Stack.Screen name="register" />
    </Stack>
  )
}
```

**Fichier 3 — Écran login (`apps/mobile/app/(auth)/login.tsx`):**
```typescript
import { useState } from 'react'
import {
  View, Text, TextInput, TouchableOpacity,
  StyleSheet, KeyboardAvoidingView, Platform, Alert, ActivityIndicator,
} from 'react-native'
import { useRouter } from 'expo-router'
import { supabase } from '../../lib/supabase'
import { colors, spacing, borderRadius, typography } from '../../constants/theme'

export default function LoginScreen() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [loading, setLoading] = useState(false)
  const router = useRouter()

  async function handleLogin() {
    if (!email || !password) {
      Alert.alert('Erreur', 'Veuillez remplir tous les champs')
      return
    }
    setLoading(true)
    const { error } = await supabase.auth.signInWithPassword({ email, password })
    setLoading(false)
    if (error) Alert.alert('Erreur de connexion', error.message)
    // Si succès, AuthGuard redirige automatiquement vers /(tabs)
  }

  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    >
      <View style={styles.inner}>
        <Text style={styles.title}>Immo CI</Text>
        <Text style={styles.subtitle}>Connectez-vous à votre compte</Text>

        <TextInput
          style={styles.input}
          placeholder="Email"
          placeholderTextColor={colors.textLight}
          value={email}
          onChangeText={setEmail}
          keyboardType="email-address"
          autoCapitalize="none"
          autoComplete="email"
        />
        <TextInput
          style={styles.input}
          placeholder="Mot de passe"
          placeholderTextColor={colors.textLight}
          value={password}
          onChangeText={setPassword}
          secureTextEntry
          autoComplete="password"
        />

        <TouchableOpacity
          style={[styles.button, loading && styles.buttonDisabled]}
          onPress={handleLogin}
          disabled={loading}
        >
          {loading ? (
            <ActivityIndicator color={colors.white} />
          ) : (
            <Text style={styles.buttonText}>Se connecter</Text>
          )}
        </TouchableOpacity>

        <TouchableOpacity onPress={() => router.push('/(auth)/register')}>
          <Text style={styles.link}>Pas de compte ? S'inscrire</Text>
        </TouchableOpacity>
      </View>
    </KeyboardAvoidingView>
  )
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  inner: { flex: 1, justifyContent: 'center', padding: spacing.xl },
  title: { ...typography.h1, color: colors.primary, textAlign: 'center', marginBottom: spacing.sm },
  subtitle: { ...typography.body, color: colors.textLight, textAlign: 'center', marginBottom: spacing.xl },
  input: {
    borderWidth: 1, borderColor: colors.border,
    borderRadius: borderRadius.md,
    padding: spacing.md,
    marginBottom: spacing.md,
    color: colors.text,
    backgroundColor: colors.white,
    fontSize: 14,
  },
  button: {
    backgroundColor: colors.primary,
    padding: spacing.md,
    borderRadius: borderRadius.md,
    alignItems: 'center',
    marginTop: spacing.sm,
  },
  buttonDisabled: { opacity: 0.6 },
  buttonText: { color: colors.white, fontWeight: '600', fontSize: 16 },
  link: { color: colors.primary, textAlign: 'center', marginTop: spacing.lg, fontSize: 14 },
})
```

**Fichier 4 — Écran register (`apps/mobile/app/(auth)/register.tsx`):**
Structure identique à login.tsx. Utiliser `supabase.auth.signUp({ email, password, options: { data: { nom, prenom } } })`. Ajouter champs TextInput pour nom et prénom. Afficher message "Vérifiez votre email" après succès. Bouton retour vers login en bas.

**Fichier 5 — Tabs layout (`apps/mobile/app/(tabs)/_layout.tsx`):**
```typescript
import { Tabs } from 'expo-router'
import { Ionicons } from '@expo/vector-icons'
import { colors } from '../../constants/theme'

type IoniconsName = React.ComponentProps<typeof Ionicons>['name']

function TabIcon({ name, color, size }: { name: IoniconsName; color: string; size: number }) {
  return <Ionicons name={name} size={size} color={color} />
}

export default function TabsLayout() {
  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: colors.primary,
        tabBarInactiveTintColor: colors.textLight,
        tabBarStyle: { backgroundColor: colors.white, borderTopColor: colors.border },
        headerStyle: { backgroundColor: colors.white },
        headerTintColor: colors.primary,
        headerTitleStyle: { fontWeight: '700' },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: 'Accueil',
          tabBarIcon: ({ color, size }) => <TabIcon name="home-outline" color={color} size={size} />,
        }}
      />
      <Tabs.Screen
        name="favoris"
        options={{
          title: 'Favoris',
          tabBarIcon: ({ color, size }) => <TabIcon name="heart-outline" color={color} size={size} />,
        }}
      />
      <Tabs.Screen
        name="reservations"
        options={{
          title: 'Réservations',
          tabBarIcon: ({ color, size }) => <TabIcon name="calendar-outline" color={color} size={size} />,
        }}
      />
      <Tabs.Screen
        name="profil"
        options={{
          title: 'Profil',
          tabBarIcon: ({ color, size }) => <TabIcon name="person-outline" color={color} size={size} />,
        }}
      />
    </Tabs>
  )
}
```

**Fichier 6 — Composant BienCard (`apps/mobile/components/BienCard.tsx`):**
```typescript
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native'
import { Image } from 'expo-image'
import { useRouter } from 'expo-router'
import { formatFCFA } from '@immo-ci/shared'
import type { Bien } from '@immo-ci/shared'
import { colors, spacing, borderRadius, typography } from '../constants/theme'

interface BienCardProps {
  bien: Bien
}

export function BienCard({ bien }: BienCardProps) {
  const router = useRouter()

  return (
    <TouchableOpacity
      style={styles.card}
      onPress={() => router.push(`/bien/${bien.id}`)}
      activeOpacity={0.85}
    >
      <Image
        source={{ uri: bien.photo_principale_url ?? 'https://via.placeholder.com/300x200' }}
        style={styles.image}
        contentFit="cover"
        transition={200}
        placeholder={{ blurhash: 'L6PZfSi_.AyE_3t7t7R**0o#DgR4' }}
      />
      <View style={styles.body}>
        <Text style={styles.titre} numberOfLines={2}>{bien.titre}</Text>
        <Text style={styles.commune}>{bien.commune} · {bien.type_bien}</Text>
        <Text style={styles.prix}>{formatFCFA(bien.prix)}</Text>
      </View>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: colors.white,
    borderRadius: borderRadius.lg,
    marginHorizontal: spacing.md,
    marginBottom: spacing.md,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.08,
    shadowRadius: 4,
    elevation: 3,
    overflow: 'hidden',
  },
  image: { width: '100%', height: 180 },
  body: { padding: spacing.md },
  titre: { ...typography.h3, color: colors.text, marginBottom: spacing.xs },
  commune: { ...typography.caption, color: colors.textLight, marginBottom: spacing.sm },
  prix: { fontSize: 16, fontWeight: '700', color: colors.secondary },
})
```

**Fichier 7 — Composant StatutBadge (`apps/mobile/components/StatutBadge.tsx`):**
```typescript
import { View, Text, StyleSheet } from 'react-native'
import { colors, borderRadius } from '../constants/theme'

type StatutType = 'en_attente' | 'confirmee' | 'annulee' | 'terminee' | 'approuve' | 'rejete' | 'en_cours' | 'non_soumis'

interface StatutBadgeProps {
  statut: StatutType
  size?: 'sm' | 'md'
}

const STATUT_CONFIG: Record<StatutType, { label: string; bg: string; text: string }> = {
  en_attente:  { label: 'En attente',  bg: '#FEF9E7', text: '#D4AC0D' },
  confirmee:   { label: 'Confirmée',   bg: '#EAFAF1', text: colors.accent },
  annulee:     { label: 'Annulée',     bg: '#FDEDEC', text: colors.danger },
  terminee:    { label: 'Terminée',    bg: '#EBF5FB', text: colors.primary },
  approuve:    { label: 'Approuvé',    bg: '#EAFAF1', text: colors.accent },
  rejete:      { label: 'Rejeté',      bg: '#FDEDEC', text: colors.danger },
  en_cours:    { label: 'En cours',    bg: '#EBF5FB', text: colors.primary },
  non_soumis:  { label: 'Non soumis',  bg: '#F2F3F4', text: colors.textLight },
}

export function StatutBadge({ statut, size = 'md' }: StatutBadgeProps) {
  const config = STATUT_CONFIG[statut] ?? { label: statut, bg: '#F2F3F4', text: colors.textLight }
  return (
    <View style={[styles.badge, { backgroundColor: config.bg }, size === 'sm' && styles.badgeSm]}>
      <Text style={[styles.text, { color: config.text }, size === 'sm' && styles.textSm]}>
        {config.label}
      </Text>
    </View>
  )
}

const styles = StyleSheet.create({
  badge: { paddingHorizontal: 10, paddingVertical: 4, borderRadius: borderRadius.full, alignSelf: 'flex-start' },
  badgeSm: { paddingHorizontal: 8, paddingVertical: 2 },
  text: { fontSize: 13, fontWeight: '600' },
  textSm: { fontSize: 11 },
})
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\(auth)\login.tsx" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\(tabs)\_layout.tsx" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\components\BienCard.tsx" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\components\StatutBadge.tsx" && echo "Fichiers layouts et composants créés"</automated>
  </verify>
  <done>Root layout avec SessionProvider+AuthGuard, layout auth, login/register, layout tabs avec 4 onglets, BienCard avec expo-image, StatutBadge avec couleurs CI.</done>
</task>

<task type="auto">
  <name>Task 3: Écrans onglets (Accueil, Favoris, Réservations, Profil) + Fiche bien</name>
  <files>
    apps/mobile/app/(tabs)/index.tsx,
    apps/mobile/app/(tabs)/favoris.tsx,
    apps/mobile/app/(tabs)/reservations.tsx,
    apps/mobile/app/(tabs)/profil.tsx,
    apps/mobile/app/bien/[id].tsx
  </files>
  <read_first>
    apps/mobile/components/BienCard.tsx,
    apps/mobile/components/StatutBadge.tsx,
    apps/mobile/lib/supabase.ts,
    apps/mobile/constants/theme.ts
  </read_first>
  <action>
**Fichier 1 — Onglet Accueil (`apps/mobile/app/(tabs)/index.tsx`):**
```typescript
import { useState, useEffect, useCallback } from 'react'
import { View, FlatList, TextInput, StyleSheet, ActivityIndicator, Text, RefreshControl } from 'react-native'
import { supabase } from '../../lib/supabase'
import { BienCard } from '../../components/BienCard'
import { COMMUNES_CI } from '@immo-ci/shared'
import type { Bien } from '@immo-ci/shared'
import { colors, spacing } from '../../constants/theme'

export default function AccueilScreen() {
  const [biens, setBiens] = useState<Bien[]>([])
  const [search, setSearch] = useState('')
  const [loading, setLoading] = useState(true)
  const [refreshing, setRefreshing] = useState(false)

  async function fetchBiens(searchText = '') {
    let query = supabase
      .from('biens')
      .select('id, titre, prix, commune, type_bien, statut, photo_principale_url')
      .eq('statut', 'publie')
      .order('created_at', { ascending: false })
      .limit(20)

    if (searchText) {
      // Filtrer par commune OU titre (recherche simple côté client pour v1)
      query = query.or(`commune.ilike.%${searchText}%,titre.ilike.%${searchText}%`)
    }

    const { data, error } = await query
    if (!error && data) setBiens(data as Bien[])
  }

  useEffect(() => {
    setLoading(true)
    fetchBiens().finally(() => setLoading(false))
  }, [])

  const onRefresh = useCallback(async () => {
    setRefreshing(true)
    await fetchBiens(search)
    setRefreshing(false)
  }, [search])

  const onSearch = useCallback((text: string) => {
    setSearch(text)
    fetchBiens(text)
  }, [])

  if (loading) {
    return <View style={styles.center}><ActivityIndicator color={colors.primary} size="large" /></View>
  }

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.searchBar}
        placeholder="Rechercher par commune ou titre..."
        placeholderTextColor={colors.textLight}
        value={search}
        onChangeText={onSearch}
      />
      <FlatList
        data={biens}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => <BienCard bien={item} />}
        contentContainerStyle={styles.list}
        ListEmptyComponent={
          <Text style={styles.empty}>Aucun bien trouvé</Text>
        }
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={onRefresh} tintColor={colors.primary} />
        }
      />
    </View>
  )
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  center: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  searchBar: {
    margin: spacing.md,
    padding: spacing.md,
    backgroundColor: colors.white,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: colors.border,
    color: colors.text,
    fontSize: 14,
  },
  list: { paddingTop: spacing.sm, paddingBottom: spacing.xl },
  empty: { textAlign: 'center', color: colors.textLight, marginTop: 48, fontSize: 15 },
})
```

**Fichier 2 — Onglet Favoris (`apps/mobile/app/(tabs)/favoris.tsx`):**
Query `favoris` jointure `biens` pour l'utilisateur connecté. FlatList avec BienCard. Empty state: icône coeur + "Aucun favori pour l'instant". Utiliser `useSession()` pour récupérer `session.user.id`. Query: `supabase.from('favoris').select('bien_id, biens(*)').eq('user_id', userId)`.

**Fichier 3 — Onglet Réservations (`apps/mobile/app/(tabs)/reservations.tsx`):**
Query `reservations` + `biens` pour l'utilisateur connecté. FlatList chaque item affiche: titre bien, dates (formatDate), StatutBadge statut. Query: `supabase.from('reservations').select('*, biens(titre, commune)').eq('locataire_id', userId).order('created_at', { ascending: false })`. Empty state: "Aucune réservation".

**Fichier 4 — Onglet Profil (`apps/mobile/app/(tabs)/profil.tsx`):**
```typescript
import { useEffect, useState } from 'react'
import { View, Text, StyleSheet, TouchableOpacity, Alert, ActivityIndicator, ScrollView } from 'react-native'
import { supabase } from '../../lib/supabase'
import { useSession } from '../../hooks/useAuth'
import { StatutBadge } from '../../components/StatutBadge'
import type { Profile } from '@immo-ci/shared'
import { colors, spacing, typography, borderRadius } from '../../constants/theme'

export default function ProfilScreen() {
  const { session } = useSession()
  const [profile, setProfile] = useState<Profile | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    if (!session?.user.id) return
    supabase
      .from('profiles')
      .select('*')
      .eq('id', session.user.id)
      .single()
      .then(({ data }) => {
        if (data) setProfile(data as Profile)
        setLoading(false)
      })
  }, [session])

  async function handleLogout() {
    Alert.alert('Déconnexion', 'Voulez-vous vous déconnecter ?', [
      { text: 'Annuler', style: 'cancel' },
      {
        text: 'Déconnexion',
        style: 'destructive',
        onPress: async () => {
          await supabase.auth.signOut()
          // AuthGuard redirige automatiquement vers login
        },
      },
    ])
  }

  if (loading) return <View style={styles.center}><ActivityIndicator color={colors.primary} /></View>

  return (
    <ScrollView style={styles.container} contentContainerStyle={styles.content}>
      <View style={styles.avatar}>
        <Text style={styles.avatarText}>
          {profile?.prenom?.[0]?.toUpperCase()}{profile?.nom?.[0]?.toUpperCase()}
        </Text>
      </View>
      <Text style={styles.name}>{profile?.prenom} {profile?.nom}</Text>
      <Text style={styles.email}>{session?.user.email}</Text>
      <Text style={styles.role}>{profile?.role === 'proprietaire' ? 'Propriétaire' : profile?.role === 'agence' ? 'Agence' : 'Locataire'}</Text>

      <View style={styles.section}>
        <Text style={styles.sectionTitle}>Statut KYC</Text>
        <StatutBadge statut={profile?.kyc_statut ?? 'non_soumis'} />
      </View>

      <TouchableOpacity style={styles.logoutButton} onPress={handleLogout}>
        <Text style={styles.logoutText}>Déconnexion</Text>
      </TouchableOpacity>
    </ScrollView>
  )
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  content: { alignItems: 'center', padding: spacing.xl },
  center: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  avatar: {
    width: 80, height: 80, borderRadius: 40,
    backgroundColor: colors.primary, justifyContent: 'center', alignItems: 'center',
    marginBottom: spacing.md,
  },
  avatarText: { color: colors.white, fontSize: 28, fontWeight: '700' },
  name: { ...typography.h2, color: colors.text, marginBottom: spacing.xs },
  email: { ...typography.body, color: colors.textLight, marginBottom: spacing.xs },
  role: { ...typography.caption, color: colors.primary, fontWeight: '600', marginBottom: spacing.lg },
  section: { width: '100%', marginBottom: spacing.lg },
  sectionTitle: { ...typography.body, color: colors.textLight, marginBottom: spacing.sm },
  logoutButton: {
    backgroundColor: colors.danger, paddingVertical: spacing.md,
    paddingHorizontal: spacing.xl, borderRadius: borderRadius.md, marginTop: spacing.xl,
  },
  logoutText: { color: colors.white, fontWeight: '700', fontSize: 16 },
})
```

**Fichier 5 — Fiche bien (`apps/mobile/app/bien/[id].tsx`):**
```typescript
import { useEffect, useState } from 'react'
import { View, Text, ScrollView, StyleSheet, ActivityIndicator, TouchableOpacity, Alert } from 'react-native'
import { Image } from 'expo-image'
import { useLocalSearchParams, useRouter } from 'expo-router'
import { supabase } from '../../lib/supabase'
import { useSession } from '../../hooks/useAuth'
import { formatFCFA } from '@immo-ci/shared'
import type { Bien } from '@immo-ci/shared'
import { colors, spacing, typography, borderRadius } from '../../constants/theme'

export default function FicheBienScreen() {
  const { id } = useLocalSearchParams<{ id: string }>()
  const { session } = useSession()
  const router = useRouter()
  const [bien, setBien] = useState<Bien | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    supabase
      .from('biens')
      .select('*')
      .eq('id', id)
      .single()
      .then(({ data }) => {
        if (data) setBien(data as Bien)
        setLoading(false)
      })
  }, [id])

  function handleReserver() {
    if (!session) {
      Alert.alert('Connexion requise', 'Connectez-vous pour réserver ce bien.')
      return
    }
    // Navigation vers flow réservation web (web app) — v1: ouvrir dans navigateur externe
    Alert.alert('Réservation', `Réservez ${bien?.titre} depuis l'application web pour finaliser le paiement.`)
  }

  if (loading) return <View style={styles.center}><ActivityIndicator color={colors.primary} size="large" /></View>
  if (!bien) return <View style={styles.center}><Text style={styles.errorText}>Bien introuvable</Text></View>

  return (
    <ScrollView style={styles.container}>
      <Image
        source={{ uri: bien.photo_principale_url ?? 'https://via.placeholder.com/400x250' }}
        style={styles.image}
        contentFit="cover"
        transition={300}
      />
      <View style={styles.content}>
        <Text style={styles.prix}>{formatFCFA(bien.prix)}</Text>
        <Text style={styles.titre}>{bien.titre}</Text>
        <Text style={styles.commune}>{bien.commune} · {bien.type_bien}</Text>

        {bien.description && (
          <View style={styles.section}>
            <Text style={styles.sectionTitle}>Description</Text>
            <Text style={styles.description}>{bien.description}</Text>
          </View>
        )}

        {bien.surface && (
          <Text style={styles.detail}>Surface : {bien.surface} m²</Text>
        )}
        {bien.nb_pieces && (
          <Text style={styles.detail}>Pièces : {bien.nb_pieces}</Text>
        )}

        <TouchableOpacity style={styles.ctaButton} onPress={handleReserver}>
          <Text style={styles.ctaText}>Réserver ce bien</Text>
        </TouchableOpacity>
      </View>
    </ScrollView>
  )
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  center: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: spacing.xl },
  image: { width: '100%', height: 260 },
  content: { padding: spacing.lg },
  prix: { fontSize: 24, fontWeight: '700', color: colors.secondary, marginBottom: spacing.xs },
  titre: { ...typography.h2, color: colors.text, marginBottom: spacing.xs },
  commune: { ...typography.body, color: colors.textLight, marginBottom: spacing.lg },
  section: { marginBottom: spacing.md },
  sectionTitle: { ...typography.h3, color: colors.text, marginBottom: spacing.sm },
  description: { ...typography.body, color: colors.text, lineHeight: 22 },
  detail: { ...typography.body, color: colors.text, marginBottom: spacing.xs },
  errorText: { color: colors.danger, fontSize: 16 },
  ctaButton: {
    backgroundColor: colors.primary, padding: spacing.md,
    borderRadius: borderRadius.md, alignItems: 'center', marginTop: spacing.xl,
  },
  ctaText: { color: colors.white, fontWeight: '700', fontSize: 16 },
})
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\(tabs)\index.tsx" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\(tabs)\profil.tsx" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app\bien\[id].tsx" && echo "Écrans créés" && cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>5 écrans créés: liste biens avec FlatList+SearchBar, favoris, réservations avec StatutBadge, profil avec logout, fiche bien avec ScrollView+CTA réserver.</done>
</task>

</tasks>

<verification>
```bash
# 1. Migration SQL existe et contient ALTER TABLE
grep -n "fcm_token" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\supabase\migrations\009_fcm_token.sql"

# 2. Client Supabase a detectSessionInUrl: false
grep "detectSessionInUrl" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\lib\supabase.ts"

# 3. useAuth exporte SessionProvider et AuthGuard
grep -E "export function (SessionProvider|AuthGuard|useSession)" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\hooks\useAuth.ts"

# 4. Aucun usage de NativeWind/className dans le mobile
grep -rn "className=" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\app" 2>/dev/null | grep -v node_modules

# 5. BienCard importe depuis @immo-ci/shared
grep "from '@immo-ci/shared'" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\components\BienCard.tsx"

# 6. TypeScript check
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile" && npx tsc --noEmit
```
</verification>

<success_criteria>
1. Migration 009 SQL crée la colonne `fcm_token TEXT` dans `profiles` sans erreur
2. `lib/supabase.ts` a `detectSessionInUrl: false` et `react-native-url-polyfill/auto` comme premier import
3. `hooks/useAuth.ts` exporte `SessionProvider`, `AuthGuard`, `useSession`
4. `app/_layout.tsx` enveloppe la Stack dans `SessionProvider > AuthGuard`
5. 4 onglets visibles dans la tab bar avec icônes Ionicons
6. `BienCard` utilise `formatFCFA` depuis `@immo-ci/shared` et `expo-image` pour les images
7. `StatutBadge` couvre les statuts `en_attente`, `confirmee`, `annulee`, `terminee`, `approuve`, `rejete`
8. Aucun `className=` ou import NativeWind dans les fichiers mobile
9. `npx tsc --noEmit` passe sans erreurs bloquantes
</success_criteria>

<output>
After completion, create `.planning/phases/05-app-mobile-tests-d-ploiement/05-01-SUMMARY.md` documenting:
- Files created (list each with one-line purpose)
- Packages installed (expo-sqlite, expo-image, react-native-url-polyfill, etc.)
- Any deviations from plan (e.g., missing fields in Bien type)
- TypeScript errors encountered and how resolved
</output>
