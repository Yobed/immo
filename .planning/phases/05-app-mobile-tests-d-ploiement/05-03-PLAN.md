---
phase: 05-app-mobile-tests-d-ploiement
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - apps/web/playwright.config.ts
  - apps/web/tests/e2e/auth.spec.ts
  - apps/web/tests/e2e/reservation.spec.ts
  - apps/web/tests/e2e/dashboard.spec.ts
  - apps/mobile/.maestro/flows/auth.yaml
  - apps/mobile/.maestro/flows/search-biens.yaml
  - apps/mobile/.maestro/flows/profil.yaml
  - apps/web/package.json
autonomous: true
requirements: [MOB-01, MOB-02, MOB-04]

must_haves:
  truths:
    - "La suite Playwright s'exécute sans erreur avec `npx playwright test`"
    - "Le test auth.spec.ts valide inscription → connexion → déconnexion sur web"
    - "Le test reservation.spec.ts valide le flow liste biens → fiche → formulaire dates"
    - "Le test dashboard.spec.ts vérifie que les KPIs sont visibles après connexion propriétaire"
    - "Les 3 flows Maestro sont des YAML valides avec appId ci.immo.app"
    - "Les flows Maestro couvrent login, recherche biens et affichage profil"
  artifacts:
    - path: "apps/web/playwright.config.ts"
      provides: "Configuration Playwright avec webServer localhost:3000"
      exports: ["defineConfig"]
    - path: "apps/web/tests/e2e/auth.spec.ts"
      provides: "Tests E2E flow auth complet"
    - path: "apps/web/tests/e2e/reservation.spec.ts"
      provides: "Tests E2E flow réservation"
    - path: "apps/web/tests/e2e/dashboard.spec.ts"
      provides: "Tests E2E dashboard propriétaire"
    - path: "apps/mobile/.maestro/flows/auth.yaml"
      provides: "Flow Maestro login mobile"
    - path: "apps/mobile/.maestro/flows/search-biens.yaml"
      provides: "Flow Maestro recherche biens"
    - path: "apps/mobile/.maestro/flows/profil.yaml"
      provides: "Flow Maestro profil KYC"
  key_links:
    - from: "apps/web/playwright.config.ts"
      to: "apps/web (Next.js server)"
      via: "webServer.command: 'npm run start', port: 3000"
      pattern: "webServer"
    - from: "apps/web/tests/e2e/auth.spec.ts"
      to: "page /auth/login et /auth/register"
      via: "page.goto('/auth/login')"
      pattern: "goto.*auth"
    - from: "apps/mobile/.maestro/flows/auth.yaml"
      to: "app Expo (appId: ci.immo.app)"
      via: "launchApp + tapOn"
      pattern: "appId: ci.immo.app"
---

<objective>
Crée la suite de tests de qualité complète : Playwright pour les flows critiques web (auth, réservation, dashboard) et Maestro pour les flows mobiles (login, recherche, profil). Ces tests constituent la porte qualité avant déploiement en production.

Purpose: Garantir que les flows critiques ne régressent pas lors des mises à jour. Les tests Playwright détectent les régressions web, Maestro valide l'expérience mobile sur device réel via EAS.
Output: Configuration Playwright + 3 specs E2E web, 3 flows YAML Maestro, instructions test dans SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-app-mobile-tests-d-ploiement/05-RESEARCH.md

<interfaces>
<!-- Routes web connues d'après les phases précédentes -->

Routes Next.js existantes (construites en phases 1-4):
- /auth/login                 — Page connexion email/password
- /auth/register              — Page inscription
- /biens                      — Liste biens publics (recherche + filtres)
- /biens/[id]                 — Fiche bien publique
- /reservations/nouvelle      — Flow réservation (protected)
- /reservations/[id]          — Statut réservation (protected)
- /dashboard                  — Dashboard propriétaire (protected)
- /profil                     — Profil utilisateur (protected)

Sélecteurs CSS attendus (Tailwind):
Les composants web utilisent Tailwind. Pour les sélecteurs Playwright, utiliser:
- `role` selectors (priorité): `page.getByRole('button', { name: 'Se connecter' })`
- `text` selectors: `page.getByText('Tableau de bord')`
- `placeholder`: `page.getByPlaceholder('Email')`
- `testid` si disponibles: `page.getByTestId('bien-card')`
- Éviter les sélecteurs CSS fragiles (.text-primary, .bg-blue-800)

Playwright config pattern (depuis 05-RESEARCH.md):
```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',   // Attention: testDir relatif à playwright.config.ts
  use: { baseURL: 'http://localhost:3000' },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run build && npm run start',  // build requis avant next start en CI
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
```

Note Playwright: Utiliser `npm run start` (next start) pas `npm run dev` pour les tests CI.
Le build Next.js doit exister avant. En local: `npm run build && npm run start`.

Maestro CLI — format YAML officiel:
```yaml
appId: ci.immo.app
---
- launchApp
- assertVisible:
    text: "Connexion"    # ou id: "element-id"
- tapOn:
    text: "Email"
- inputText: "test@immo-ci.com"
- tapOn:
    text: "Mot de passe"
- inputText: "testpassword123"
- tapOn:
    text: "Se connecter"
- assertVisible:
    text: "Accueil"
```

Compte test à utiliser dans les specs (à documenter dans SUMMARY.md):
- Email: test@immo-ci.com (locataire)
- Email: proprio@immo-ci.com (propriétaire — pour dashboard tests)
- Password: TestPassword123! (les deux)
Ces comptes doivent être créés manuellement en staging avant de lancer les tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright config + 3 specs E2E web</name>
  <files>
    apps/web/playwright.config.ts,
    apps/web/tests/e2e/auth.spec.ts,
    apps/web/tests/e2e/reservation.spec.ts,
    apps/web/tests/e2e/dashboard.spec.ts,
    apps/web/package.json
  </files>
  <read_first>
    apps/web/package.json
  </read_first>
  <action>
**Étape 1 — Installer Playwright dans apps/web:**
```bash
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web"
npm install --save-dev @playwright/test
npx playwright install --with-deps chromium
```

Puis lire `apps/web/package.json` pour ajouter le script `e2e` sans écraser les scripts existants:
```json
"scripts": {
  "...existing scripts...",
  "e2e": "playwright test",
  "e2e:ui": "playwright test --ui",
  "e2e:report": "playwright show-report"
}
```

**Étape 2 — playwright.config.ts (`apps/web/playwright.config.ts`):**
```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: false,      // séquentiel pour éviter conflits d'état auth
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,                // 1 worker — tests partagent le même serveur Next.js
  reporter: process.env.CI ? 'github' : 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run build && npm run start',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
    // stdout: 'pipe', // activer pour debug
  },
})
```

**Étape 3 — Test auth (`apps/web/tests/e2e/auth.spec.ts`):**
```typescript
import { test, expect } from '@playwright/test'

// Comptes test — à créer manuellement en staging avant exécution
const TEST_EMAIL = process.env.TEST_EMAIL ?? 'test@immo-ci.com'
const TEST_PASSWORD = process.env.TEST_PASSWORD ?? 'TestPassword123!'

test.describe('Authentification', () => {
  test('connexion avec email et mot de passe valides', async ({ page }) => {
    await page.goto('/auth/login')

    // Vérifier que la page login est chargée
    await expect(page).toHaveTitle(/Immo CI|Connexion/i)
    await expect(page.getByRole('heading', { name: /connexion/i })).toBeVisible()

    // Remplir le formulaire
    await page.getByPlaceholder(/email/i).fill(TEST_EMAIL)
    await page.getByPlaceholder(/mot de passe/i).fill(TEST_PASSWORD)
    await page.getByRole('button', { name: /se connecter/i }).click()

    // Après connexion réussie: redirection vers dashboard ou accueil
    await expect(page).toHaveURL(/\/(dashboard|biens|profil)/, { timeout: 10_000 })
  })

  test('connexion avec mauvais mot de passe affiche une erreur', async ({ page }) => {
    await page.goto('/auth/login')

    await page.getByPlaceholder(/email/i).fill(TEST_EMAIL)
    await page.getByPlaceholder(/mot de passe/i).fill('mauvais-mot-de-passe')
    await page.getByRole('button', { name: /se connecter/i }).click()

    // Doit afficher un message d'erreur
    await expect(page.getByText(/identifiants invalides|mot de passe incorrect|Invalid login/i)).toBeVisible({
      timeout: 5_000,
    })
    // Doit rester sur la page login
    await expect(page).toHaveURL(/auth\/login/)
  })

  test('déconnexion depuis le profil', async ({ page }) => {
    // Se connecter d'abord
    await page.goto('/auth/login')
    await page.getByPlaceholder(/email/i).fill(TEST_EMAIL)
    await page.getByPlaceholder(/mot de passe/i).fill(TEST_PASSWORD)
    await page.getByRole('button', { name: /se connecter/i }).click()
    await page.waitForURL(/\/(dashboard|biens|profil)/, { timeout: 10_000 })

    // Naviguer vers le profil
    await page.goto('/profil')
    await expect(page).toHaveURL(/profil/)

    // Cliquer sur déconnexion
    await page.getByRole('button', { name: /déconnexion|se déconnecter/i }).click()

    // Doit rediriger vers login ou landing
    await expect(page).toHaveURL(/(auth\/login|\/)/, { timeout: 5_000 })
  })
})
```

**Étape 4 — Test réservation (`apps/web/tests/e2e/reservation.spec.ts`):**
```typescript
import { test, expect } from '@playwright/test'

const TEST_EMAIL = process.env.TEST_EMAIL ?? 'test@immo-ci.com'
const TEST_PASSWORD = process.env.TEST_PASSWORD ?? 'TestPassword123!'

// Helper: se connecter avant chaque test
async function login(page: import('@playwright/test').Page) {
  await page.goto('/auth/login')
  await page.getByPlaceholder(/email/i).fill(TEST_EMAIL)
  await page.getByPlaceholder(/mot de passe/i).fill(TEST_PASSWORD)
  await page.getByRole('button', { name: /se connecter/i }).click()
  await page.waitForURL(/\/(dashboard|biens|profil)/, { timeout: 10_000 })
}

test.describe('Flow Réservation', () => {
  test('liste des biens affiche des annonces publiées', async ({ page }) => {
    await page.goto('/biens')

    // La liste doit contenir au moins 1 bien
    const bienCards = page.locator('[data-testid="bien-card"], .bien-card, article').first()
    await expect(bienCards).toBeVisible({ timeout: 8_000 })

    // Vérifier qu'un prix FCFA est visible
    await expect(page.getByText(/FCFA/)).toBeVisible()
  })

  test('filtrer les biens par commune', async ({ page }) => {
    await page.goto('/biens')

    // Chercher par commune
    const searchInput = page.getByPlaceholder(/commune|rechercher|search/i).first()
    if (await searchInput.isVisible()) {
      await searchInput.fill('Cocody')
      // Attendre que les résultats se mettent à jour
      await page.waitForTimeout(1000)
      // Les résultats filtrés doivent être visibles ou message "aucun résultat"
      const hasResults = await page.getByText(/FCFA/).isVisible().catch(() => false)
      const hasEmpty = await page.getByText(/aucun résultat|no results/i).isVisible().catch(() => false)
      expect(hasResults || hasEmpty).toBeTruthy()
    }
  })

  test('fiche bien affiche le titre et le bouton réserver', async ({ page }) => {
    // Naviguer vers la liste
    await page.goto('/biens')
    await page.waitForLoadState('networkidle')

    // Cliquer sur le premier bien
    const firstCard = page.locator('a[href*="/biens/"]').first()
    await expect(firstCard).toBeVisible({ timeout: 8_000 })
    await firstCard.click()

    // Vérifier que la fiche bien est affichée
    await expect(page).toHaveURL(/\/biens\/[a-zA-Z0-9-]+/)
    await expect(page.getByText(/FCFA/)).toBeVisible()

    // Bouton réserver visible (même sans être connecté — il redirige vers login)
    await expect(page.getByRole('button', { name: /réserver|contacter/i })).toBeVisible()
  })

  test('tentative de réservation sans connexion redirige vers login', async ({ page }) => {
    // S'assurer d'être déconnecté
    await page.goto('/biens')
    const firstBienLink = page.locator('a[href*="/biens/"]').first()
    await expect(firstBienLink).toBeVisible({ timeout: 8_000 })
    await firstBienLink.click()

    // Cliquer sur réserver
    const reserverBtn = page.getByRole('button', { name: /réserver/i }).first()
    if (await reserverBtn.isVisible()) {
      await reserverBtn.click()
      // Doit rediriger vers login ou afficher modal connexion
      const isOnLogin = await page.waitForURL(/auth\/login/, { timeout: 5_000 }).then(() => true).catch(() => false)
      const hasLoginModal = await page.getByText(/connectez-vous|connexion requise/i).isVisible().catch(() => false)
      expect(isOnLogin || hasLoginModal).toBeTruthy()
    }
  })
})
```

**Étape 5 — Test dashboard (`apps/web/tests/e2e/dashboard.spec.ts`):**
```typescript
import { test, expect } from '@playwright/test'

// Compte propriétaire pour tester le dashboard
const PROPRIO_EMAIL = process.env.PROPRIO_EMAIL ?? 'proprio@immo-ci.com'
const PROPRIO_PASSWORD = process.env.PROPRIO_PASSWORD ?? 'TestPassword123!'

test.describe('Dashboard Propriétaire', () => {
  test.beforeEach(async ({ page }) => {
    // Se connecter en tant que propriétaire avant chaque test
    await page.goto('/auth/login')
    await page.getByPlaceholder(/email/i).fill(PROPRIO_EMAIL)
    await page.getByPlaceholder(/mot de passe/i).fill(PROPRIO_PASSWORD)
    await page.getByRole('button', { name: /se connecter/i }).click()
    await page.waitForURL(/\/(dashboard|biens|profil)/, { timeout: 10_000 })
  })

  test('dashboard charge et affiche les KPI cards', async ({ page }) => {
    await page.goto('/dashboard')
    await expect(page).toHaveURL(/dashboard/, { timeout: 5_000 })

    // Attendre que la page soit chargée (données Supabase)
    await page.waitForLoadState('networkidle')

    // KPIs attendus (labels vérifiés au niveau texte)
    const kpiLabels = [
      /revenus|recettes/i,
      /réservations/i,
      /biens/i,
    ]

    for (const label of kpiLabels) {
      const kpiElement = page.getByText(label).first()
      // Au moins 2 KPIs sur 3 doivent être visibles
      const isVisible = await kpiElement.isVisible({ timeout: 5_000 }).catch(() => false)
      if (!isVisible) {
        console.warn(`KPI "${label}" non visible — peut être OK si le dashboard est vide`)
      }
    }

    // Au moins 1 chiffre visible (valeur FCFA ou nombre)
    const hasNumbers = await page.getByText(/\d+/).first().isVisible().catch(() => false)
    expect(hasNumbers).toBeTruthy()
  })

  test('dashboard affiche un graphique ou section analytique', async ({ page }) => {
    await page.goto('/dashboard')
    await page.waitForLoadState('networkidle')

    // Chercher des éléments de chart (SVG Recharts ou canvas)
    const hasSvgChart = await page.locator('svg').first().isVisible({ timeout: 5_000 }).catch(() => false)
    const hasChartSection = await page.getByText(/évolution|statistiques|analytique/i).isVisible().catch(() => false)

    // Au moins un indicateur de dashboard analytique
    expect(hasSvgChart || hasChartSection).toBeTruthy()
  })

  test('section alertes prioritaires visible', async ({ page }) => {
    await page.goto('/dashboard')
    await page.waitForLoadState('networkidle')

    // Chercher la section alertes
    const alertesSectionVisible = await page.getByText(/alertes|prioritaire|attention/i).isVisible({ timeout: 5_000 }).catch(() => false)
    // Tolérant si aucune alerte active — la section peut être masquée
    if (!alertesSectionVisible) {
      console.log('Section alertes non visible (peut-être aucune alerte active)')
    }

    // La page dashboard doit au minimum charger sans erreur 500
    await expect(page).not.toHaveURL(/error|500/)
  })
})
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\playwright.config.ts" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\tests\e2e\auth.spec.ts" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\tests\e2e\reservation.spec.ts" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\tests\e2e\dashboard.spec.ts" && echo "Specs créées" && grep "testDir\|webServer\|baseURL" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\playwright.config.ts"</automated>
  </verify>
  <done>playwright.config.ts configuré avec webServer, 3 specs E2E web couvrant auth/réservation/dashboard, @playwright/test installé.</done>
</task>

<task type="auto">
  <name>Task 2: Flows Maestro YAML (3 flows mobile)</name>
  <files>
    apps/mobile/.maestro/flows/auth.yaml,
    apps/mobile/.maestro/flows/search-biens.yaml,
    apps/mobile/.maestro/flows/profil.yaml
  </files>
  <action>
**Créer le dossier `.maestro/flows/` dans `apps/mobile/`.**

Les flows Maestro testent l'app Expo sur device physique ou émulateur (Android SDK 34+).
L'app doit être installée avec `eas build --profile preview` avant d'exécuter les flows.
`appId` correspond au package Android / bundle iOS: `ci.immo.app`.

**Fichier 1 — Flow auth (`apps/mobile/.maestro/flows/auth.yaml`):**
```yaml
# Flow Maestro: Authentification mobile
# Prérequis: App installée sur device avec appId ci.immo.app
# Compte test: test@immo-ci.com / TestPassword123!
# Exécution: maestro test .maestro/flows/auth.yaml

appId: ci.immo.app
---
- launchApp:
    clearState: true    # Reset état app pour test propre

# Vérifier que l'écran login est affiché (app démarre sur login si non connecté)
- assertVisible:
    text: "Immo CI"

- assertVisible:
    text: "Connectez-vous"

# Remplir le formulaire de connexion
- tapOn:
    placeholder: "Email"
- inputText: "test@immo-ci.com"

- tapOn:
    placeholder: "Mot de passe"
- inputText: "TestPassword123!"

- tapOn:
    text: "Se connecter"

# Attendre la navigation vers les onglets
- waitForAnimationToEnd

# Vérifier l'arrivée sur l'écran Accueil (onglet 1)
- assertVisible:
    text: "Accueil"

# Vérifier la tab bar (4 onglets)
- assertVisible:
    text: "Favoris"
- assertVisible:
    text: "Réservations"
- assertVisible:
    text: "Profil"

# Naviguer vers Profil
- tapOn:
    text: "Profil"

- assertVisible:
    text: "Déconnexion"

# Tester la déconnexion
- tapOn:
    text: "Déconnexion"

- assertVisible:
    text: "Déconnexion"    # Dialog de confirmation

- tapOn:
    text: "Déconnexion"   # Confirmer dans l'Alert

# Vérifier retour sur login
- assertVisible:
    text: "Immo CI"
```

**Fichier 2 — Flow recherche biens (`apps/mobile/.maestro/flows/search-biens.yaml`):**
```yaml
# Flow Maestro: Recherche de biens et navigation vers fiche
# Prérequis: App connectée avec compte test
# Ce flow suppose que la connexion a déjà eu lieu (utiliser auth.yaml avant)

appId: ci.immo.app
---
- launchApp

# Si sur l'écran login, se connecter d'abord
- runFlow:
    when:
      visible:
        text: "Se connecter"
    file: auth.yaml

# Aller sur l'onglet Accueil
- tapOn:
    text: "Accueil"

- waitForAnimationToEnd

# Vérifier que la liste de biens est chargée (au moins 1 carte)
- assertVisible:
    text: "FCFA"
    timeout: 8000

# Rechercher par commune
- tapOn:
    placeholder: "Rechercher par commune ou titre..."
- inputText: "Cocody"

- waitForAnimationToEnd

# Vérifier que des résultats s'affichent OU message vide
# (pas d'assertion stricte sur le nombre de résultats)
- assertVisible:
    text: "FCFA"
    optional: true
- assertVisible:
    text: "Aucun bien trouvé"
    optional: true

# Effacer la recherche
- tapOn:
    placeholder: "Rechercher par commune ou titre..."
- clearText

- waitForAnimationToEnd

# Taper sur le premier bien visible
- tapOn:
    text: "FCFA"    # Premier prix visible = premier bien
    index: 0

- waitForAnimationToEnd

# Vérifier la fiche bien
- assertVisible:
    text: "FCFA"    # Prix toujours visible

- assertVisible:
    text: "Réserver ce bien"

# Revenir à la liste
- pressKey: Back
```

**Fichier 3 — Flow profil (`apps/mobile/.maestro/flows/profil.yaml`):**
```yaml
# Flow Maestro: Profil utilisateur et badge KYC
# Prérequis: App connectée avec compte test

appId: ci.immo.app
---
- launchApp

# Se connecter si nécessaire
- runFlow:
    when:
      visible:
        text: "Se connecter"
    file: auth.yaml

# Naviguer vers l'onglet Profil
- tapOn:
    text: "Profil"

- waitForAnimationToEnd

# Vérifier que le nom est visible (initiales dans l'avatar ou nom complet)
- assertVisible:
    id: "profil-screen"
    optional: true    # fallback si pas de testId

# Vérifier l'email ou le rôle (au moins un doit être visible)
- assertVisible:
    text: "Locataire"
    optional: true
- assertVisible:
    text: "Propriétaire"
    optional: true
- assertVisible:
    text: "test@immo-ci.com"
    optional: true

# Vérifier la section KYC
- assertVisible:
    text: "Statut KYC"

# Vérifier que le badge statut est présent (une des valeurs possibles)
- assertVisible:
    text: "Non soumis"
    optional: true
- assertVisible:
    text: "En cours"
    optional: true
- assertVisible:
    text: "Vérifié"
    optional: true

# Vérifier le bouton déconnexion
- assertVisible:
    text: "Déconnexion"
```

**Instructions d'exécution Maestro (à inclure dans SUMMARY.md):**

```bash
# Installation Maestro CLI (macOS/Linux uniquement — Windows: WSL2)
curl -Ls "https://get.maestro.mobile.dev" | bash

# Exécution sur device connecté (USB ou WiFi ADB)
maestro test apps/mobile/.maestro/flows/auth.yaml
maestro test apps/mobile/.maestro/flows/search-biens.yaml
maestro test apps/mobile/.maestro/flows/profil.yaml

# Exécution depuis EAS Workflows (CI)
# Ajouter dans .eas/workflows/e2e.yml:
# - uses: eas/maestro-test
#   with:
#     flow: apps/mobile/.maestro/flows/auth.yaml
```
  </action>
  <verify>
    <automated>ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\.maestro\flows\auth.yaml" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\.maestro\flows\search-biens.yaml" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\.maestro\flows\profil.yaml" && echo "Flows Maestro créés" && grep "appId: ci.immo.app" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\.maestro\flows\auth.yaml"</automated>
  </verify>
  <done>3 flows Maestro YAML créés pour auth/search-biens/profil avec appId ci.immo.app, assertions sur éléments texte UI.</done>
</task>

</tasks>

<verification>
```bash
# 1. Playwright config a le bon testDir
grep "testDir" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\playwright.config.ts"

# 2. Les 3 specs existent
ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\tests\e2e"

# 3. Specs utilisent des role selectors (pas de sélecteurs CSS fragiles)
grep "getByRole\|getByText\|getByPlaceholder" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\tests\e2e\auth.spec.ts"

# 4. Tous les flows ont appId correct
grep "appId" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\mobile\.maestro\flows\auth.yaml"

# 5. Playwright est installé dans apps/web
ls "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\node_modules\.bin\playwright" 2>/dev/null || grep "@playwright/test" "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web\package.json"

# 6. Vérification syntaxe specs (TypeScript)
cd "C:\Users\WILFRIED\OneDrive - Gravel Ivoire\Bureau\immo\apps\web" && npx tsc --noEmit 2>&1 | head -10
```
</verification>

<success_criteria>
1. `npx playwright test --list` depuis `apps/web/` liste les 3 specs et leurs tests (9+ tests au total)
2. `playwright.config.ts` a `testDir: './tests/e2e'`, `webServer.command: 'npm run start'`, `reuseExistingServer: !process.env.CI`
3. `auth.spec.ts` couvre connexion réussie, erreur mauvais mot de passe, déconnexion
4. `reservation.spec.ts` couvre liste biens, filtre commune, fiche bien, CTA réserver sans auth
5. `dashboard.spec.ts` couvre KPI cards, section graphique, pas d'erreur 500
6. Les 3 flows Maestro ont `appId: ci.immo.app` et utilisent `assertVisible` sur des textes UI réels
7. Le flow auth.yaml teste login + navigation onglets + logout + retour login
8. Script `"e2e": "playwright test"` ajouté dans `apps/web/package.json`
</success_criteria>

<output>
After completion, create `.planning/phases/05-app-mobile-tests-d-ploiement/05-03-SUMMARY.md` documenting:
- Playwright version installed
- Résultat de `npx playwright test --list` (nombre de tests listés)
- Comptes test à créer en staging (emails + rôles)
- Instructions Maestro (platform, comment installer, device requis)
- Flows Maestro non exécutables sur CI Windows (WSL2 requis)
</output>
