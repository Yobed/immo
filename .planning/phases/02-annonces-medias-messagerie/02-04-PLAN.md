---
phase: 02-annonces-medias-messagerie
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/web/lib/mapbox.ts
  - apps/web/components/search/SearchBar.tsx
  - apps/web/components/search/SearchFilters.tsx
  - apps/web/components/bien/BienCard.tsx
  - apps/web/components/map/PropertiesMap.tsx
  - apps/web/app/(public)/recherche/page.tsx
autonomous: true
requirements: [BIEN-05, BIEN-06, BIEN-08]

user_setup:
  - service: mapbox
    why: "Affichage carte interactive des biens"
    env_vars:
      - name: NEXT_PUBLIC_MAPBOX_TOKEN
        source: "Mapbox Dashboard (mapbox.com) → Account → Access Tokens → Create a token"
    dashboard_config:
      - task: "Créer un access token Mapbox"
        location: "https://account.mapbox.com/access-tokens/"
        details: "Token type: Public (pk.*). Scopes: styles:read, tiles:read. Allowed URLs: ajouter votre domaine Vercel"

must_haves:
  truths:
    - "Visiteur peut rechercher des biens par texte (full-text search français sur titre, description, commune)"
    - "Visiteur peut filtrer par commune, prix min/max, type de bien, et équipements"
    - "Résultats apparaissent en moins de 500ms après soumission du formulaire de recherche"
    - "Visiteur peut basculer entre vue grille (BienCards) et vue carte (Mapbox markers)"
    - "Carte Mapbox affiche les biens avec markers prix FCFA; clic marker → fiche bien"
  artifacts:
    - path: "apps/web/app/(public)/recherche/page.tsx"
      provides: "Page recherche Server Component avec FTS + filtres + vue carte"
      contains: "textSearch"
    - path: "apps/web/components/map/PropertiesMap.tsx"
      provides: "react-map-gl carte Mapbox centrée sur Abidjan"
      contains: "dynamic.*react-map-gl.*ssr.*false"
    - path: "apps/web/components/search/SearchFilters.tsx"
      provides: "Formulaire filtres commune/prix/type/équipements"
      contains: "commune\|prix_min\|prix_max\|type_bien"
  key_links:
    - from: "apps/web/app/(public)/recherche/page.tsx"
      to: "supabase.from('biens').textSearch"
      via: "Server Component — searchParams.q déclenche la requête FTS"
      pattern: "textSearch.*fts.*french"
    - from: "apps/web/components/map/PropertiesMap.tsx"
      to: "react-map-gl Map"
      via: "dynamic import ssr:false"
      pattern: "dynamic.*Map.*ssr.*false"
---

<objective>
Construire la page de recherche avec full-text search PostgreSQL (FTS français sur la colonne `fts` tsvector), filtres combinés (commune, prix, type, équipements), vue grille BienCards et vue carte Mapbox avec markers prix FCFA.

Purpose: Les visiteurs doivent pouvoir trouver des biens. Sans recherche, la liste paginée `/biens` est inutilisable au-delà de 50 annonces.
Output: Page /recherche avec SearchBar, SearchFilters, grille résultats, PropertiesMap carte Mapbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-annonces-medias-messagerie/02-RESEARCH.md
@packages/shared/types/database.ts
@apps/web/components/ui/index.ts
@apps/web/lib/supabase/server.ts
@apps/web/components/bien/BienCard.tsx
@.planning/phases/02-annonces-medias-messagerie/02-01-SUMMARY.md
</context>

<interfaces>
<!-- FTS pattern depuis RESEARCH.md Pattern 7 -->
```typescript
// Column fts est un tsvector généré (migration 002) sur titre || description || commune || quartier
// Requête FTS avec Supabase:
const { data } = await supabase
  .from('biens')
  .select('...')
  .eq('statut', 'publie')
  .textSearch('fts', query, { type: 'plain', config: 'french' })
// IMPORTANT: type: 'plain' gère les apostrophes (Plateau d'Abidjan) automatiquement
// Ne jamais interpoler user input dans raw SQL

// Filtres combinés:
if (commune) query = query.eq('commune', commune)
if (prixMin) query = query.gte('prix_mois_fcfa', prixMin)
if (prixMax) query = query.lte('prix_mois_fcfa', prixMax)
if (typeBien) query = query.eq('type_bien', typeBien)
if (equipements.length) query = query.contains('equipements', equipements)
```

<!-- Mapbox pattern depuis RESEARCH.md Pattern 8 -->
```typescript
// mapbox-gl accède à window sur import — dynamic import obligatoire
const Map = dynamic(() => import('react-map-gl').then(m => m.Map), { ssr: false })
const Marker = dynamic(() => import('react-map-gl').then(m => m.Marker), { ssr: false })
// Centre Abidjan: longitude: -4.008256, latitude: 5.352781, zoom: 11
// Token: process.env.NEXT_PUBLIC_MAPBOX_TOKEN
// Style: "mapbox://styles/mapbox/streets-v12"
```

<!-- BienCard interface (depuis 02-01) -->
```typescript
// BienCard accepte: id, titre, commune, type_bien, prix_mois_fcfa, prix_vente_fcfa,
//                   superficie_m2, nb_pieces, photo_url, statut
```

<!-- searchParams depuis Next.js App Router -->
```typescript
// Page Server Component reçoit searchParams automatiquement
// ?q=texte&commune=Cocody&prix_min=100000&prix_max=500000&type_bien=appartement&page=0
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: lib/mapbox.ts + PropertiesMap (Mapbox dynamic) + SearchFilters</name>
  <files>
    apps/web/lib/mapbox.ts,
    apps/web/components/map/PropertiesMap.tsx,
    apps/web/components/search/SearchFilters.tsx
  </files>
  <read_first>
    - .planning/phases/02-annonces-medias-messagerie/02-RESEARCH.md (Pattern 8: Mapbox dynamic import)
    - packages/shared/constants/communes.ts (COMMUNES_CI — liste des communes)
    - packages/shared/constants/biens.ts (TYPES_BIEN_LABELS, EQUIPEMENTS_LABELS)
    - apps/web/components/ui/index.ts (Button, Input)
    - apps/web/tailwind.config.ts (tokens rounded-pill, font-mono pour prix markers)
  </read_first>
  <action>
**Créer apps/web/lib/mapbox.ts:**
```typescript
// Centralise l'accès token Mapbox; utilisé par PropertiesMap
export const MAPBOX_TOKEN = process.env.NEXT_PUBLIC_MAPBOX_TOKEN ?? ''

// Centre géographique d'Abidjan
export const ABIDJAN_CENTER = {
  longitude: -4.008256,
  latitude:  5.352781,
  zoom:      11,
} as const
```

**Créer apps/web/components/map/PropertiesMap.tsx:**

ANTI-PATTERN CRITIQUE: Ne JAMAIS importer `mapbox-gl` ou `react-map-gl` en import statique. Toujours `dynamic(..., { ssr: false })`. Erreur: `ReferenceError: window is not defined`.

```tsx
'use client'
import dynamic from 'next/dynamic'
import { useState, useCallback } from 'react'
import Link from 'next/link'
import { MAPBOX_TOKEN, ABIDJAN_CENTER } from '@/lib/mapbox'

// Imports dynamiques obligatoires — mapbox-gl accède à window sur import
const Map = dynamic(() => import('react-map-gl').then((m) => m.Map), { ssr: false })
const Marker = dynamic(() => import('react-map-gl').then((m) => m.Marker), { ssr: false })
const Popup = dynamic(() => import('react-map-gl').then((m) => m.Popup), { ssr: false })

interface BienMarker {
  id: string
  titre: string
  commune: string
  latitude: number | null
  longitude: number | null
  prix_mois_fcfa: number | null
  prix_vente_fcfa: number | null
}

function formatFCFA(n: number): string {
  return new Intl.NumberFormat('fr-CI', {
    style: 'currency',
    currency: 'XOF',
    maximumFractionDigits: 0,
    notation: 'compact',
  }).format(n)
}

interface PropertiesMapProps {
  biens: BienMarker[]
  hauteur?: number
}

export function PropertiesMap({ biens, hauteur = 500 }: PropertiesMapProps) {
  const [selectedBien, setSelectedBien] = useState<BienMarker | null>(null)

  const handleMarkerClick = useCallback((bien: BienMarker) => {
    setSelectedBien((prev) => prev?.id === bien.id ? null : bien)
  }, [])

  const biensWithCoords = biens.filter(
    (b) => b.latitude !== null && b.longitude !== null
  )

  if (!MAPBOX_TOKEN) {
    return (
      <div
        className="w-full rounded-card bg-[var(--surface)] flex items-center justify-center border border-[var(--border)]"
        style={{ height: hauteur }}
      >
        <p className="text-muted font-sans text-sm">
          Carte non disponible — configurer NEXT_PUBLIC_MAPBOX_TOKEN
        </p>
      </div>
    )
  }

  return (
    <div className="w-full rounded-card overflow-hidden border border-[var(--border)]" style={{ height: hauteur }}>
      <Map
        mapboxAccessToken={MAPBOX_TOKEN}
        initialViewState={ABIDJAN_CENTER}
        style={{ width: '100%', height: '100%' }}
        mapStyle="mapbox://styles/mapbox/streets-v12"
      >
        {biensWithCoords.map((bien) => (
          <Marker
            key={bien.id}
            longitude={bien.longitude!}
            latitude={bien.latitude!}
            anchor="bottom"
          >
            <button
              onClick={() => handleMarkerClick(bien)}
              className="bg-secondary text-white text-xs font-mono px-2 py-1 rounded-pill shadow-md hover:bg-secondary/90 transition-colors whitespace-nowrap"
            >
              {bien.prix_mois_fcfa
                ? formatFCFA(bien.prix_mois_fcfa)
                : bien.prix_vente_fcfa
                ? formatFCFA(bien.prix_vente_fcfa)
                : '—'}
            </button>
          </Marker>
        ))}

        {selectedBien && selectedBien.longitude && selectedBien.latitude && (
          <Popup
            longitude={selectedBien.longitude}
            latitude={selectedBien.latitude}
            anchor="top"
            onClose={() => setSelectedBien(null)}
            closeButton
            closeOnClick={false}
          >
            <div className="p-2 max-w-[200px]">
              <p className="font-sans text-sm font-medium text-[var(--text)] line-clamp-2 mb-1">
                {selectedBien.titre}
              </p>
              <p className="text-xs text-muted font-sans mb-2">{selectedBien.commune}</p>
              <Link
                href={`/biens/${selectedBien.id}`}
                className="text-xs text-primary font-sans underline hover:no-underline"
              >
                Voir la fiche →
              </Link>
            </div>
          </Popup>
        )}
      </Map>
    </div>
  )
}
```

**Créer apps/web/components/search/SearchFilters.tsx:**
```tsx
'use client'
import { useRouter, useSearchParams, usePathname } from 'next/navigation'
import { useState, useCallback } from 'react'
import { Button, Input } from '@/components/ui'
import { COMMUNES_CI } from '@immo-ci/shared/constants/communes'
import { TYPES_BIEN, TYPES_BIEN_LABELS, EQUIPEMENTS_DISPONIBLES, EQUIPEMENTS_LABELS } from '@immo-ci/shared/constants/biens'
import { cn } from '@/lib/utils'

export function SearchFilters() {
  const router = useRouter()
  const pathname = usePathname()
  const searchParams = useSearchParams()

  const [commune, setCommune] = useState(searchParams.get('commune') ?? '')
  const [prixMin, setPrixMin] = useState(searchParams.get('prix_min') ?? '')
  const [prixMax, setPrixMax] = useState(searchParams.get('prix_max') ?? '')
  const [typeBien, setTypeBien] = useState(searchParams.get('type_bien') ?? '')
  const [equipements, setEquipements] = useState<string[]>(
    searchParams.get('equipements')?.split(',').filter(Boolean) ?? []
  )

  const toggleEquipement = (eq: string) => {
    setEquipements((prev) =>
      prev.includes(eq) ? prev.filter((e) => e !== eq) : [...prev, eq]
    )
  }

  const applyFilters = useCallback(() => {
    const params = new URLSearchParams(searchParams.toString())
    params.set('page', '0')
    if (commune) params.set('commune', commune); else params.delete('commune')
    if (prixMin) params.set('prix_min', prixMin); else params.delete('prix_min')
    if (prixMax) params.set('prix_max', prixMax); else params.delete('prix_max')
    if (typeBien) params.set('type_bien', typeBien); else params.delete('type_bien')
    if (equipements.length) params.set('equipements', equipements.join(',')); else params.delete('equipements')
    router.push(`${pathname}?${params.toString()}`)
  }, [commune, prixMin, prixMax, typeBien, equipements, pathname, searchParams, router])

  const clearFilters = () => {
    setCommune('')
    setPrixMin('')
    setPrixMax('')
    setTypeBien('')
    setEquipements([])
    const q = searchParams.get('q')
    router.push(`${pathname}${q ? `?q=${q}` : ''}`)
  }

  const hasActiveFilters = commune || prixMin || prixMax || typeBien || equipements.length > 0

  return (
    <div className="bg-white rounded-card border border-[var(--border)] p-5 space-y-5">
      <div className="flex items-center justify-between">
        <h3 className="font-sans font-medium text-[var(--text)]">Filtres</h3>
        {hasActiveFilters && (
          <button onClick={clearFilters} className="text-xs text-danger font-sans hover:underline">
            Effacer tout
          </button>
        )}
      </div>

      {/* Commune */}
      <div>
        <label className="block text-sm font-sans font-medium text-[var(--text)] mb-2">Commune</label>
        <select
          value={commune}
          onChange={(e) => setCommune(e.target.value)}
          className="w-full rounded-btn border border-[var(--border)] px-3 py-2 text-sm font-sans focus:outline-none focus:ring-2 focus:ring-primary/30 bg-white"
        >
          <option value="">Toutes les communes</option>
          {COMMUNES_CI.map((c) => <option key={c} value={c}>{c}</option>)}
        </select>
      </div>

      {/* Type de bien */}
      <div>
        <label className="block text-sm font-sans font-medium text-[var(--text)] mb-2">Type de bien</label>
        <select
          value={typeBien}
          onChange={(e) => setTypeBien(e.target.value)}
          className="w-full rounded-btn border border-[var(--border)] px-3 py-2 text-sm font-sans focus:outline-none focus:ring-2 focus:ring-primary/30 bg-white"
        >
          <option value="">Tous les types</option>
          {TYPES_BIEN.map((t) => <option key={t} value={t}>{TYPES_BIEN_LABELS[t]}</option>)}
        </select>
      </div>

      {/* Prix */}
      <div>
        <label className="block text-sm font-sans font-medium text-[var(--text)] mb-2">Prix mensuel (FCFA)</label>
        <div className="grid grid-cols-2 gap-2">
          <Input
            placeholder="Min (ex: 100000)"
            type="number"
            value={prixMin}
            onChange={(e) => setPrixMin(e.target.value)}
          />
          <Input
            placeholder="Max (ex: 500000)"
            type="number"
            value={prixMax}
            onChange={(e) => setPrixMax(e.target.value)}
          />
        </div>
      </div>

      {/* Équipements */}
      <div>
        <label className="block text-sm font-sans font-medium text-[var(--text)] mb-2">Équipements</label>
        <div className="grid grid-cols-2 gap-1.5">
          {EQUIPEMENTS_DISPONIBLES.map((eq) => (
            <button
              key={eq}
              type="button"
              onClick={() => toggleEquipement(eq)}
              className={cn(
                'text-left px-2 py-1.5 rounded-btn text-xs font-sans border transition-colors',
                equipements.includes(eq)
                  ? 'border-primary bg-primary-light text-primary font-medium'
                  : 'border-[var(--border)] text-muted hover:border-primary/40'
              )}
            >
              {EQUIPEMENTS_LABELS[eq]}
            </button>
          ))}
        </div>
      </div>

      <Button className="w-full" onClick={applyFilters}>
        Appliquer les filtres
      </Button>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit 2>&1 | grep -E "(PropertiesMap|SearchFilters|mapbox)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "^import.*mapbox\|^import.*react-map-gl" apps/web/components/map/PropertiesMap.tsx retourne 0 lignes (pas d'import statique!)
    - grep "dynamic.*Map.*ssr.*false\|ssr: false" apps/web/components/map/PropertiesMap.tsx retourne au moins 1 ligne
    - grep "MAPBOX_TOKEN" apps/web/lib/mapbox.ts retourne une ligne
    - grep "longitude.*-4.008256\|ABIDJAN_CENTER" apps/web/components/map/PropertiesMap.tsx retourne au moins une ligne
    - grep "commune\|prix_min\|prix_max\|type_bien" apps/web/components/search/SearchFilters.tsx retourne au moins 4 lignes
    - grep "EQUIPEMENTS_DISPONIBLES" apps/web/components/search/SearchFilters.tsx retourne une ligne
    - npx tsc --noEmit dans apps/web ne produit pas d'erreur sur ces fichiers
  </acceptance_criteria>
  <done>lib/mapbox.ts centralisé, PropertiesMap avec dynamic import ssr:false, Popup au clic marker, SearchFilters avec commune/prix/type/équipements persistés dans searchParams.</done>
</task>

<task type="auto">
  <name>Task 2: SearchBar + page /recherche (FTS + filtres + vue grille/carte)</name>
  <files>
    apps/web/components/search/SearchBar.tsx,
    apps/web/app/(public)/recherche/page.tsx
  </files>
  <read_first>
    - .planning/phases/02-annonces-medias-messagerie/02-RESEARCH.md (Pattern 7: FTS Supabase, requête combinée filtres+FTS)
    - apps/web/lib/supabase/server.ts (createClient server)
    - apps/web/components/bien/BienCard.tsx (props interface)
    - apps/web/components/search/SearchFilters.tsx (créé en Task 1)
    - apps/web/components/map/PropertiesMap.tsx (créé en Task 1)
  </read_first>
  <action>
**Créer apps/web/components/search/SearchBar.tsx:**
```tsx
'use client'
import { useState, useTransition } from 'react'
import { useRouter, useSearchParams, usePathname } from 'next/navigation'
import { Button } from '@/components/ui'

interface SearchBarProps {
  placeholder?: string
  className?: string
}

export function SearchBar({
  placeholder = 'Rechercher un bien (commune, type, quartier...)',
  className = '',
}: SearchBarProps) {
  const router = useRouter()
  const pathname = usePathname()
  const searchParams = useSearchParams()
  const [query, setQuery] = useState(searchParams.get('q') ?? '')
  const [isPending, startTransition] = useTransition()

  const handleSearch = (e: React.FormEvent) => {
    e.preventDefault()
    const params = new URLSearchParams(searchParams.toString())
    params.set('page', '0')
    if (query.trim()) {
      params.set('q', query.trim())
    } else {
      params.delete('q')
    }
    startTransition(() => {
      router.push(`/recherche?${params.toString()}`)
    })
  }

  return (
    <form onSubmit={handleSearch} className={`flex gap-2 ${className}`}>
      <input
        type="search"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        placeholder={placeholder}
        className="flex-1 rounded-btn border border-[var(--border)] px-4 py-2.5 text-sm font-sans focus:outline-none focus:ring-2 focus:ring-primary/30 min-w-0"
      />
      <Button type="submit" isLoading={isPending}>
        Rechercher
      </Button>
    </form>
  )
}
```

**Créer apps/web/app/(public)/recherche/page.tsx:**
```tsx
import { createClient } from '@/lib/supabase/server'
import { BienCard } from '@/components/bien/BienCard'
import { SearchBar } from '@/components/search/SearchBar'
import { SearchFilters } from '@/components/search/SearchFilters'
import { PropertiesMap } from '@/components/map/PropertiesMap'

const PAGE_SIZE = 12

interface SearchParams {
  q?: string
  commune?: string
  prix_min?: string
  prix_max?: string
  type_bien?: string
  equipements?: string
  vue?: 'grille' | 'carte'
  page?: string
}

export default async function RecherchePage({
  searchParams,
}: {
  searchParams: SearchParams
}) {
  const supabase = await createClient()
  const page = Math.max(0, parseInt(searchParams.page ?? '0', 10))
  const vue = searchParams.vue ?? 'grille'

  // Construire la requête Supabase dynamiquement
  let dbQuery = supabase
    .from('biens')
    .select(`
      id, titre, commune, type_bien, prix_mois_fcfa, prix_vente_fcfa,
      superficie_m2, nb_pieces, latitude, longitude,
      biens_medias(url, est_couverture, ordre, type)
    `, { count: 'exact' })
    .eq('statut', 'publie')

  // Full-text search
  // type: 'plain' gère les apostrophes automatiquement (Plateau d'Abidjan)
  if (searchParams.q?.trim()) {
    dbQuery = dbQuery.textSearch('fts', searchParams.q.trim(), {
      type: 'plain',
      config: 'french',
    })
  }

  // Filtres combinés
  if (searchParams.commune) {
    dbQuery = dbQuery.eq('commune', searchParams.commune)
  }
  if (searchParams.prix_min) {
    dbQuery = dbQuery.gte('prix_mois_fcfa', parseInt(searchParams.prix_min, 10))
  }
  if (searchParams.prix_max) {
    dbQuery = dbQuery.lte('prix_mois_fcfa', parseInt(searchParams.prix_max, 10))
  }
  if (searchParams.type_bien) {
    dbQuery = dbQuery.eq('type_bien', searchParams.type_bien)
  }
  if (searchParams.equipements) {
    const equipList = searchParams.equipements.split(',').filter(Boolean)
    if (equipList.length > 0) {
      // Opérateur @> : tous les équipements doivent être présents
      dbQuery = dbQuery.contains('equipements', equipList)
    }
  }

  const { data: biens, count } = await dbQuery
    .order('created_at', { ascending: false })
    .range(page * PAGE_SIZE, (page + 1) * PAGE_SIZE - 1)

  const totalPages = Math.ceil((count ?? 0) / PAGE_SIZE)
  const totalResults = count ?? 0

  const hasFilters = searchParams.q || searchParams.commune || searchParams.prix_min ||
    searchParams.prix_max || searchParams.type_bien || searchParams.equipements

  return (
    <main className="bg-surface min-h-screen py-8">
      <div className="max-w-7xl mx-auto px-4">
        {/* Search bar */}
        <div className="mb-6">
          <SearchBar className="max-w-2xl" />
        </div>

        <div className="flex gap-6">
          {/* Sidebar filtres */}
          <aside className="hidden lg:block w-64 flex-shrink-0">
            <SearchFilters />
          </aside>

          {/* Résultats */}
          <div className="flex-1 min-w-0">
            {/* Header résultats */}
            <div className="flex items-center justify-between mb-4">
              <p className="text-sm text-muted font-sans">
                {totalResults === 0
                  ? 'Aucun résultat'
                  : `${totalResults} bien${totalResults > 1 ? 's' : ''} trouvé${totalResults > 1 ? 's' : ''}`}
                {searchParams.q && <span className="text-[var(--text)]"> pour « {searchParams.q} »</span>}
              </p>
              {/* Toggle grille / carte */}
              <div className="flex gap-2">
                <a
                  href={`/recherche?${new URLSearchParams({ ...searchParams, vue: 'grille' }).toString()}`}
                  className={`px-3 py-1.5 rounded-btn text-sm font-sans border transition-colors ${
                    vue === 'grille'
                      ? 'bg-primary text-white border-primary'
                      : 'border-[var(--border)] text-muted hover:border-primary/40'
                  }`}
                >
                  Grille
                </a>
                <a
                  href={`/recherche?${new URLSearchParams({ ...searchParams, vue: 'carte' }).toString()}`}
                  className={`px-3 py-1.5 rounded-btn text-sm font-sans border transition-colors ${
                    vue === 'carte'
                      ? 'bg-primary text-white border-primary'
                      : 'border-[var(--border)] text-muted hover:border-primary/40'
                  }`}
                >
                  Carte
                </a>
              </div>
            </div>

            {/* Vue carte */}
            {vue === 'carte' && biens && biens.length > 0 && (
              <div className="mb-6">
                <PropertiesMap
                  biens={(biens as any[]).map((b) => ({
                    id: b.id,
                    titre: b.titre,
                    commune: b.commune,
                    latitude: b.latitude,
                    longitude: b.longitude,
                    prix_mois_fcfa: b.prix_mois_fcfa,
                    prix_vente_fcfa: b.prix_vente_fcfa,
                  }))}
                  hauteur={450}
                />
              </div>
            )}

            {/* Vue grille */}
            {(!biens || biens.length === 0) ? (
              <div className="text-center py-16">
                <p className="text-muted font-sans mb-2">Aucun bien ne correspond à votre recherche.</p>
                {hasFilters && (
                  <a href="/recherche" className="text-sm text-primary font-sans hover:underline">
                    Effacer les filtres
                  </a>
                )}
              </div>
            ) : (
              <>
                <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-5">
                  {(biens as any[]).map((bien) => {
                    const medias = (bien.biens_medias ?? []) as Array<{ url: string; est_couverture: boolean; ordre: number; type: string }>
                    const cover = medias
                      .filter((m) => m.type === 'photo')
                      .sort((a, b) => (a.est_couverture ? -1 : 1) || a.ordre - b.ordre)[0]
                    return (
                      <BienCard
                        key={bien.id}
                        id={bien.id}
                        titre={bien.titre}
                        commune={bien.commune}
                        type_bien={bien.type_bien}
                        prix_mois_fcfa={bien.prix_mois_fcfa}
                        prix_vente_fcfa={bien.prix_vente_fcfa}
                        superficie_m2={bien.superficie_m2}
                        nb_pieces={bien.nb_pieces}
                        photo_url={cover?.url ?? null}
                      />
                    )
                  })}
                </div>

                {/* Pagination */}
                {totalPages > 1 && (
                  <div className="flex justify-center gap-2 mt-8">
                    {Array.from({ length: Math.min(totalPages, 10) }, (_, i) => (
                      <a
                        key={i}
                        href={`/recherche?${new URLSearchParams({ ...searchParams, page: String(i) }).toString()}`}
                        className={`px-4 py-2 rounded-btn text-sm font-sans border transition-colors ${
                          i === page
                            ? 'bg-primary text-white border-primary'
                            : 'border-[var(--border)] text-muted hover:border-primary/40'
                        }`}
                      >
                        {i + 1}
                      </a>
                    ))}
                  </div>
                )}
              </>
            )}
          </div>
        </div>
      </div>
    </main>
  )
}
```
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit 2>&1 | grep -E "(recherche|SearchBar|SearchFilters)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "textSearch.*fts.*french" apps/web/app/\(public\)/recherche/page.tsx retourne une ligne
    - grep "type.*plain" apps/web/app/\(public\)/recherche/page.tsx retourne une ligne (protection apostrophes)
    - grep "contains.*equipements" apps/web/app/\(public\)/recherche/page.tsx retourne une ligne (opérateur @>)
    - grep "vue.*carte\|vue.*grille" apps/web/app/\(public\)/recherche/page.tsx retourne au moins 2 lignes
    - grep "PropertiesMap" apps/web/app/\(public\)/recherche/page.tsx retourne au moins 2 lignes (import + usage)
    - grep "startTransition" apps/web/components/search/SearchBar.tsx retourne une ligne
    - npx tsc --noEmit dans apps/web ne produit pas d'erreur TypeScript sur ces fichiers
  </acceptance_criteria>
  <done>SearchBar avec useTransition, page /recherche Server Component avec FTS type:plain + 5 filtres combinés + toggle grille/carte + pagination + PropertiesMap conditionnelle.</done>
</task>

</tasks>

<verification>
```bash
# 1. Fichiers créés
ls apps/web/lib/mapbox.ts
ls apps/web/components/search/SearchBar.tsx apps/web/components/search/SearchFilters.tsx
ls apps/web/components/map/PropertiesMap.tsx
ls apps/web/app/\(public\)/recherche/page.tsx

# 2. Pas d'import statique mapbox-gl (anti-pattern)
grep "^import.*mapbox-gl\|^import.*react-map-gl" apps/web/components/map/PropertiesMap.tsx && echo "ERREUR DETECTEE"

# 3. FTS correctement configuré
grep "textSearch.*french" apps/web/app/\(public\)/recherche/page.tsx

# 4. TypeScript propre
cd apps/web && npx tsc --noEmit 2>&1 | wc -l
```
</verification>

<success_criteria>
1. Page `/recherche?q=cocody` retourne les biens dont le tsvector contient "cocody" via FTS français
2. Filtres `?commune=Cocody&type_bien=appartement&prix_max=300000` se combinent correctement
3. Requête FTS utilise `type: 'plain'` — pas d'injection SQL possible
4. Carte Mapbox s'affiche avec `?vue=carte` (requiert NEXT_PUBLIC_MAPBOX_TOKEN)
5. Si MAPBOX_TOKEN absent: message "Carte non disponible" — pas de crash
6. `npx tsc --noEmit` retourne exit code 0
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-annonces-medias-messagerie/02-04-SUMMARY.md`
</output>
