# PLAN 1006-01: Correction Géo et Déploiement RPC

**Goal:** Résoudre le problème d'affichage de la carte "Near Me" en déployant l'infrastructure SQL nécessaire et en fiabilisant le frontend.

## Tasks

### 1. Infrastructure SQL
- [x] Créer `supabase/migrations/011_geo_and_schema_fix.sql` avec `get_biens_proches`
- [x] Créer `supabase/seed_coords.sql` pour les données de test

### 2. Frontend Fiabilisation
- [x] Mettre à jour `NearMeSection.tsx` pour gérer les erreurs RPC et élargir le rayon de recherche.
- [ ] Vérifier que `PropertiesMap.tsx` affiche correctement les marqueurs avec le nouveau style (9px, sans FCFA).
- [ ] S'assurer que le bouton "Ma position" déclenche bien la recherche RPC.

### 3. Déploiement & Validation
- [ ] Déployer les migrations SQL sur Supabase (Nécessite intervention manuelle ou `npx supabase db push` si configuré).
- [ ] Déploiement groupé sur Vercel : `vercel deploy --prod --force`.
- [ ] Vérification manuelle sur le site de production.

## Verification
- L'appel RPC `get_biens_proches` via `(supabase as any).rpc(...)` ne renvoie plus d'erreur 404 (function not found).
- La carte se peuple de marqueurs même si l'utilisateur refuse la géolocalisation (grâce au fallback "Dernières Annonces").
- Le design des marqueurs est conforme : épuré, typographie luxueuse.
